# 阶段 15：RAG 知识库（会话级绑定 + Memory 向量库）

> 给聊天应用接入"文档问答"能力：用户上传文档 → 服务端切块+嵌入+存向量库 → 聊天时检索相关片段注入 prompt → AI 基于文档回答。
>
> 这是项目从"通用聊天"升级为"知识库助手"的关键一步，也是 LangChain + RAG 在简历上的核心卖点。
>
> **项目独立**：本阶段在 `Nextjs-RAG` 项目（从原 Nextjs clone 出来）上做，原项目保持阶段 1-14 干净版。

---

## 一、核心设计：RAG 是 chat 的"前置步骤"，不接管 chat

最重要的架构决策：**现有 `/api/chat` 的流式/中断/错误处理逻辑零改动**。RAG 只做一件事——在调用 LLM 前，检索相关片段，拼进 system message。

```
现有流程（保留）：
  用户消息 → fetch /api/chat → 转发上游 SSE → 流式回复

加了 RAG 后：
  用户消息 + kbId
    ↓
  [RAG 前置] 检索 kbId 知识库里 top-3 相关块 → 拼进 system
    ↓
  照常 fetch 上游 SSE → 流式回复（带上了知识上下文）
```

**为什么不引入 ChatOpenAI.stream()**：保留阶段 4-11 打磨的裸 fetch SSE 链路（五轮 debug 换来的中断/错误处理）。ChatOpenAI 的引入留给路线 B（阶段 16+）。LangChain 在本阶段只负责"嵌入 + 检索"两件事。

---

## 二、向量库选型：为什么用 MemoryVectorStore + 抽象层

### 三种向量库对比

| | MemoryVectorStore | Chroma | FAISS |
|---|---|---|---|
| 存储 | 进程内存（globalThis 单例） | Docker 容器本地文件 | 本地 .faiss + .pkl 文件 |
| 重启丢吗 | 丢（重启重嵌入） | 不丢 | 不丢 |
| 装环境 | **不装**（纯 JS） | 要 Docker | 要 node-gyp 编译 |
| 换电脑 | **npm install 即可** | 重装 Docker + 重起容器 | 重新编译（Windows 坑大） |
| Windows 坑 | 无 | WSL2/虚拟化/权限 | Python + VS Build Tools |
| 适合 | 学习项目、文档量小 | 生产环境 | 不推荐（Windows） |

### 决策：选 MemoryVectorStore

**原因**：
1. 零环境依赖——不装 Docker、不编译，换电脑 npm install 就能跑
2. 学习项目文档量小，重启重新嵌入几秒，无所谓
3. 符合"换电脑无痛"的实际需求

**局限（已知，不遮掩）**：
- 重启进程要重新嵌入（文档多时慢）
- 无法部署上线（serverless 重启就丢）
- 不支持单文档级删除（只能整个 kb 删）

### 化解局限：抽象层 + 对比文档 + 待办

**抽象层**（`lib/vectorstore.ts`）：业务代码只依赖 `VectorStore` 接口，未来换 Chroma 只改这一个文件。

```ts
interface VectorStore {
  addDocuments(kbId, chunks, meta): Promise<void>;
  similaritySearch(kbId, query, k): Promise<string[]>;
  delete(kbId): Promise<void>;
}

// 当前实现
class MemoryStore implements VectorStore { ... }
// 未来：class ChromaStore implements VectorStore { ... }
```

**简历措辞**：不写"用了 MemoryVectorStore"（弱），写"设计可插拔向量库抽象层，对比评估 Memory/Chroma/FAISS 三种方案的工程取舍，当前用 Memory 实现，规划支持 Chroma 部署"（强、诚实、有思考）。

---

## 三、RAG 原理：切块、嵌入、检索、注入

### 为什么要把文档切块（chunk）

LLM 上下文窗口有限（几K到几十K token），整篇文档塞不进去。更重要的是**检索精度**——用户问"RAG 为什么要切块"，我们只想找出讲这个的那一段，不是把整篇文档都塞给 LLM。切块让"检索"能精准定位到相关片段。

### 切块参数

| 参数 | 默认值 | 含义 |
|---|---|---|
| chunkSize | 500 字符 | 每块多大。太小丢上下文，太大检索不精准 |
| chunkOverlap | 50 字符 | 相邻块重叠量，避免关键信息被切到两块各一半 |

> langChain-demo 用的 chunkSize=100 太小（只适合 demo），这里用 500（中英文混合适用）。

### 切块器：RecursiveCharacterTextSplitter

递归地按 `段落 > 换行 > 句号 > 空格` 的优先级切。它会**尽量在自然边界**（段落、句子）切，避免把一句话切成两半。

### 嵌入（embedding）

把文本块转成高维向量（bge-m3 是 1024 维浮点数组）。语义相近的文本，向量也相近。这一步调外部 API（硅基流动的 bge-m3）。

### 检索（retrieval）

用户提问 → 把问题也嵌入成向量 → 在向量库里找 cosine 相似度最高的 top-k 块。返回最相关的几个片段。

### 注入（inject）

把检索到的 top-k 块拼成结构化文本，包装进 system message：

```
你是一个知识库问答助手。请基于以下检索到的知识库内容回答用户问题。
如果知识库内容不足以回答，请明确说明，不要编造。

【知识库内容】
片段1：
...
片段2：
...
```

LLM 看到这个 system + 用户问题，就会基于检索到的知识回答（而不是凭空生成）。

---

## 四、数据库 schema 变动

新增 2 张表 + 给 conversations 加 1 列：

```sql
-- 知识库表
CREATE TABLE knowledge_bases (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  created_at TEXT NOT NULL
);

-- 文档表（只存元数据，向量在向量库）
CREATE TABLE documents (
  id TEXT PRIMARY KEY,
  kb_id TEXT NOT NULL,
  filename TEXT NOT NULL,
  chunk_count INTEGER DEFAULT 0,
  created_at TEXT NOT NULL,
  FOREIGN KEY (kb_id) REFERENCES knowledge_bases(id) ON DELETE CASCADE
);

-- 会话绑定知识库（加列）
ALTER TABLE conversations ADD COLUMN kb_id TEXT REFERENCES knowledge_bases(id);
```

**为什么向量不存 SQLite**：向量是 1024 维浮点数组，存 SQLite 又大又慢且没法做相似度搜索。SQLite 只存元数据，向量交给专门的向量库。

**ALTER TABLE 的迁移处理**：SQLite 的 `ADD COLUMN` 不支持 `IF NOT EXISTS`，列已存在会报错。用 try/catch 包，首次添加才执行（见 db.ts）。

---

## 五、新增/变动文件清单

### 新增（7 个）

| 文件 | 作用 |
|---|---|
| `lib/langchain.ts` | OpenAIEmbeddings 工厂（env 驱动，复用硅基流动） |
| `lib/vectorstore.ts` | 向量库抽象层 + Memory 实现（可插拔） |
| `lib/rag.ts` | RAG 业务：splitText / indexDocument / retrieveContext / buildRagSystemPrompt |
| `app/api/knowledge/route.ts` | GET 列出 / POST 新建知识库 |
| `app/api/knowledge/[id]/route.ts` | GET 详情+文档 / DELETE 删知识库 |
| `app/api/knowledge/[id]/documents/route.ts` | POST 上传文档（切块+嵌入+存） |
| `components/KbSelector.tsx` | ChatBox 内的知识库下拉选择 |
| `components/KnowledgePanel.tsx` | 知识库管理面板（模态框） |

### 变动（5 个）

| 文件 | 改动 |
|---|---|
| `app/api/chat/route.ts` | 加 RAG 前置（有 kbId 就检索拼 system），LLM 调用逻辑零改动 |
| `app/api/conversations/[id]/route.ts` | PATCH 扩展支持改 kbId（绑定/解绑知识库） |
| `lib/db.ts` | 建知识库/文档表 + conversations 加 kb_id 列 |
| `lib/queries.ts` | 加知识库/文档 CRUD + Conversation 类型加 kbId |
| `lib/store.ts` | 加 currentKbId / knowledgeBases 状态 + actions |
| `components/ChatBox.tsx` | 发请求带 kbId + 渲染 KbSelector |
| `components/Sidebar.tsx` | 加"📚 知识库"按钮 + 渲染 KnowledgePanel |
| `app/globals.css` | 追加 RAG 相关样式 |

---

## 六、完整数据流

### 上传文档

```
用户在 KnowledgePanel 选文件 → file.text() 读内容
  ↓
POST /api/knowledge/[id]/documents { filename, content }
  ↓
indexDocument: splitText(切块) → vectorStore.addDocuments(嵌入+存)
  ↓
addDocument: SQLite 存元数据（文件名、块数）
  ↓
返回 { id, filename, chunkCount }
```

### RAG 问答

```
ChatBox 检测到 currentKbId → 发请求带 kbId
  ↓
POST /api/chat { messages, system, kbId }
  ↓
[RAG 前置] retrieveContext(kbId, 最后一条user消息, k=3)
  ↓ 检索 top-3 相关块
buildRagSystemPrompt(上下文, 原system) → 拼成新 system
  ↓
照常 fetch 上游 SSE（messages 头部多了带知识的 system）
  ↓
流式回复（AI 基于检索到的知识回答）
```

---

## 七、使用流程（用户视角）

1. 点 Sidebar 的"📚 知识库管理" → 打开面板
2. 输入名（如"前端面试"）→ 新建知识库
3. 展开该库 → 选 .txt 文件上传 → 等待"已索引 N 块"
4. 回到聊天 → ChatBox 顶部出现"📚 知识库"下拉 → 选"前端面试"
5. 提问 → AI 基于上传的文档回答（如果检索到了相关内容）

---

## 八、自测题

### Q1
为什么 RAG 要把文档切块，而不是整篇塞给 LLM？给两个理由。

### Q2
chunkOverlap（块重叠）解决什么问题？如果没有 overlap 会怎样？

### Q3
为什么本项目的向量不存 SQLite，而要单独用向量库？

### Q4
为什么 chat 路由的 LLM 调用逻辑零改动就能支持 RAG？（提示：RAG 在哪一步介入）

### Q5
MemoryVectorStore 有什么局限？简历上怎么把"用了 Memory"讲成加分项？

### Q6
`lib/vectorstore.ts` 的抽象层为什么有价值？换 Chroma 时要改哪些文件？

### Q7（进阶）
检索失败时（比如向量库挂了），chat 路由怎么处理？为什么这么设计？

<details>
<summary>答案</summary>

**A1**：① LLM 上下文窗口有限，整篇文档塞不进去 ② 检索精度——用户问某个点，只想找讲那个点的段落，不是把整篇都给 LLM（整篇会让 LLM 抓不住重点，也浪费 token）。

**A2**：避免关键信息被切到两块的边界各一半。比如"RAG 的核心是检索"这句话如果刚好在块边界，可能"RAG 的核心"在块 A、"是检索"在块 B，两个块单独看都不完整。overlap 让相邻块有重叠部分，关键信息至少在一个块里完整出现。

**A3**：向量是 1024 维浮点数组，存 SQLite 又大又慢（一行就要 4KB+），且 SQLite 没有原生向量相似度搜索能力。向量库专门优化了高维向量的存储和相似度计算（用 HNSW 等索引算法）。

**A4**：RAG 在"调用 LLM 之前"介入——检索相关块拼进 system message。LLM 调用本身（fetch 上游 SSE）不需要知道 system 是怎么来的，它只看到"有个 system message"。所以 RAG 只增强 system 内容，不改 LLM 调用方式。

**A5**：局限——重启重嵌入、无法部署上线、不支持单文档删除。讲成加分项："设计可插拔向量库抽象层，对比评估 Memory/Chroma/FAISS 三种方案，当前用 Memory 实现（学习阶段零环境依赖），抽象层支持未来平滑切换到 Chroma 部署"。重点体现"做过对比、有工程思考"，而不是"用了什么"。

**A6**：抽象层让业务代码不绑死具体向量库。换 Chroma 时只改 `lib/vectorstore.ts`（加一个 ChromaStore 实现 + 改单例导出），业务代码（rag.ts、API 路由、ChatBox）零改动。这就是"可插拔"的真实含义。

**A7**：检索失败时 try/catch 捕获，静默降级为普通对话（不带知识上下文）。这么设计是因为 RAG 是"增强"不是"必需"——检索挂了用户还能正常聊天，只是没知识加持。比"检索挂了整个聊天就不能用"体验好。

</details>

---

## 九、阶段总结与后续

### 简历可写的技术点

- 基于 LangChain 实现 RAG 全流程（文档切块→bge-m3 嵌入→向量检索→上下文注入）
- 设计可插拔向量库抽象层，对比评估 Memory/Chroma/FAISS 三种方案
- 会话级知识库绑定（每个会话可选独立知识库或纯对话模式）
- RAG 作为 chat 的前置步骤，保留现有流式/中断/错误处理打磨（架构解耦）

### 已知局限 + 后续（待学与待办.md 记录）

- 向量库换 Chroma 实现部署上线（当前 Memory，无法 serverless）
- 支持 PDF/Word（当前纯文本，文档加载器扩展）
- 检索结果溯源展示（让用户看到 AI 引用了哪几个片段）
- 路线 B：用 ChatOpenAI 替代裸 fetch（统一 LangChain 抽象）

### 环境配置提醒

`.env.local` 新增变量（已加进 .env.local.example 的更新待办）：
```
OPENAI_EMBEDDING_MODEL=BAAI/bge-m3   # 嵌入模型，独立于聊天模型
```
