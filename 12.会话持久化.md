# 阶段 12：会话持久化（node:sqlite）

> 前 11 个阶段，聊天记录只活在浏览器内存里——刷新页面就没了。这一阶段接入数据库，让记录真正"存下来"，并且每个对话有独立 URL（`/chat/[id]`），刷新、重开浏览器都能恢复。
>
> **本阶段做的事**：
> 1. 选 SQLite 作为数据库（用 Node 22 内置的 `node:sqlite`）
> 2. 设计两张表：会话表 + 消息表（外键关联）
> 3. 后端建 API：新建会话 / 取会话历史 / 保存消息
> 4. 前端改造：URL 带 id、mount 时加载历史、流式结束后保存
>
> 涉及文件：新增 6 个（`lib/` + 3 个 API 路由 + 动态页面）、修改 3 个（ChatBox、chat/page、.gitignore）

---

## 一、数据库选型：为什么是 node:sqlite

本来计划用 `better-sqlite3`，但安装时失败了——它是**原生模块**（C++ 编译的），安装时要么下载预编译二进制，要么本地从源码编译（需要 Python + VS Build Tools）。这台机器既下载超时又没 Python，编译走不通。

于是改用 **`node:sqlite`**——Node.js 22 内置的 SQLite 模块，零依赖零编译。

### 三种 SQLite 方案的本质区别

理解这个区别，比记住"用哪个"更重要：

| | node:sqlite | better-sqlite3 | sql.js |
|---|---|---|---|
| **怎么来的** | Node 22 内置 | npm 装，下载预编译/本地编译 | npm 装，纯 JS+WASM |
| **要不要编译** | 不要（Node 自带） | 要 | 不要 |
| **持久化** | 自动写文件 | 自动写文件 | **手动** export + writeFileSync |
| **同步/异步** | 同步 | 同步 | 同步（WASM 较慢） |
| **换电脑** | 保证 Node >= 22 | 要重走 npm install（可能又编译失败） | npm install 即可 |
| **成熟度** | 实验性（有 warning） | 工业级成熟 | 成熟，多用于浏览器 |

**底层原理**：
- `node:sqlite`：SQLite 的 C 源码**已被编译进 Node.js 22 的可执行文件**，`require("node:sqlite")` 直接用
- `better-sqlite3`：npm 下载时编译（下载预编译包或本地 node-gyp 编译），产物是 `.node` 二进制 + JS 胶水层
- `sql.js`：SQLite 被编译成 `.wasm`（WebAssembly），由 V8 的 WASM 运行时执行，不碰任何 C 编译器

### 为什么选 node:sqlite

1. **现在就能用**，不卡在编译问题上
2. API 和 better-sqlite3 几乎一样，**学到的知识 100% 可迁移**——以后换 better-sqlite3 只改两行 import
3. 学习项目该专注"SQL + 持久化设计"，而不是"怎么配 Python + 编译器"

**写法对比（几乎一样）**：
```ts
// better-sqlite3                // node:sqlite
import Database from "..."        import { DatabaseSync } from "node:sqlite"
const db = new Database(path)     const db = new DatabaseSync(path)
db.prepare("...").run()           db.prepare("...").run()      // 完全一样
db.prepare("...").get()           db.prepare("...").get()       // 完全一样
```

### ⚠️ 重要约束（换电脑时注意）

- **必须 Node >= 22**：低于 22 没有 `node:sqlite` 模块
- **实验性特性**：运行会有 warning（`ExperimentalWarning: SQLite is an experimental feature`），功能完全可用，这是 Node 官方未来方向
- **不能部署到 Vercel 等 serverless**：它们不支持本地文件系统 + 实验性模块。部署阶段（阶段 15）会换 Turso（SQLite 云托管）或 Postgres

---

## 二、数据库 schema 设计（两张表）

```sql
-- 会话表：一个对话一行
CREATE TABLE conversations (
  id TEXT PRIMARY KEY,           -- UUID，作为 URL 标识
  title TEXT,                    -- 会话标题（首条消息前20字，阶段13侧边栏用）
  system_prompt TEXT,            -- 该会话的 system 提示词
  created_at TEXT NOT NULL       -- ISO 时间戳字符串
);

-- 消息表：每条消息一行
CREATE TABLE messages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,  -- 自增主键
  conversation_id TEXT NOT NULL,         -- 外键，关联 conversations.id
  role TEXT NOT NULL,                    -- 'user' | 'assistant'
  content TEXT NOT NULL,                 -- 消息内容
  created_at TEXT NOT NULL,
  FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE CASCADE
);
CREATE INDEX idx_messages_conv ON messages(conversation_id);
```

### 关键设计点

#### a) 为什么 id 用 TEXT（UUID）而不是自增数字
会话 id 要暴露在 URL 里（`/chat/f47ac10b-...`）。用自增数字有两个问题：
- **泄露规模**：竞争对手访问 `/chat/103` 就知道你至少有 103 个会话
- **可遍历**：`/chat/1`、`/chat/2`... 能被爬虫遍历（虽然现在没鉴权，但这是好习惯）

UUID 是 128 位随机数，不可预测、不可遍历。用 `crypto.randomUUID()` 生成（Web 标准 API）。

#### b) 为什么时间戳用 TEXT 而不是 INTEGER 或 DATETIME
SQLite **没有专门的日期类型**。存时间有三种常见做法：

| 存法 | 例 | 优缺点 |
|---|---|---|
| TEXT（ISO 8601） | `"2026-07-14T10:30:00.000Z"` | 可读、可直接 `ORDER BY`（字符串排序正好对应时间排序）、跨语言友好 |
| INTEGER（Unix 秒） | `1720950600` | 省空间、比较快，但不可读 |
| INTEGER（Unix 毫秒） | `1720950600000` | 同上 |

用 TEXT 是 SQLite 官方推荐，且 `new Date().toISOString()` 直接生成，`ORDER BY created_at ASC` 字符串排序正好是时间正序。

#### c) 外键 + ON DELETE CASCADE 的意义
```sql
FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE CASCADE
```
- **外键**：`messages.conversation_id` 必须在 `conversations.id` 里存在，防止"孤儿消息"（消息指向一个不存在的会话）
- **ON DELETE CASCADE**：删会话时，它名下的所有消息**自动连带删除**。不用手写"先删消息再删会话"两条 SQL

**坑**：SQLite **默认关闭外键约束**，必须显式开启：
```ts
db.exec("PRAGMA foreign_keys = ON;");  // 在 db.ts 里
```
不开这句，外键声明就是摆设——能插入孤儿消息，CASCADE 也不生效。

#### d) 为什么建索引
```sql
CREATE INDEX idx_messages_conv ON messages(conversation_id);
```
查某个会话的所有消息是最频繁的操作（`WHERE conversation_id = ?`）。没索引时，每次查询要**扫描整张 messages 表**（O(n)）；有索引后，直接定位（O(log n)）。

类比：索引就像字典的部首目录。没目录找"海"字要翻遍整本字典；有目录先查"氵"部首，秒定位。

---

## 三、lib 层：连接管理 + 查询封装

### lib/db.ts —— 单例连接 + 建表

```ts
import { DatabaseSync } from "node:sqlite";

const globalForDb = globalThis as unknown as { db: DatabaseSync | undefined };

export const db = globalForDb.db ?? new DatabaseSync("chat.db");
db.exec("PRAGMA foreign_keys = ON;");
db.exec(`CREATE TABLE IF NOT EXISTS ... `);  // 建表

if (process.env.NODE_ENV !== "production") {
  globalForDb.db = db;  // 开发模式挂到 global
}
```

#### 为什么用单例（globalThis）
Next.js 开发模式有**热重载**（改代码自动重启模块）。如果不做单例，每次热重载都会重新执行 `new DatabaseSync()`，打开一个新连接，旧连接不释放——几次重载后连接就泄漏了。

挂到 `globalThis` 上，整个 Node 进程只保留一个连接实例，热重载时复用它。这是 Prisma、Drizzle 等所有数据库库在 Next.js 里的标准做法。

#### 为什么 db.ts 只在服务端用
`node:sqlite` 依赖 Node 的原生能力，**不能进浏览器 bundle**。db.ts 只在 route.ts（服务端）里被 import，Next.js 会自动判定它不打包进客户端代码。

### lib/queries.ts —— CRUD 封装

所有 SQL 集中在这一层，route.ts 只调用函数不写 SQL：

```ts
export function createConversation(params: { id, title?, systemPrompt? }): void {
  db.prepare(
    "INSERT INTO conversations (id, title, system_prompt, created_at) VALUES (?, ?, ?, ?)"
  ).run(params.id, params.title ?? null, params.systemPrompt ?? null, now);
}
```

#### prepared statement 防 SQL 注入
`db.prepare("...?...").run(param)` 里的 `?` 是**占位符**。参数会被安全转义，即使内容含 `' OR 1=1` 这种注入串，也只被当普通字符串处理：

```ts
// 安全：? 占位符，参数自动转义
db.prepare("SELECT * FROM messages WHERE content = ?").run(userInput);

// 危险：字符串拼接，会被注入
db.prepare(`SELECT * FROM messages WHERE content = '${userInput}'`);
// userInput = "x' OR '1'='1" → 整张表被查出来
```

**记忆点**：永远用 `?` 占位符，永远不要字符串拼接 SQL。

---

## 四、API 路由设计

三个新路由：

| 路由 | 方法 | 作用 |
|---|---|---|
| `/api/conversations` | POST | 新建空会话，返回 id |
| `/api/conversations/[id]` | GET | 取会话 + 所有消息（页面加载用） |
| `/api/conversations/[id]` | DELETE | 删会话（CASCADE 连带删消息） |
| `/api/messages` | POST | 保存一轮对话（user + assistant） |

### 动态路由参数的新写法（Next.js 15）

```ts
type Params = { params: Promise<{ id: string }> };

export async function GET(_request: Request, { params }: Params) {
  const { id } = await params;  // 注意：params 现在是 Promise，要 await
  // ...
}
```

**Next.js 15 的变化**：`params` 从同步对象变成了 **Promise**。这是为了支持异步渲染。必须 `await params` 才能拿到 `id`。

### messages 路由的事务处理

保存一轮对话要写两条消息（user + assistant），必须用事务保证原子性：

```ts
try {
  db_begin();
  addMessage({ conversationId, role: "user", content: userMessage });
  addMessage({ conversationId, role: "assistant", content: assistantMessage });
  db_commit();
} catch (err) {
  db_rollback();  // 任一条失败，全部回滚
}
```

**事务的意义**：把多条语句"打包"成原子操作——中间任意一步失败，之前已执行的也回滚，保证不留半截数据。比如 user 消息存成功但 assistant 那条失败，没事务的话会留下一条没回复的 user 消息；有事务则两条都不存。

> 注意：`better-sqlite3` 有 `db.transaction(fn)` 的包装函数（自动管 BEGIN/COMMIT/ROLLBACK），`node:sqlite` 目前没有，这里手动写了 `db_begin/commit/rollback` 三个辅助函数（在 db.ts 里）。

---

## 五、前端改造（ChatBox.tsx）

### 5.1 接收 conversationId prop

```tsx
export default function ChatBox({ conversationId }: { conversationId?: string }) {
```

- 有 id（从 `/chat/[id]` 进入）→ 加载历史
- 无 id（从 `/chat` 进入）→ 首次发送时创建会话

### 5.2 useEffect 加载历史（重点）

```tsx
useEffect(() => {
  if (!conversationId) {
    setMessages([]);  // 无 id = 新会话，清空
    return;
  }

  let cancelled = false;
  (async () => {
    const res = await fetch(`/api/conversations/${conversationId}`);
    const data = await res.json();
    if (cancelled) return;  // 组件卸载了就别 setState
    setMessages(data.messages.map(...));
  })();

  return () => { cancelled = true; };  // cleanup
}, [conversationId]);  // ← 依赖数组
```

#### 几个关键理解点

**a) 为什么 useEffect 不能直接是 async**
```ts
// 错误：useEffect 不能返回 Promise
useEffect(async () => { ... })

// 正确：里面套一个 async IIFE
useEffect(() => {
  (async () => { ... })();
}, [...]);
```
useEffect 要求返回 `void` 或 `cleanup 函数`。async 函数返回 Promise，React 会报错。用 IIFE（立即调用函数表达式）包一层。

**b) 依赖数组 `[conversationId]` 的意义**
React 根据 deps 决定"什么时候重新跑 effect"。
- `[]`：只在 mount 时跑一次
- `[conversationId]`：mount 时跑 + conversationId 变化时再跑

这里要 `[conversationId]`——用户点"新建会话"跳到 `/chat`，conversationId 从有变无，effect 要重跑清空状态。

**c) cancelled 标志防竞态**
如果用户快速切换会话（A→B），A 的请求还没回来就发了 B 的请求。如果 A 后回来，会把 A 的消息覆盖到 B 上。用 `cancelled` 标志：组件卸载或重新执行时，旧请求回来直接 `return`，不 setState。

### 5.3 首次发送创建会话 + 跳转 URL

```tsx
let activeConvId = conversationId;
if (!activeConvId) {
  const res = await fetch("/api/conversations", { method: "POST", ... });
  const data = await res.json();
  activeConvId = data.id;
  router.push(`/chat/${activeConvId}`);  // 更新 URL
}
```

`router.push` 是 Next.js 客户端导航，不触发整页刷新。URL 变了但组件不重新 mount（同一个 ChatBox 实例），所以本地 messages 不会丢。

### 5.4 流式结束后保存（核心设计）

这是这次最值得理解的设计：

```tsx
let completedNormally = false;  // 声明在 try 外（finally 要用）
let assistantContent = "";       // 同样在 try 外

try {
  // ... fetch + reader 循环
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    const chunk = decoder.decode(value, { stream: true });
    assistantContent += chunk;   // 局部变量同步累积
    setMessages(prev => ...);    // state 异步更新
  }
  completedNormally = true;      // 正常 break 出循环才标记
} catch {
  // 中止/出错，completedNormally 保持 false
} finally {
  if (completedNormally && activeConvId) {
    fetch("/api/messages", { ... });  // 只在正常完成时保存
  }
}
```

#### 为什么用局部变量 assistantContent 而不读 state
`setMessages` 是**异步**的——调用后 state 不会立刻更新，React 会批处理。finally 执行时读 `messages` state，可能读到的是**不完整**的内容（最后几个 chunk 还没刷进 state）。

用局部变量 `assistantContent += chunk` 是**同步**的，每个 chunk 立刻累加，finally 里能拿到 100% 完整的内容。

#### 为什么中止/出错不保存
- **中止**：用户主动停下，半截回复可能没意义，不存
- **出错**：网络断了/上游崩了，assistant 内容是空的或错误提示，不该当真实回复存

这是**简化处理**。理想方案是后端流式时边写边存（tee 流），即使前端断了也能保留已生成的部分——这个改进点记在 `待学与待办.md`，留到阶段 14 合入 LangChain 时做（是面试亮点）。

### 5.5 「清空」→「新建会话」

```tsx
// 旧：仅清本地 state，刷新后历史还在
onClick={() => setMessages([])}

// 新：跳到 /chat，触发全新会话流程
onClick={() => router.push("/chat")}
```

语义变了：以前"清空"是清当前显示；现在"新建会话"是开一个全新对话，旧的还在数据库里（阶段 13 侧边栏能看到所有历史会话）。

---

## 六、动态路由页面

### app/chat/[id]/page.tsx

```tsx
type Params = { params: Promise<{ id: string }> };

export default async function ChatByIdPage({ params }: Params) {
  const { id } = await params;
  return (
    <main>
      <ChatBox conversationId={id} />
    </main>
  );
}
```

文件名 `[id]` 的方括号是 Next.js 动态路由语法——访问 `/chat/abc` 时，`params.id = "abc"`。

### app/chat/page.tsx（无 id 入口）

不传 conversationId，对应"新建会话"场景。

---

## 七、完整数据流（一定要看懂）

### 新建会话并发送一条消息

```
用户在 /chat 输入"你好"点发送
  ↓
handleSend 发现无 conversationId
  ↓
POST /api/conversations → 返回 { id: "f47ac10b-..." }
  ↓
router.push("/chat/f47ac10b-...")  → URL 更新
  ↓
POST /api/chat { messages: [{role:"user",content:"你好"}] }
  ↓
流式接收 AI 回复，assistantContent 累积 "你好啊！我是..."
  ↓
流结束，completedNormally = true
  ↓
finally: POST /api/messages { conversationId, userMessage:"你好", assistantMessage:"你好啊！..." }
  ↓
后端事务写入两条消息 + 更新会话 title 为"你好"
```

### 刷新页面恢复历史

```
用户刷新 /chat/f47ac10b-...
  ↓
ChatBox mount，conversationId = "f47ac10b-..."
  ↓
useEffect 触发：GET /api/conversations/f47ac10b-...
  ↓
返回 { conversation: {...}, messages: [{role:"user",...}, {role:"assistant",...}] }
  ↓
setMessages(messages)  → 历史恢复显示
```

---

## 八、改动汇总

### 新增文件
| 文件 | 作用 |
|---|---|
| `lib/db.ts` | 单例连接 + 建表 + 事务辅助函数 |
| `lib/queries.ts` | CRUD 封装（prepared statements） |
| `app/api/conversations/route.ts` | POST 新建会话 |
| `app/api/conversations/[id]/route.ts` | GET 取历史 / DELETE 删会话 |
| `app/api/messages/route.ts` | POST 保存一轮对话（事务） |
| `app/chat/[id]/page.tsx` | 动态路由页面 |

### 修改文件
| 文件 | 改动 |
|---|---|
| `components/ChatBox.tsx` | conversationId prop、useEffect 加载历史、首次发送创建会话+跳转、流后保存、新建会话按钮 |
| `app/chat/page.tsx` | 注释更新（无 id = 新建会话入口） |
| `tsconfig.json` | 加 `@/*` 路径别名 |
| `.gitignore` | 加 `*.db` 等 SQLite 文件忽略 |

---

## 九、自测题（答案在文末）

### Q1
`node:sqlite` 和 `better-sqlite3` 在写法上的区别是什么？以后想从前者换到后者，要改哪些地方？

### Q2
为什么 `conversations.id` 用 TEXT（UUID）而不是 INTEGER（自增）？给两个理由。

### Q3
SQLite 默认不开启外键约束。这句 `db.exec("PRAGMA foreign_keys = ON;")` 不写会怎样？`ON DELETE CASCADE` 还会生效吗？

### Q4
为什么用 `let assistantContent = ""` 局部变量累积内容，而不是在 finally 里读 `messages` state？

### Q5
useEffect 为什么不能直接写成 `useEffect(async () => {...})`？正确的写法是什么？

### Q6
`cancelled` 标志解决的是什么问题？给一个具体的场景。

### Q7
为什么保存消息（POST /api/messages）放在 finally 里且用 `completedNormally` 判断，而不是在 while 循环结束后直接保存？

### Q8
`router.push("/chat/abc")` 之后，ChatBox 组件会重新 mount 吗？messages 会丢吗？为什么？

### Q9（进阶）
事务（BEGIN/COMMIT/ROLLBACK）解决的什么问题？如果不用事务，保存一轮对话时第二条 INSERT 失败，会发生什么？

### Q10（进阶）
现在的设计是"前端流式结束后 POST 保存"。如果用户在流到一半时关闭了浏览器，这条会话会怎样？怎么改进？（提示：后端 tee 流）

---

## 十、自测题答案

<details>
<summary>点击展开答案</summary>

**A1**：写法几乎一样，区别只在 import 和类名：
- import：`import Database from "better-sqlite3"` vs `import { DatabaseSync } from "node:sqlite"`
- 实例化：`new Database(path)` vs `new DatabaseSync(path)`
- API（prepare/exec/run/get/all）：完全一样。

换的时候只需改 `lib/db.ts` 里的 import 和 `new DatabaseSync(...)` 这两行，`queries.ts` 和所有 route.ts 一个字都不用改——这就是把 SQL 封装在 lib 层的好处。

**A2**：① 不泄露规模——自增数字能让别人通过 `/chat/103` 推测你至少有 103 个会话；UUID 不可预测。② 不可遍历——自增 id 能被爬虫 `/chat/1`、`/chat/2` 遍历，UUID 是 128 位随机数无法猜测。

**A3**：不写 `PRAGMA foreign_keys = ON`，外键声明就是摆设——能插入 conversation_id 不存在的孤儿消息，`ON DELETE CASCADE` 也不会生效（删会话时它的消息不会被自动删除，变成孤儿数据）。SQLite 为了向后兼容，默认关闭外键，必须显式开启。

**A4**：因为 `setMessages` 是异步的——调用后 state 不会立刻更新（React 批处理）。finally 执行时读 messages state 可能读到不完整的内容（最后几个 chunk 还没刷进去）。局部变量 `assistantContent += chunk` 是同步累加，finally 里能拿到 100% 完整内容。

**A5**：useEffect 要求返回 `void` 或 cleanup 函数；async 函数返回 Promise，React 会报错。正确写法是里面套一个 async IIFE：`useEffect(() => { (async () => { ... })(); }, [...])`。

**A6**：解决竞态条件。场景：用户快速切换会话 A→B，A 的请求还没回来就发了 B 的。如果 A 后回来，会把 A 的消息覆盖到 B 的页面上。`cancelled` 标志让旧请求回来时直接 return，不 setState。cleanup 函数在组件卸载或重新执行时设 `cancelled = true`。

**A7**：因为中止和出错都**不进 while 循环后的代码**，而是跳进 catch。但 finally 不管正常还是异常都会执行。用 `completedNormally` 标志：只有 while 正常 break（流读完）才设 true，catch 里保持 false。finally 据此判断要不要保存。如果放在 while 后直接保存，中止/出错时根本走不到那行。

**A8**：不会重新 mount，messages 不会丢。`router.push` 是 Next.js 的客户端导航——它只更新 URL 和路由参数，不卸载/重建组件。同一个 ChatBox 实例继续存在，本地 state 完整保留。conversationId 变了会触发 useEffect 重新跑，但因为此时 messages 已经有内容（我们刚发送的），useEffect 里若加载历史会覆盖——不过这里是新会话 id，数据库里还没有消息，加载回来是空，所以不影响（实际上首次发送时本地 state 已经有 user+assistant 了，比数据库的空数组更完整）。

**A9**：事务解决"原子性"——多条语句要么都成功要么都回滚。不用事务时，如果第一条 INSERT（user 消息）成功但第二条（assistant 消息）失败，会留下一条没有回复的 user 消息——半截数据。用事务的话，第二条失败会 ROLLBACK，第一条也撤销，保证不留半截状态。

**A10**：浏览器关闭时，前端不会发 POST /api/messages（页面直接没了），这轮对话**完全丢失**——user 消息和已生成的 assistant 回复都不存。改进方案：后端在 route.ts 里给流加一个"分支"（tee），边转发给前端边累积内容，流结束时后端直接写库。这样即使前端断了，后端也知道流结束（或上游断开），已生成的部分能保留。这是阶段 14 合入 LangChain 时的改进点，也是面试可以讲的亮点。

</details>

---

## 十一、阶段总结

这一阶段把项目从"内存型"推向"持久化型"。学到的核心可迁移知识：

1. **SQLite 基础**：建表、外键、索引、事务、prepared statement——这些 SQL 知识对所有关系型数据库通用
2. **数据库选型权衡**：原生模块（better-sqlite3）vs 内置模块（node:sqlite）vs WASM（sql.js），各有适用场景
3. **Next.js 服务端-only 依赖**：原生/Node 模块只能在 route.ts 用，不能进客户端 bundle
4. **单例模式**：用 globalThis 避免热重载多连接
5. **useEffect 数据加载**：async IIFE、依赖数组、cancelled 标志防竞态
6. **动态路由**：`[id]` 文件名 + `await params`（Next.js 15 变化）
7. **客户端导航**：router.push 不重新 mount，保留 state
8. **持久化时机设计**：前端流后保存是简化方案；后端 tee 边写边存是工业方案

**下一阶段预告**：阶段 13——多会话管理（侧边栏），列出所有历史会话，可切换/重命名/删除。从"单会话持久化"进入"多会话产品形态"。
