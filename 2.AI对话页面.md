# Next.js 最小聊天页面学习说明

这个文档基于你当前已经跑通的 Next.js 最小项目，继续扩展出的“最小聊天页面”。

目标不是做一个完整聊天产品，而是用最少代码带你理解：

- 新增页面
- 客户端组件
- `useState`
- 表单输入
- 请求 Next.js API Route
- 返回模拟聊天数据

---

## 这次新增了哪些文件

```txt
app/
  chat/
    page.tsx
  api/
    chat/
      route.ts

components/
  ChatBox.tsx
```

同时修改了：

- `app/page.tsx`

目的是在首页增加一个聊天页面入口。

---

## 推荐阅读顺序

建议按这个顺序看：

1. `app/chat/page.tsx`
2. `components/ChatBox.tsx`
3. `app/api/chat/route.ts`
4. `app/page.tsx`

这样读的原因：

- 先看聊天页面入口
- 再看聊天交互核心组件
- 再看接口怎么返回数据
- 最后看首页是怎么接入聊天页入口的

---

## 每个新增文件应该学什么

## `app/chat/page.tsx`

### 它是干什么的

- 它是聊天页面
- 对应路由 `/chat`
- 负责渲染标题、说明文字和聊天组件 `ChatBox`

### 它属于 React 还是 Next.js

- 组件函数本身：React
- `app/chat/page.tsx` 作为页面路由入口：Next.js
- 文件系统路由 `/chat`：Next.js

### 为什么要这样写

这里把“页面”和“聊天逻辑”分开：

- `page.tsx` 负责页面入口
- `ChatBox.tsx` 负责交互逻辑

这样结构更清晰，也更适合初学时理解“页面”和“组件”的分工。

### Vue 开发者最容易混淆的点

- `page.tsx` 不是随便取的名字，它是 Next.js 的页面约定
- 目录 `app/chat/` 自动对应 `/chat`
- 不是靠手动写路由表映射出来的

---

## `components/ChatBox.tsx`

### 它是干什么的

这是聊天框组件，负责：

- 管理输入框内容
- 管理消息列表
- 管理发送中状态
- 点击发送时调用 `/api/chat`
- 把返回结果显示到页面上

### 它属于 React 还是 Next.js

#### React 部分

- `useState`
- `onChange`
- `onClick`
- 列表渲染
- 条件渲染
- 组件状态更新

#### Next.js 部分

- `'use client'`
- 在 App Router 中声明这是客户端组件

### 为什么要这样写

#### 1. 为什么要 `'use client'`

因为这里用了：

- `useState`
- 输入框事件
- 按钮点击事件
- 浏览器发请求

在 Next.js App Router 里，这种带交互的组件必须声明为客户端组件。

#### 2. 为什么拆成三个状态

```tsx
const [input, setInput] = useState('')
const [messages, setMessages] = useState<Message[]>([])
const [loading, setLoading] = useState(false)
```

这样最适合教学：

- `input` 表示输入框当前值
- `messages` 表示消息列表
- `loading` 表示是否正在发送

每个状态只负责一件事。

#### 3. 为什么先显示用户消息，再请求接口

因为聊天页面通常需要即时反馈：

- 用户点发送
- 自己的消息先立刻显示
- 然后再等待机器人回复

这样更符合聊天场景。

#### 4. 为什么用 `fetch('/api/chat')`

因为你现在学习的是：

- 前端页面如何调用 Next.js 自己的接口
- 页面与 API Route 的最小闭环

### Vue 开发者最容易混淆的点

#### 1. `'use client'` 是很重要的概念

Vue 组件默认就是可交互的。

但 Next.js App Router 默认不是所有组件都直接运行在客户端。只要你用了 `useState`、输入框、点击事件，通常就要写 `'use client'`。

#### 2. `useState` 和 Vue 的 `ref` 写法不同

Vue 常见：

```ts
const input = ref('')
```

React 常见：

```tsx
const [input, setInput] = useState('')
```

Vue 是改 `.value`，React 是调用 setter。

#### 3. 输入框是受控组件

```tsx
<input
  value={input}
  onChange={(event) => setInput(event.target.value)}
/>
```

你可以先类比成 Vue 的：

```vue
<input v-model="input" />
```

作用相似，但 React 需要手动写 `value` 和 `onChange`。

---

## `app/api/chat/route.ts`

### 它是干什么的

- 它定义了 `/api/chat` 接口
- 接收前端发来的消息
- 返回模拟机器人回复

当前不是接真实 AI，只是先把请求链路跑通。

### 它属于 React 还是 Next.js

这部分属于 Next.js，不属于 React。

因为 React 负责 UI，`route.ts` 是 Next.js 的服务端接口能力。

### 为什么要这样写

#### 1. 为什么使用 `POST`

聊天发送消息，本质上是在提交数据，所以这里使用 `POST` 更合适。

#### 2. 为什么读取 `request.json()`

因为前端发送的是 JSON：

```tsx
body: JSON.stringify({
  message: text
})
```

所以接口里要用：

```tsx
const body = await request.json()
```

来解析请求体。

#### 3. 为什么先返回模拟数据

因为你当前阶段重点是：

- 页面状态
- 请求流程
- API Route
- 返回 JSON
- 更新页面

现在还不需要引入真实 AI、数据库或复杂工程结构。

### Vue 开发者最容易混淆的点

- 这是同一个 Next.js 项目里的接口，不是单独的后端服务
- 它不是 Express 的 `req/res` 风格
- 它是 App Router 下 `route.ts` 的约定式接口写法

---

## `app/page.tsx` 这次改了什么

首页增加了一个聊天页面入口：

```tsx
<Link href="/chat">去聊天页面</Link>
```

这样你可以从首页直接跳转到聊天页。

### 这里属于 React 还是 Next.js

- 页面组件本身：React
- `next/link` 和路由跳转：Next.js

### Vue 开发者最容易混淆的点

它可以类比成 Vue Router 的 `router-link`，但它属于 Next.js 的导航组件。

---

## 这个最小聊天页面帮助你掌握了什么

在原来的最小项目基础上，这次你会多掌握这些核心能力：

### 1. 新增页面路由

你会更熟悉：

- `app/chat/page.tsx` 对应 `/chat`

### 2. 客户端组件交互

你会理解：

- 为什么交互组件要写 `'use client'`
- 为什么 `useState` 不能随便放到默认页面组件里理解

### 3. 输入框状态管理

你会掌握：

- React 中输入框如何和状态绑定
- 什么叫受控组件

### 4. 消息列表渲染

你会看到：

- 数组状态如何渲染成列表
- 新消息如何追加进列表

### 5. 页面请求 API Route

你会建立完整链路：

- 页面输入
- 点击发送
- `fetch('/api/chat')`
- `route.ts` 返回 JSON
- 页面显示回复

### 6. Next.js 的“前后端同项目”模式

你会开始习惯：

- 页面在 `app/`
- API 也在 `app/api/`
- 一个项目里同时写 UI 和接口

---

## React 和 Next.js 的边界，再总结一次

学习到这里，你要继续保持一个很重要的判断：

## React 负责什么

- 组件
- JSX
- `useState`
- 输入框状态
- 点击事件
- 列表渲染
- 条件渲染

## Next.js 负责什么

- `app/chat/page.tsx` 这种页面约定
- `app/api/chat/route.ts` 这种接口约定
- `next/link`
- `'use client'` 对客户端组件的声明
- 文件系统路由

很多初学时的混乱，本质上都来自把这两层混在一起理解。

---

## 建议你怎么自己动手验证

启动项目：

```bash
npm install
npm run dev
```

然后访问：

- 首页：`http://localhost:3000`
- 聊天页：`http://localhost:3000/chat`
- 聊天接口：`http://localhost:3000/api/chat`

注意：

- `/api/chat` 目前是 `POST` 接口，不是给浏览器直接点开用的页面
- 它主要是给聊天页里的 `fetch` 调用的

你可以在聊天页里输入：

```txt
你好
```

点击发送后，页面会出现两条消息：

- 你：你好
- 机器人：你刚刚说的是：你好

---

## 学完这一版后，你应该能回答什么问题

如果你已经理解了这个项目，你应该能回答这些问题：

1. `app/chat/page.tsx` 为什么会变成 `/chat`
2. 为什么 `ChatBox.tsx` 要写 `'use client'`
3. `useState` 在这里分别管理了什么
4. 为什么输入框需要 `value` 和 `onChange`
5. 点击发送后，前端是怎么调用 `/api/chat` 的
6. `route.ts` 为什么能返回 JSON
7. 哪些部分属于 React，哪些部分属于 Next.js

如果这些问题你都能说清楚，说明你已经掌握了一个最小聊天页的完整骨架。

---

## 下一步最适合继续扩展什么

在这个最小聊天页面基础上，下一步最适合继续学习的是：

1. 回车发送
2. 清空消息按钮
3. 单独给 `/chat` 设置 `metadata`
4. 区分用户消息和机器人消息样式
5. 增加简单错误处理
6. 再下一步才接真实 AI 接口

建议顺序仍然是：

- 先把页面交互和 API 调用理解透
- 再接真实模型
- 再考虑更复杂的工程能力
