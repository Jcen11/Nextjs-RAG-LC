# 阶段 13：多会话管理（侧边栏）

> 这是 Nextjs 项目的**最后一个阶段**。做完这个，项目就是完整的"单机 AI 聊天应用"——能聊天、能流式、能持久化、能多会话切换，形态接近 ChatGPT。
>
> **本阶段做的事**：左侧加一个会话列表侧边栏，能列出所有会话、点击切换、重命名、删除、新建。新建/删除/重命名后列表自动刷新。

---

## 一、关键发现：很多东西阶段 12 已经预埋了

探索代码时发现，阶段 12 早就为侧边栏埋好了几个零件：

| 能力 | 位置 | 状态 |
|---|---|---|
| 更新标题函数 `updateConversationTitle` | `lib/queries.ts` | ✅ 阶段 12 已有 |
| 删除会话函数 `deleteConversation`（带 CASCADE） | `lib/queries.ts` | ✅ 阶段 12 已有 |
| 删除会话接口 `DELETE /api/conversations/[id]` | `conversations/[id]/route.ts` | ✅ 阶段 12 已有 |
| 标题自动生成（首条消息前 20 字） | `messages/route.ts` 第 63-67 行 | ✅ 阶段 12 已预埋 |

**真正缺的只有**：
- ❌ 列出所有会话的函数 `listConversations`
- ❌ 列表接口 `GET /api/conversations`
- ❌ 重命名接口 `PATCH /api/conversations/[id]`
- ❌ Sidebar 组件
- ❌ chat/layout.tsx 布局

**教训**：好的项目演进是"每一步都为下一步留接口"。阶段 12 写标题生成时注释了"供阶段 13 侧边栏显示"，到阶段 13 真的就用上了。这种"前置埋点"的思维方式值得记住。

---

## 二、核心概念：嵌套布局（layout.tsx）

### 布局是什么

Next.js App Router 里，`layout.tsx` 是**包裹页面的外壳**——它渲染的 JSX 会包住同一层级的所有 `page.tsx`。

```
app/
├── layout.tsx          ← root layout（全局），包住所有页面
├── page.tsx            ← 首页 /，被 root layout 包
├── about/page.tsx      ← /about，被 root layout 包
└── chat/
    ├── layout.tsx      ← chat layout（局部），只包 /chat 下的页面
    ├── page.tsx        ← /chat，被 root + chat 两个 layout 包
    └── [id]/page.tsx   ← /chat/xxx，被 root + chat 两个 layout 包
```

### 嵌套顺序

访问 `/chat/f47ac10b` 时，渲染层级是：

```
<RootLayout>           ← app/layout.tsx（header + footer）
  <ChatLayout>         ← app/chat/layout.tsx（sidebar + main）
    <ChatByIdPage>     ← app/chat/[id]/page.tsx（ChatBox）
    </>
  </>
</>
```

外层包内层，像俄罗斯套娃。

### 为什么侧边栏放 chat/layout.tsx 而不是 root layout

| 位置 | 影响范围 | 适合吗 |
|---|---|---|
| `app/layout.tsx`（root） | **所有页面**（首页、about、chat） | ❌ 侧边栏在首页出现没意义 |
| `app/chat/layout.tsx` | **只有 /chat 和 /chat/[id]** | ✅ 侧边栏只在聊天页出现 |

**局部 layout 只影响它所在文件夹及其子文件夹**。这是 App Router 的关键能力——不同路由区段可以有不同布局。

### layout 的另一个重要特性：不重新 mount

切换路由时（比如 `/chat/aaa` → `/chat/bbb`），**layout 不会重新 mount，只有 page 重新渲染**。这意味着：
- Sidebar 组件实例保持不变
- Sidebar 内部的 state（比如正在重命名的输入框内容）不会丢
- 侧边栏不会闪烁

这是 layout 和"每个页面各自渲染侧边栏"的关键区别。

---

## 三、刷新机制：事件驱动（重点）

侧边栏要"保持新鲜"——新建会话后要出现新条目，删除后要消失。怎么实现？

### 三种方案对比

| 方案 | 做法 | 缺点 |
|---|---|---|
| 轮询 | setInterval 每隔几秒拉一次 | 浪费请求、有延迟、不优雅 |
| 全局状态 | 把会话列表提到顶层 Context，各处手动更新 | 改动大、要重构 |
| **事件驱动**（采用） | 用 usePathname 监听路由变化 + 操作后手动刷新 | 简单、按需、无浪费 |

### 事件驱动的两个触发点

**触发点 1：usePathname 监听路由变化**

```tsx
const pathname = usePathname();

useEffect(() => {
  loadConversations();  // 重新拉列表
}, [pathname]);
```

用户切换会话（`/chat/aaa` → `/chat/bbb`）或新建会话（跳到 `/chat`）时，pathname 变了，effect 重跑，重新拉列表。这覆盖了"切换会话""新建会话"两个场景。

**触发点 2：操作后手动调 loadConversations**

重命名和删除不是路由变化（URL 没变），usePathname 监听不到。所以在 PATCH/DELETE 成功后，手动调 `loadConversations()`：

```tsx
async function commitEdit(id) {
  await fetch(`/api/conversations/${id}`, { method: "PATCH", ... });
  loadConversations();  // 手动刷新
}
```

### router.refresh()：让服务端组件也更新

ChatBox 保存消息成功后调 `router.refresh()`：

```tsx
// ChatBox.tsx 的 finally
await fetch("/api/messages", { ... });
router.refresh();  // 触发 Next.js 重新渲染服务端组件
```

**router.refresh() 的作用**：让当前路由的所有服务端组件重新执行（重新查数据库），但不重置客户端 state。所以：
- 侧边栏的 usePathname effect 被触发（因为 refresh 会重新走一遍）→ 重新拉列表
- ChatBox 的 messages 不丢（客户端 state 不受影响）

### usePathname / router.push / router.refresh 三者关系

| API | 作用 | 改 URL 吗 | 重置 state 吗 |
|---|---|---|---|
| `usePathname()` | 读当前 URL 路径 | 只读 | — |
| `router.push(url)` | 客户端导航到新 URL | ✅ 改 | ❌ 不重置 |
| `router.refresh()` | 重新渲染服务端组件 | ❌ 不改 | ❌ 不重置 |

关键：push 和 refresh **都不重置客户端 state**。这就是为什么 ChatBox 在切换会话时不会丢内容（push），刷新时也不会丢（refresh）。

---

## 四、实现细节拆解

### 4.1 数据层：listConversations

```ts
export function listConversations(): Conversation[] {
  const rows = db
    .prepare("SELECT * FROM conversations ORDER BY created_at DESC")
    .all() as ConversationRow[];
  return rows.map((row) => ({ ... }));  // 下划线 → 驼峰映射
}
```

`ORDER BY created_at DESC` —— 最新的会话排最上面（侧边栏从上到下是新→旧）。DESC = 降序。

**注意不取消息内容**：列表接口只返回会话元信息（id/title/createdAt），不 JOIN messages。因为侧边栏只需要标题，不需要每条消息——取消息是打开会话时才做的事。这是性能考量。

### 4.2 重命名接口：PATCH

```ts
// conversations/[id]/route.ts
export async function PATCH(request, { params }) {
  const { id } = await params;
  const body = await request.json();

  if (typeof body?.title !== "string" || !body.title.trim()) {
    return Response.json({ error: "title 不能为空" }, { status: 400 });
  }

  if (!getConversation(id)) {
    return Response.json({ error: "会话不存在" }, { status: 404 });
  }

  updateConversationTitle(id, body.title.trim());
  return Response.json({ ok: true });
}
```

**为什么用 PATCH 不是 PUT**：
- `PUT` 语义是"替换整个资源"（要传所有字段）
- `PATCH` 语义是"部分更新"（只改 title 一个字段）
- 重命名只改标题，用 PATCH 更符合 HTTP 语义

### 4.3 Sidebar 组件结构

```
<aside className="sidebar">
  <button>+ 新建会话</button>        ← router.push("/chat")
  <ul>
    {conversations.map(conv => (
      <li className={isActive ? "active" : ""}>
        {正在重命名 ? (
          <input />                  ← 回车提交，Esc 取消，失焦提交
        ) : (
          <>
            <button onClick={切换}>{标题}</button>
            <span className="actions">
              <button onClick={重命名}>✏</button>
              <button onClick={删除}>🗑</button>
            </span>
          </>
        )}
      </li>
    ))}
  </ul>
</aside>
```

#### 当前会话怎么判断高亮

```tsx
const pathname = usePathname();
// pathname 形如 "/chat/f47ac10b-..." 或 "/chat"
const currentId = pathname.startsWith("/chat/") ? pathname.slice("/chat/".length) : null;

// 渲染时
const isActive = conv.id === currentId;
<li className={`sidebar-item${isActive ? " active" : ""}`}>
```

从 URL 提取 id，和列表项的 id 比较——URL 是"当前在哪"的唯一真相。

#### 重命名的交互细节

```tsx
const [editingId, setEditingId] = useState(null);  // 哪个会话在编辑
const [editTitle, setEditTitle] = useState("");     // input 临时值

// 开始编辑：把原标题填进 input
function startEdit(conv) {
  setEditingId(conv.id);
  setEditTitle(conv.title);
}

// 提交：PATCH + 刷新列表
async function commitEdit(id) {
  const title = editTitle.trim();
  setEditingId(null);  // 先退出编辑态（即使请求失败也退出，避免卡住）
  if (!title) return;
  await fetch(`/api/conversations/${id}`, { method: "PATCH", ... });
  loadConversations();
}
```

input 的三个事件：
- `Enter` → `commitEdit`（回车提交）
- `Escape` → `setEditingId(null)`（取消）
- `onBlur`（失焦）→ `commitEdit`（点别处也算提交）

`autoFocus` 让 input 出现时光标自动进去，用户直接打字。

#### 删除的交互

```tsx
async function handleDelete(id) {
  if (!window.confirm("确定删除？")) return;  // 二次确认
  await fetch(`/api/conversations/${id}`, { method: "DELETE" });
  if (id === currentId) {
    router.push("/chat");  // 删的是当前会话，跳回新建入口
  }
  loadConversations();
}
```

**删当前会话要跳转**：不跳的话，URL 还指向已删除的会话，ChatBox 会显示空/报错。跳回 `/chat`（无 id）进入新建流程最合理。

---

## 五、CSS：flex 布局让侧边栏和聊天区并排

```css
.chat-shell {
  display: flex;            /* 横向并排 */
  min-height: calc(100vh - 80px);
}

.sidebar {
  width: 240px;
  flex-shrink: 0;           /* 不被压缩，保持固定宽度 */
  overflow-y: auto;         /* 会话多了滚动 */
}

.chat-main {
  flex: 1;                  /* 占满剩余宽度 */
  min-width: 0;             /* ⚠️ 关键：让内容能收缩 */
  overflow-y: auto;
}
```

### ⚠️ min-width: 0 的坑

`flex: 1` 的子项默认 `min-width: auto`，意味着它**不会比内容更窄**。如果聊天区有长内容（比如长 Markdown 代码块），会撑破布局，把侧边栏挤变形。

加 `min-width: 0` 覆盖默认值，让 flex 子项能正确收缩。这是 flex 布局的经典坑，记住就行。

### hover 才显示操作按钮

```css
.sidebar-actions { display: none; }
.sidebar-item:hover .sidebar-actions { display: inline-flex; }
```

平时只显示标题，鼠标悬停才露出 ✏🗑——保持列表干净，参考 ChatGPT 的交互。

---

## 六、ChatBox 的改动：保存后刷新侧边栏

阶段 12 的保存逻辑是 fire-and-forget（发了请求不管结果）。阶段 13 改成 await + router.refresh：

```tsx
// 阶段 12（旧）：fire-and-forget
fetch("/api/messages", { ... }).catch(() => {});

// 阶段 13（新）：await + refresh
await fetch("/api/messages", { ... });
router.refresh();  // 让侧边栏重新拉列表，显示新会话的标题
```

**为什么要刷新**：会话标题是"保存首条消息时"才生成的（messages/route.ts 第 63-67 行）。如果不刷新，新建会话在侧边栏里显示的是空标题，要点一下切换才更新——体验不好。保存完立刻 refresh，侧边栏马上显示标题。

---

## 七、改动汇总

### 新增文件
| 文件 | 作用 |
|---|---|
| `components/Sidebar.tsx` | 会话列表 + 切换/重命名/删除/新建 |
| `app/chat/layout.tsx` | /chat 路由的布局（侧边栏 + 聊天区 flex） |

### 修改文件
| 文件 | 改动 |
|---|---|
| `lib/queries.ts` | 加 `listConversations()` |
| `app/api/conversations/route.ts` | 加 `GET`（列表接口） |
| `app/api/conversations/[id]/route.ts` | 加 `PATCH`（重命名接口） |
| `app/globals.css` | 加侧边栏样式（flex 布局 + 列表项 + 高亮 + 操作按钮） |
| `components/ChatBox.tsx` | 保存后 await + router.refresh（旧代码注释保留） |

---

## 八、自测题（答案在文末）

### Q1
为什么侧边栏放在 `app/chat/layout.tsx` 而不是 `app/layout.tsx`（root layout）？如果放 root layout 会怎样？

### Q2
切换会话时（`/chat/aaa` → `/chat/bbb`），Sidebar 组件会重新 mount 吗？为什么这点重要？

### Q3
`usePathname` 监听路由变化能覆盖哪些场景？哪些场景覆盖不了，需要手动刷新？

### Q4
`router.push()` 和 `router.refresh()` 有什么区别？为什么保存消息后要用 refresh 而不是 push？

### Q5
重命名接口为什么用 PATCH 而不是 PUT？两者的语义区别是什么？

### Q6
`listConversations` 为什么不把每个会话的消息也取出来（不 JOIN messages）？

### Q7
删除当前会话后为什么要 `router.push("/chat")`？不跳转会怎样？

### Q8（进阶）
CSS 里 `.chat-main` 为什么要加 `min-width: 0`？不加会出什么问题？

### Q9（进阶）
阶段 12 的标题自动生成逻辑（messages/route.ts），为什么到阶段 13 才"发挥作用"？这体现了什么项目管理思路？

---

## 九、自测题答案

<details>
<summary>点击展开答案</summary>

**A1**：因为侧边栏只在聊天页有意义，不该出现在首页和 about 页。root layout 包住所有页面，放那里侧边栏会全局显示。`app/chat/layout.tsx` 是局部 layout，只影响 `/chat` 和 `/chat/[id]`，正好是侧边栏该出现的范围。

**A2**：不会重新 mount。layout 在路由切换时保持实例不变，只有 page 重新渲染。这很重要——Sidebar 的 state（如正在重命名的输入框内容）不会丢，侧边栏不会闪烁。如果每个页面各自渲染侧边栏，切换时会重新 mount，state 丢失。

**A3**：usePathname 能覆盖"切换会话"和"新建会话"（这两个都会改 URL 路径）。覆盖不了的是"重命名"和"删除"——这两个操作不改 URL（重命名只改标题，删除如果不是当前会话 URL 也不变）。所以重命名/删除成功后要手动调 `loadConversations()`。

**A4**：push 改 URL 做客户端导航；refresh 不改 URL，只让服务端组件重新执行（重新查数据库）。保存消息后 URL 不需要变（还在同一个会话），但侧边栏列表需要更新（新会话标题生成了），所以用 refresh 触发服务端组件重渲染，而不是 push。

**A5**：PUT 语义是"替换整个资源"（要传所有字段）；PATCH 语义是"部分更新"（只改动的字段）。重命名只改 title 一个字段，用 PATCH 更符合 HTTP 语义规范。

**A6**：性能。侧边栏只需要标题（id/title/createdAt），不需要每条消息。如果一个用户有 100 个会话、每个 50 条消息，JOIN 出来是 5000 行数据，但侧边栏只需要 100 行会话元信息。消息是打开会话时才单独取（GET /api/conversations/[id]）。按需取数据是性能优化的基本原则。

**A7**：不跳转的话，URL 还指向已删除的会话 id，ChatBox 会尝试加载一个不存在的历史，显示空或报错。跳回 `/chat`（无 id）进入新建流程，用户看到干净的聊天区，符合"删了就没了"的预期。

**A8**：flex 子项默认 `min-width: auto`，意味着不会比内容更窄。如果聊天区有长内容（长代码块、长 URL），会撑破 flex 布局，把侧边栏挤变形或导致横向滚动。加 `min-width: 0` 覆盖默认，让 flex 子项能正确收缩。这是 flex 布局的经典坑。

**A9**：阶段 12 写标题生成时，侧边栏还不存在，标题生成了"没人用"。但当时注释了"供阶段 13 侧边栏显示"，到阶段 13 真的用上了。这体现"每一步为下一步留接口"的演进思路——做当前阶段时，预判后续阶段的需要，提前埋好数据/逻辑。这样后续阶段能复用，不用返工。这也是"好的项目演进"和"堆砌功能"的区别。

</details>

---

## 十、阶段总结 & Nextjs 项目收官

这一阶段把项目从"单会话"变成"多会话"，形态接近真正的 ChatGPT。学到的核心可迁移知识：

1. **嵌套布局**：layout.tsx 的层级嵌套，局部 layout 只影响特定路由区段
2. **layout 不重新 mount**：路由切换时保持实例，state 不丢、不闪烁
3. **事件驱动刷新**：usePathname 监听 + 操作后手动刷新，比轮询优雅
4. **router.refresh vs push**：refresh 刷数据不改 URL，push 改 URL 做导航
5. **HTTP 方法语义**：PATCH（部分更新）vs PUT（整体替换）
6. **flex 布局**：固定侧栏 + 自适应主区，min-width: 0 防撑破
7. **按需取数据**：列表接口只取元信息，详情才取完整数据

### Nextjs 项目（阶段 1-13）完整能力清单

经过 13 个阶段，这个项目已经具备：
- ✅ Next.js App Router 全栈架构（服务端组件 + 客户端组件 + API 路由）
- ✅ 流式 AI 对话（SSE 解析 + ReadableStream + 中断控制）
- ✅ 多轮上下文 + 系统提示词
- ✅ Markdown 渲染 + 代码高亮 + 复制按钮
- ✅ 完整的错误处理（分层 try/catch + 超时 + 中断 + 友好提示）
- ✅ 会话持久化（SQLite + 动态路由 + 事务）
- ✅ 多会话管理（侧边栏 + 切换/重命名/删除）
- ✅ 完整的学习文档体系（13 份阶段文档 + SQLite 速通 + 待学与待办）

### 已知约束（融合阶段要处理）

- **不能部署 Vercel**：node:sqlite 是本地文件 + 实验性模块，serverless 不支持。阶段 15 部署时要换 Turso（SQLite 云托管）或 Postgres
- **断连不保存**：前端流式结束才保存，中途断开丢失。阶段 14 合入 LangChain 时改后端 tee 边写边存
- **无鉴权**：任何人都能看所有会话（本地项目无所谓，上线要加登录）

**下一阶段（阶段 14）起，进入融合阶段**：把这个 Nextjs 项目和 langChain-demo 的 RAG 能力合并，做成"AI 知识库助手"。那是新项目、新文件夹、新对话的事了。
