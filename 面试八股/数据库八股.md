# 数据库面试八股

> 对应项目：Nextjs-RAG-LC（阶段 12/15）
> 配套文档：`SQLite速通.md`、`12.会话持久化.md`、`15.RAG知识库.md`
> 对应代码：`lib/db.ts`、`lib/queries.ts`
>
> 这份覆盖 SQLite 基础 + SQL 工程 + 持久化设计。投后端/全栈岗重点看。

---

## 一、SQLite 基础

### 【概念⭐】SQLite 是什么？和 MySQL/PostgreSQL 的区别？
SQLite 是**单文件数据库**——所有数据在一个文件里（项目的 `chat.db`）。MySQL/PostgreSQL 是服务型数据库，要单独部署服务进程。

| | SQLite | MySQL/PostgreSQL |
|---|---|---|
| 存储 | 单文件 | 服务进程 + 独立存储 |
| 安装 | 装个库就行 | 装服务、配端口、建用户 |
| 并发 | 单写多读 | 多写并发强 |
| 适合 | 本地应用、学习、小型 | 中大型、多用户 |

**项目举例**：学习项目 + 单机，选 SQLite，一个 chat.db 文件搞定。

---

### 【概念⭐⭐】SQLite 有哪些数据类型？为什么没有日期类型？
5 种存储类型：TEXT、INTEGER、REAL、BLOB、NULL。**没有专门的日期类型**——SQLite 是弱类型。存日期用 TEXT（ISO 8601 字符串）或 INTEGER（Unix 时间戳）。

**项目举例**：`created_at` 用 TEXT 存 `new Date().toISOString()` 的结果。

---

### 【追问⭐⭐】为什么用 TEXT 存日期不用 INTEGER？
ISO 8601 字符串（"2026-07-17T10:30:00.000Z"）有个巧妙特性——**字符串排序正好对应时间排序**（高位在前）。所以 `ORDER BY created_at ASC` 字符串升序就是时间正序。且可读、跨语言友好。INTEGER 省空间但不可读。

---

### 【概念⭐】PRIMARY KEY 和 AUTOINCREMENT 是什么？
- `PRIMARY KEY`：主键，唯一标识一行，不能重复不能为空
- `AUTOINCREMENT`：自增。配合 INTEGER PRIMARY KEY 用，插入新行自动分配 1、2、3...

**项目举例**：`messages.id INTEGER PRIMARY KEY AUTOINCREMENT`（数据库自动分配 id），`conversations.id TEXT PRIMARY KEY`（代码生成的 UUID）。

---

### 【决策⭐⭐】为什么 conversations.id 用 UUID（TEXT），messages.id 用自增数字？
- conversations.id 暴露在 URL 里（`/chat/xxx`），用 UUID 防泄露规模（看不到 id=103 推测有 103 个会话）、防遍历（不可猜测）
- messages.id 只在数据库内部用（前端渲染的 key），不需要对外，自增整数省空间、快

---

### 【概念⭐】NOT NULL 是什么？NULL 和空字符串的区别？
- `NOT NULL`：这列必须有值，不能为空
- `NULL`：表示"未知/没有值"
- `""`（空字符串）：表示"值为空"

它们不一样。查询 `WHERE title = NULL` 永远查不到，要写 `WHERE title IS NULL`。

**项目举例**：`title TEXT`（允许 NULL，新建会话还没标题），`content TEXT NOT NULL`（必须有值）。

---

## 二、外键 / 约束

### 【概念⭐⭐】什么是外键？解决什么问题？
外键是"一张表的某列引用另一张表的主键"，保证引用完整性——不会出现指向不存在记录的"孤儿数据"。

**项目举例**：`messages.conversation_id` 引用 `conversations.id`。有了外键，不能插入 conversation_id 指向不存在会话的消息。

---

### 【决策⭐⭐⭐】（踩坑题）SQLite 默认关闭外键，你踩过这个坑吗？
**是的**。SQLite 为了向后兼容，**默认关闭外键约束**——写了 `FOREIGN KEY` 和 `ON DELETE CASCADE`，但不显式开启就是摆设：能插孤儿数据、CASCADE 不生效。

必须代码里开：
```ts
db.exec("PRAGMA foreign_keys = ON;");  // lib/db.ts
```

这个坑面试可以讲，体现"理解原理不是只会用"。

---

### 【概念⭐⭐】ON DELETE CASCADE 是什么？
级联删除——删被引用的行时，引用它的行**自动连带删除**。

**项目举例**：删一个会话，它名下的所有消息自动删除，不留垃圾。不用手写"先删消息再删会话"。

---

### 【追问⭐⭐】如果不开 PRAGMA foreign_keys，CASCADE 会发生什么？
不生效。删会话后，messages 表里该会话的消息变成孤儿数据（conversation_id 指向不存在的会话）。

---

## 三、索引

### 【概念⭐⭐】什么是索引？为什么能加速查询？
索引是**表旁边的"目录"**——预先按某列排好序的独立数据结构。查询时先翻目录（二分查找 O(log n)）定位行号，再回表取数据，不用全表扫描（O(n)）。

**重要**：索引**不改变表数据顺序**，是旁边多出来的目录。详见 `SQLite速通.md` 3.4 节。

---

### 【决策⭐⭐】为什么给 messages.conversation_id 建索引，不给别的列建？
最频繁的查询是"按 conversation_id 取消息"（`WHERE conversation_id = ?`）。建索引让这个查询从全表扫描变二分查找。不常查的列（content、role）不建——索引占空间且拖慢写入。

---

### 【追问⭐⭐⭐】索引的代价是什么？什么时候不该建？
1. **占空间**：索引是额外数据结构
2. **写入变慢**：每次 INSERT/UPDATE/DELETE 都要更新索引

不该建：不常查的列、数据量很小的表、频繁写入少读取的表。

---

### 【追问⭐⭐】你怎么知道查询有没有走索引？
`EXPLAIN QUERY PLAN SELECT ...` 看 SQLite 的查询计划。如果显示 "SEARCH USING INDEX" 就是走了索引，"SCAN" 就是全表扫描。

---

## 四、SQL 注入 / 安全

### 【概念⭐⭐】什么是 SQL 注入？怎么防？
恶意用户在输入里塞 SQL 片段，如果用字符串拼接构造 SQL，会被解析成 SQL 语法：
```ts
// 危险：拼接
db.exec(`SELECT * FROM users WHERE name = '${userInput}'`);
// userInput = "x' OR '1'='1" → 整张表被查出来
```

防御：**永远用 prepared statement + 占位符 `?`**，参数会被当纯数据转义：
```ts
db.prepare("SELECT * FROM users WHERE name = ?").run(userInput);
```

---

### 【决策⭐⭐】你的项目怎么防 SQL 注入？
所有 SQL 都用 prepared statement（`lib/queries.ts`）：
```ts
db.prepare("INSERT INTO conversations (id, title, ...) VALUES (?, ?, ...)")
  .run(params.id, params.title, ...);
```
永远不用字符串拼接。详见 `SQLite速通.md` 第四节。

---

## 五、事务

### 【概念⭐⭐】什么是事务？ACID 是什么？
事务 = 一组操作打包成原子单位（要么全成功要么全回滚）。

ACID：
- **A**tomicity 原子性：全成或全败
- **C**onsistency 一致性：执行前后数据合法
- **I**solation 隔离性：并发事务互不干扰
- **D**urability 持久性：提交后永久保存

---

### 【决策⭐⭐⭐】你的项目哪里用了事务？为什么？
保存一轮对话（user 消息 + assistant 消息）要写两条 INSERT，用事务保证原子性：
```ts
db_begin();
addMessage({ role: "user", content: userMsg });
addMessage({ role: "assistant", content: assistantMsg });
db_commit();
```

如果不用事务，第一条成功第二条失败，会留下"没回复的 user 消息"——半截数据。事务保证不留半截。

详见 `12.会话持久化.md` messages 路由。

---

### 【追问⭐⭐】BEGIN/COMMIT/ROLLBACK 怎么工作？
- `BEGIN`：开启事务，之后的语句不立即生效
- `COMMIT`：全部成功，永久写入
- `ROLLBACK`：有失败，全部撤销

**项目举例**：try 块里 BEGIN + 操作 + COMMIT，catch 里 ROLLBACK。

---

## 六、schema 设计

### 【决策⭐⭐】你的 schema 怎么设计的？为什么是两张表？
两张表：`conversations`（会话）+ `messages`（消息），通过 conversation_id 外键关联。这是**一对多关系**——一个会话有多条消息。

为什么不放一张表？因为会话的属性（title、system_prompt）和消息的属性（role、content）是不同维度，分开存避免冗余（每个消息都重复存 title 没意义）。

---

### 【决策⭐⭐】阶段 15 加知识库时 schema 怎么扩展的？
加了 `knowledge_bases`（知识库）+ `documents`（文档）两张表，并给 `conversations` 加 `kb_id` 列实现会话级绑定：
```sql
ALTER TABLE conversations ADD COLUMN kb_id TEXT REFERENCES knowledge_bases(id);
```

为什么向量不存 SQLite？向量是 1024 维浮点数组，存 SQLite 又大又慢且没法做相似度搜索。SQLite 只存元数据（文件名、块数），向量交给向量库。

---

## 七、连接管理

### 【决策⭐⭐⭐】你的数据库连接为什么用单例（globalThis）？
Next.js 开发模式有热重载，每次重载重新执行模块代码。不做单例的话，每次热重载 `new DatabaseSync()` 开新连接，旧连接不释放——几次重载后连接泄漏。

挂到 globalThis 上，整个进程只保留一个连接，热重载复用。这是 Prisma/Drizzle 等库在 Next.js 里的标准做法。详见 `12.会话持久化.md` db.ts 解读。

---

### 【追问⭐⭐】为什么只在开发模式挂 globalThis？
单例只为解决热重载问题，而热重载只发生在开发模式。生产环境代码只加载一次，不会重复创建连接，挂全局反而浪费内存。

```ts
if (process.env.NODE_ENV !== "production") {
  globalForDb.db = db;
}
```

---

## 八、选型 / 进阶

### 【决策⭐⭐⭐】node:sqlite vs better-sqlite3，你怎么选的？
本来想用 better-sqlite3（工业级成熟），但安装时**编译失败**——它是原生模块，Windows 上要 node-gyp 编译，缺 Python + VS Build Tools。

改用 `node:sqlite`（Node 22 内置）：
- 零依赖零编译，Node 自带
- API 和 better-sqlite3 几乎一样，学到的知识可迁移
- 代价：实验性（有 warning）、需 Node >= 22

详见 `SQLite速通.md` 第一节三方案对比。

---

### 【追问⭐⭐⭐】如果项目要部署到 Vercel，SQLite 还能用吗？
不能。Vercel 是 serverless，没有持久本地文件系统，且不支持实验性 node:sqlite。要换：
- Turso（SQLite 云托管，libSQL）
- 或 Postgres（Vercel Postgres / Supabase）

SQL 语句大部分通用，主要改连接层（lib/db.ts）。这也是 `待学与待办.md` 标的部署改进点。

---

### 【追问⭐⭐⭐】高并发下 SQLite 有什么问题？
SQLite 是**单写入者**——同时只能有一个写操作，其它写要等。高并发写场景（如多用户同时发消息保存）会排队甚至锁库。

解决：
- WAL 模式（提升并发读写）
- 换 MySQL/PostgreSQL（多写并发）
- 写操作走队列串行化

---

### 【追问⭐⭐】你怎么直接看 chat.db 里有什么？
- GUI：DB Browser for SQLite 打开 chat.db
- 命令行：`sqlite3 chat.db` → `.tables` → `SELECT * FROM conversations`

---

## 速查（一页纸）

```
SQLite：单文件，vs MySQL/PostgreSQL 服务型
类型：TEXT/INTEGER/REAL/BLOB/NULL，无日期类型（用 TEXT 存 ISO）
PRIMARY KEY：唯一标识；AUTOINCREMENT：自增
UUID vs 自增：对外用 UUID（防泄露/遍历），对内用自增
NULL vs ""：不一样，查 NULL 用 IS NULL

外键：保证引用完整性
⚠️坑：SQLite 默认关外键，要 PRAGMA foreign_keys = ON
CASCADE：删被引用行时连带删引用行

索引：旁边的目录（不改变表数据），二分查找加速
代价：占空间 + 拖慢写入
看是否走索引：EXPLAIN QUERY PLAN

SQL注入：字符串拼接会被注入
防：永远用 ? 占位符 + prepared statement

事务：BEGIN/COMMIT/ROLLBACK，ACID
作用：多条语句原子化，不留半截数据

连接单例：globalThis 防热重载泄漏
node:sqlite vs better-sqlite3：编译失败换内置
Vercel 部署：SQLite 不行，换 Turso/Postgres
```
