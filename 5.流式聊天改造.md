# Next.js 聊天页升级为流式输出学习说明

这个文档基于你当前已经完成的 Next.js 最小聊天页项目，记录这次如何把 OpenAI 兼容的非流式聊天接口，升级成流式输出版本。

这次的目标是：

- 前端页面结构尽量不变
- 后端 `route.ts` 改成调用模型流式接口
- 前端使用 `fetch + ReadableStream` 接收流式响应
- 页面中逐步显示 AI 回复
- 保持代码最小化、教学化

---

## 这次改了哪些文件

```txt
app/
  api/
    chat/
      route.ts
  chat/
    page.tsx

components/
  ChatBox.tsx

5.流式聊天改造.md
```

---

## 这次升级的核心变化

之前的版本是：

- 前端调用 `/api/chat`
- 后端请求 OpenAI 兼容接口
- 后端等待完整 JSON
- 前端也等待完整 JSON
- 最后一次性显示完整回复

现在的版本是：

- 前端还是调用 `/api/chat`
- 后端请求 OpenAI 兼容流式接口
- 后端边读上游流，边把文本返回给前端
- 前端边读流，边更新最后一条 assistant 消息
- 页面会逐步显示 AI 回复

也就是说，你这次学的是：

**如何在不大改页面结构的前提下，把聊天接口从“一次性返回”升级成“边生成边显示”。**

---

## 推荐阅读顺序

建议按下面顺序阅读：

1. `components/ChatBox.tsx`
2. `app/api/chat/route.ts`
3. `app/chat/page.tsx`
4. `.env.local`
5. 这份文档

这样读的原因：

- 先看前端怎么逐步显示
- 再看服务端怎么把上游流式结果转出来
- 再确认页面本身其实没怎么变
- 最后再回头理解整条链路

---

## `components/ChatBox.tsx`

### 它这次改了什么

这是前端最关键的改动文件。

它从：

- `fetch('/api/chat')`
- `await response.json()`
- 一次性拿到 `{ reply }`
- 再一次性加入 assistant 消息

改成了：

- `fetch('/api/chat')`
- 从 `response.body` 读取流
- 用 `ReadableStream` + `getReader()` 逐步接收内容
- 每收到一段文本，就更新最后一条 assistant 消息

### 它现在负责什么

它依然负责：

1. 输入框状态
2. 消息列表状态
3. 点击发送
4. 请求 `/api/chat`
5. 把流式返回的文本逐步显示到页面上

### 它属于前端还是服务端

它属于前端。

### 它属于 React 还是 Next.js

#### React 部分

- `useState`
- 输入框状态
- 消息列表状态
- 逐步更新最后一条消息
- `loading` 状态

#### Next.js 部分

- `'use client'`
- 客户端组件运行方式

---

## 为什么前端不能继续用 `response.json()`

这是这次最重要的理解点之一。

### 非流式时

后端返回的是一个完整 JSON，例如：

```json
{ "reply": "你好" }
```

这种情况下可以直接：

```ts
const data = await response.json()
```

因为响应体本身就是一个完整的 JSON 文档。

### 流式时

现在后端不再一次性返回完整 JSON，而是边生成边返回文本块。

也就是说，响应体变成了：

- 先来一小段
- 再来一小段
- 再来一小段
- 最后结束

这时候它不是一个一开始就完整的 JSON，所以前端不能再：

```ts
await response.json()
```

因为你现在要的不是“最后一次性拿结果”，而是“边来边显示”。

所以必须改成：

- `response.body`
- `getReader()`
- `reader.read()`
- 每次拿一块文本
- 每次更新 UI

---

## `app/api/chat/route.ts`

### 它这次改了什么

这是服务端最关键的改动文件。

它从：

- 发送 `stream: false`
- `await response.json()`
- 读取 `choices[0].message.content`
- 再返回 `{ reply }`

改成了：

- 发送 `stream: true`
- 不再调用 `response.json()` 处理成功响应
- 改为读取上游 `response.body`
- 解析 OpenAI 兼容流式数据
- 提取 `choices[0].delta.content`
- 把纯文本继续流式返回给前端

### 它现在负责什么

它依然负责：

1. 接收前端发来的消息
2. 读取服务端环境变量
3. 请求 OpenAI 兼容接口
4. 读取上游流式响应
5. 解析出真正的文本内容
6. 再把文本流式返回给前端

### 它属于前端还是服务端

它属于服务端。

因为：

- `route.ts` 是 Next.js 服务端接口文件
- 它运行在服务端环境
- 它可以安全读取密钥
- 它负责对接第三方模型服务

### 它属于 React 还是 Next.js

这部分不属于 React，主要属于：

- Next.js API Route
- 服务端 HTTP 调用
- 流式响应处理

---

## 为什么后端也不能继续用 `response.json()`

这也是这次的核心点。

因为你现在请求上游时已经改成了：

```ts
stream: true
```

这意味着上游不会一次性给你完整 JSON，而是会不断推送分段数据。

OpenAI 兼容流式接口常见返回形式不是：

```json
{
  "choices": [
    {
      "message": {
        "content": "完整回复"
      }
    }
  ]
}
```

而更像是不断发这样的片段：

```txt
data: {"choices":[{"delta":{"content":"你"}}]}

data: {"choices":[{"delta":{"content":"好"}}]}

data: [DONE]
```

所以后端也不能再：

```ts
const data = await response.json()
```

因为成功响应已经不是一个完整 JSON 文档，而是一段一段到来的流。

---

## OpenAI 兼容流式接口这次是怎么处理的

## 1. 请求体改成 `stream: true`

```ts
body: JSON.stringify({
  model,
  messages: [
    {
      role: 'user',
      content: userMessage
    }
  ],
  stream: true
})
```

### 作用

告诉上游模型接口：

- 不要等全部生成完再给我
- 生成一点，就发一点

---

## 2. 读取上游 `response.body`

后端现在不再等完整 JSON，而是去读：

```ts
response.body
```

这代表的是一个可持续读取的数据流。

---

## 3. 逐行解析 `data:`

OpenAI 兼容流式接口常见会返回 SSE 风格文本。

所以后端要做这些事：

- 把二进制块解码成文本
- 按行切开
- 找到 `data:` 开头的行
- 遇到 `[DONE]` 结束
- 对 JSON 片段做 `JSON.parse(...)`
- 从 `choices[0].delta.content` 里拿文本

这一步的关键意义是：

**服务端把“供应商协议”转换成更简单的“纯文本流”。**

---

## 4. 为什么服务端不直接把原始 SSE 丢给前端

这是这次非常值得理解的架构点。

当然也可以把原始 SSE 直接传给前端，让前端自己去解析：

- `data:`
- `[DONE]`
- `delta.content`

但这样会让前端学习成本变高。

你当前这个项目的目标是最小化、教学化，所以更适合：

- 服务端负责协议适配
- 前端只负责读纯文本流

这样前端会更简单，也更符合你前面一直在学的结构：

- 前端对接自己的 `/api/chat`
- 服务端对接第三方模型服务

---

## assistant 消息为什么要先插入一个空白项

前端现在不是等完整回复出来后再新增一条消息，而是：

1. 先插入 user 消息
2. 再立刻插入一条空的 assistant 消息
3. 后面每收到一个文本块，就往这条 assistant 消息后面追加

例如一开始是：

```txt
你：你好
机器人：
```

然后逐步变成：

```txt
你：你好
机器人：你
```

再变成：

```txt
你：你好
机器人：你好
```

再继续变长。

这样页面就能表现出“边生成边显示”的效果。

---

## `app/chat/page.tsx`

### 它这次改了什么

这个文件几乎没有结构变化。

只需要轻微更新说明文字，让页面描述更符合现在的能力：

- 不只是调用 API
- 还会流式显示回复

### 为什么几乎不用改

因为真正的流式逻辑主要发生在：

- 前端组件 `components/ChatBox.tsx`
- 服务端接口 `app/api/chat/route.ts`

页面壳本身不需要承担流式处理逻辑。

---

## 当前完整链路是什么

现在整条链路是：

```txt
浏览器输入消息
  -> ChatBox.tsx 的 handleSend()
  -> fetch('/api/chat')
  -> Next.js 服务端 route.ts
  -> fetch('https://api.siliconflow.cn/v1/chat/completions')
  -> 请求体里带 stream: true
  -> 硅基流动持续返回 OpenAI 兼容流式片段
  -> route.ts 解析 data: ...
  -> route.ts 提取 delta.content
  -> route.ts 把纯文本继续流式返回给前端
  -> ChatBox.tsx 用 ReadableStream 逐块读取
  -> 不断更新最后一条 assistant 消息
  -> 页面逐步显示完整回复
```

---

## 这次升级后，你掌握了什么

### 1. 非流式和流式的核心区别

你会知道：

- 非流式 = 一次性拿完整结果
- 流式 = 边接收边处理

### 2. 为什么流式时不能继续用 `response.json()`

你会更清楚地知道：

- 因为返回的已经不是一整个 JSON
- 而是分段到达的数据流

### 3. 前端如何读取流式响应

你会接触到：

- `response.body`
- `getReader()`
- `reader.read()`
- `TextDecoder`

### 4. 服务端如何做协议适配

你会理解：

- 上游是 OpenAI 兼容 SSE 风格数据
- 前端不一定要直接处理这些协议细节
- 服务端可以先做一层转换

### 5. 为什么前端仍然只请求自己的后端

这能继续强化你的工程意识：

- 前端面向自己项目接口
- 服务端负责对接第三方供应商
- 协议变化尽量收敛在服务端

---

## 你应该怎么运行和验证

### 1. 确认 `.env.local`

```env
SILICONFLOW_API_KEY=你的硅基流动 key
OPENAI_BASE_URL=https://api.siliconflow.cn/v1
OPENAI_MODEL=你的真实模型名
```

### 2. 启动开发服务

```bash
npm run dev
```

### 3. 打开聊天页

访问：

- `http://localhost:3000/chat`

### 4. 观察现象

发送一条消息后，应该看到：

- 用户消息立即出现
- 机器人消息先出现一个空占位
- 回复内容会逐步增长
- 不是等很久后一次性整段出现

---

## 学完这一版后，你应该能回答什么问题

1. 为什么流式时前端不能继续用 `response.json()`
2. 为什么流式时后端也不能继续用 `response.json()`
3. `ReadableStream` 在这里扮演什么角色
- ReadableStream 是浏览器/Web 标准提供的流式数据抽象。在这个项目里它出现了两次：后端用它构造一个自定义响应流（生产者），前端用它持续读取响应体（消费者）。它让"边来边处理"成为可能，替代了"等一个完整 JSON 再处理"的旧写法。
4. 为什么 assistant 消息要先插入一个空项
5. 为什么服务端要解析 `delta.content`
6. 为什么服务端适合做协议适配层
7. 为什么前端页面结构其实可以基本不变

如果这些问题你都能讲清楚，说明你已经把“最小 Next.js 聊天页升级为流式版本”这一层打通了。
