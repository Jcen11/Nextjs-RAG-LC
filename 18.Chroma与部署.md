# 18. Chroma 向量库 + 部署改造（Turso/Vercel）

> 阶段 18 目标：把项目从"本地能跑"变成"能上线"。
> 前置知识见 `Docker速通.md`（Chroma 自托管）和 `Vercel部署速通.md`（部署障碍分析）。

---

## 一、这个阶段在解决什么

阶段 15-17 的项目有三个东西**不能部署**（详见 Vercel 速通第二节）：

| 障碍 | 原因 | 解决 |
|---|---|---|
| SQLite（node:sqlite） | serverless 文件系统临时，chat.db 存不住 | 换 Turso（云 SQLite，SQL 不变） |
| MemoryVectorStore | serverless 无持久内存，重启重嵌入 | 换 Chroma（持久化向量库） |
| .env.local | Vercel 不读本地文件 | 环境变量配到 Vercel 后台 |

本阶段完成了**代码侧的全部改动**，让项目支持两种运行方式：

```
本地开发（默认，零配置）：node:sqlite + MemoryVectorStore，行为完全不变
部署（Vercel）：Turso + Chroma，只靠环境变量切换
```

---

## 二、改动总览

| 文件 | 改动 |
|---|---|
| `lib/vectorstore.ts` | 新增 ChromaStore 实现（自托管 + Chroma Cloud 双模式），底部按环境变量选实现 |
| `lib/db.ts` | 重写为双后端：本地 node:sqlite / Turso（@libsql/client），统一 async 接口 + `dbRunBatch` 原子执行 |
| `lib/queries.ts` | 全部函数 async 化；新增 `saveMessagePair`（原子保存一轮对话） |
| `app/api/messages/route.ts` | 改用 `saveMessagePair`（替代手动 BEGIN/COMMIT 事务） |
| 其余 5 个 API 路由 | 查询调用补 `await` |
| `package.json` | 新增依赖 `chromadb`、`@libsql/client` |
| `.env.local.example` | 新增 DATABASE_URL / TURSO_AUTH_TOKEN / CHROMA_* 配置说明 |

**业务代码零改动的证明**：`lib/rag.ts`、`app/api/chat/route.ts` 一行没动——这就是阶段 15 做的"可插拔抽象层"兑现价值的地方。

---

## 三、向量库：Memory → Chroma

### 实现选择（vectorstore.ts 底部）

```ts
// CHROMA_URL（自托管）或 CHROMA_CLOUD_API_KEY（Chroma Cloud）任一存在 → Chroma
// 都没配 → Memory（本地零依赖，行为同阶段 15-17）
const useChroma = Boolean(process.env.CHROMA_URL || process.env.CHROMA_CLOUD_API_KEY);
export const vectorStore: VectorStore = useChroma ? new ChromaStore() : new MemoryStore();
```

### 两种连法

1. **自托管（Docker）**：`CHROMA_URL=http://localhost:8000`（或云服务器地址）。跑法见 `Docker速通.md` 第五节。
2. **Chroma Cloud（托管）**：`CHROMA_CLOUD_API_KEY` + `CHROMA_TENANT` + `CHROMA_DATABASE`。不用装 Docker，控制台创建即用，Vercel 也能直连。

### 关键设计点

- **数据隔离**：每个知识库一个 collection，名字 `kb_<kbId>`（uuid 满足 Chroma 命名要求）。对应 Memory 实现里"外层 Map 模拟命名空间"的做法。
- **统一构造路径**：把 chromadb 客户端（`ChromaClient` 自托管 / `CloudClient` 云）作为 `index` 传给 LangChain 的 `Chroma` 包装器，而不是传 `url`——这样 add/search/delete 三种操作共用同一个 client，删除知识库时能直接调 `client.deleteCollection()`。
- **维度显式声明**：`collectionMetadata: { dimension: 1024, "hnsw:space": "cosine" }`。Chroma Cloud 建集合时必须给维度（bge-m3 是 1024 维），cosine 适合语义相似度。
- **检索失败兜底**：`similaritySearch` 包 try/catch，集合不存在（如换环境后 SQLite 有元数据但 Chroma 没有集合）返回空数组，上层 chat 路由已有"检索失败降级普通对话"的逻辑。
- **删除幂等**：`deleteCollection` 集合不存在会报错，catch 忽略——删知识库本身是幂等操作。

### 踩坑记录

- `chromadb` 是 `@langchain/community` 的可选依赖，默认不装，要手动 `npm install chromadb`。
- LangChain 包装器内部用已弃用的 `path` 参数构造客户端，仍可用；我们自己建 client 传 `index` 绕开了这个坑，顺带支持了 Cloud 认证。

---

## 四、数据层：node:sqlite → 双后端

### 为什么必须 async 化

node:sqlite 是**同步** API；Turso 客户端走 HTTP，天生 **async**。两个后端要共用一套 `queries.ts`，只能统一成 async——本地后端在 async 函数里包一层同步调用，语义不变。这是本阶段最大的"看不见的改动"：`queries.ts` 全部函数 + 5 个路由的调用点。

### 后端选择与对外接口（db.ts）

```ts
const useTurso = Boolean(process.env.DATABASE_URL);

// queries.ts 只认这三个东西：
export function dbPrepare(sql: string): DbStatement;            // 预编译语句
export async function dbRunBatch(stmts): Promise<DbBatchResult[]>; // 原子执行
```

- **本地（默认）**：`node:sqlite` 建表在模块加载时同步完成；单例挂 `globalThis` 防热重载重复建连接。
- **Turso**：`DATABASE_URL=libsql://...` + `TURSO_AUTH_TOKEN`。建表不能同步做（HTTP），用懒初始化 promise——第一次用库时才建表，只跑一次。

### 事务处理的变化（值得面试讲）

阶段 12 的 `db_begin/commit/rollback`（手动 BEGIN/COMMIT）是**同步连接专属**的模式：Turso 的 HTTP 客户端没有"同一连接上先 BEGIN 再发语句"的概念。但它有 **batch**——一次 HTTP 请求内**原子**执行多条语句。

所以 `saveMessagePair`（保存 user + assistant + 可能的标题更新）改成：

```ts
const statements = [ /* 两条 INSERT */ ];
if (!conversation.title && userMessage.trim()) {
  statements.push(/* UPDATE 标题 */); // 判断在批次前完成，语句一次原子执行
}
const results = await dbRunBatch(statements);
// results[0].lastInsertRowid / results[1].lastInsertRowid → 新消息 id
```

两个后端语义一致：要么全成功要么全回滚。这本身就是个可讲的点：**"同一原子性目标，本地用事务、云端用 batch 两种实现，被接口隔离"**。

### 踩坑记录（重要的一个）

第一次 build 报 `LibsqlError: URL_INVALID: The URL 'undefined' is not in a valid format`：

- **根因**：`createClient({ url: DATABASE_URL })` 写在模块顶层，import 时就执行——本地开发没配 `DATABASE_URL`，URL 是 undefined，客户端构造时校验 URL 直接抛错。
- **修复**：客户端改成**惰性创建**（第一次真正用 Turso 时才 `createClient`），和 `useTurso` 分支对齐。
- **教训**：有副作用的初始化（校验参数、建连接）不能无条件放在模块顶层，要么惰性，要么和"是否启用"的判断绑在一起。

---

## 五、验证结果

- `npm run build`：通过（类型检查 + 打包全部正常）。
- 本地冒烟测试（node:sqlite 后端，无任何环境变量）：
  - 新建会话 → 保存一轮消息（原子 batch，返回 `messageIds [1,2]`）→ 读回历史（2 条、标题自动生成、systemPrompt 保留）→ 级联删除 ✓
  - 知识库新建 / 列表 / 删除 ✓
  - 清理后数据库无残留测试数据 ✓
- 说明：Turso / Chroma 两条链路需要在配好账号后验证（见下一节），代码按官方 API 编写。

---

## 六、部署操作清单（账号相关步骤，需要你自己操作）

### 第 1 步：把代码推到 GitHub

```bash
git remote add origin https://github.com/你的用户名/ai-knowledge-assistant.git
git push -u origin main
```

推送前确认：`.env.local` 在 `.gitignore` 里（当前已忽略）、历史提交里没泄露过 key。

### 第 2 步：开 Turso 数据库（免费）

1. 注册 https://turso.tech → 创建数据库（如 `rag-chat`）
2. 拿到 `DATABASE_URL`（`libsql://xxx-org.turso.io`）和 `TURSO_AUTH_TOKEN`

### 第 3 步：准备 Chroma（二选一）

- **方式 A 自托管**：一台有 Docker 的服务器（或本地开发时用 Docker Desktop）：
  ```bash
  docker run -d -p 8000:8000 -v chroma_data:/chroma/chroma --name chroma chromadb/chroma
  ```
  记下 `CHROMA_URL`（部署到 Vercel 时需要服务器有公网地址）。
- **方式 B Chroma Cloud（更省事）**：注册 https://trychroma.com → 创建 tenant + database → 拿 `CHROMA_CLOUD_API_KEY` / `CHROMA_TENANT` / `CHROMA_DATABASE`。不用自己运维服务器。

### 第 4 步：Vercel 部署

1. vercel.com 用 GitHub 登录 → Add New Project → 导入仓库（自动识别 Next.js，零配置）
2. Environment Variables 里配（全部**无前缀**，只在服务端）：
   - `SILICONFLOW_API_KEY` / `OPENAI_BASE_URL` / `OPENAI_MODEL` / `OPENAI_EMBEDDING_MODEL`（原有）
   - `DATABASE_URL` / `TURSO_AUTH_TOKEN`
   - `CHROMA_URL`（方式 A）或 `CHROMA_CLOUD_API_KEY` + `CHROMA_TENANT` + `CHROMA_DATABASE`（方式 B）
3. 建议加 `vercel.json` 提高超时（RAG 嵌入/检索可能超过默认 10s）：
   ```json
   {
     "functions": {
       "app/api/chat/route.ts": { "maxDuration": 60 },
       "app/api/knowledge/[id]/documents/route.ts": { "maxDuration": 60 }
     }
   }
   ```
4. Deploy → 拿公网网址 → 以后每次 `git push` 自动重新部署

---

## 七、遗留与后续

- [ ] **本地跑 Chroma 自托管需要 Docker Desktop**（本机当前没装；也可直接用 Chroma Cloud 跳过）
- [ ] **Turso / Chroma 链路的实际验证**：账号配好后做一次"部署版冒烟测试"（建库→传文档→绑定→提问）
- [ ] 部署版数据迁移：本地 chat.db 的旧会话/知识库不会自动跟到 Turso（可手动导出或用 SQLite 导出导入）
- [ ] 后续改进（待办里原有）：PDF/Word 支持、检索溯源展示、断连保存、`vercel.json` 已在本文给出
- [ ] 简历阶段（阶段 19）：可写"部署在 Vercel，Turso 云数据库 + Chroma 向量库，serverless 架构"

---

## 八、一页纸速查

```
代码侧（已完成）：
  vectorstore.ts  加 ChromaStore（自托管/Cloud 双模式）+ env 切换，Memory 保留默认
  db.ts           双后端（node:sqlite / Turso）+ 统一 async 接口 + dbRunBatch
  queries.ts      全部 async + saveMessagePair（原子保存一轮）
  路由层          补 await；messages 改用 saveMessagePair
  build + 本地冒烟测试 通过

部署（待你操作）：
  1. push GitHub → 2. 开 Turso → 3. Chroma（Docker 或 Cloud）→ 4. Vercel 导入 + env + vercel.json

环境变量速查：
  本地：什么都不配（零依赖，和阶段 17 一样）
  部署：DATABASE_URL + TURSO_AUTH_TOKEN + CHROMA_URL（或 CHROMA_CLOUD_API_KEY/TENANT/DATABASE）
```
