# 待学与待办

> 这份文档跟踪项目里标记出来、将来要深入或完成的事项。
> 跨设备/跨会话保留——换电脑时只要带着项目文件，这些记录就在。

---

## 🔖 待学项（概念/原理，想深入理解）

### 1. AST（抽象语法树）与 Markdown 解析原理

**标记时间**：阶段 8（Markdown 渲染）做完后

**触发点**：阶段 8 文档里讲到 `react-markdown` 的工作方式——它不是用正则把 `##` 替换成 `<h2>`，而是先把 Markdown 文本**解析成一棵结构化数据（AST，抽象语法树）**，再遍历这棵树生成 HTML。当时觉得这个点很关键，想之后单独深入。

**以后学这个，会搞明白**：
- AST 长什么样：一段 Markdown 文本是怎么组织成"节点树"的（标题节点、段落节点、代码节点……）
- 解析器怎么工作：`remark`（react-markdown 底层用的）怎么把字符串变成 AST
- 插件机制的本质：`remark-gfm`、`remark-math` 这些插件，其实就是在**修改这棵 AST**（给树加节点、改节点类型）
- 为什么 react-markdown 的 `components` 能拦截 `code`、`h1`：本质是"自定义某个 AST 节点怎么渲染成 JSX"

**为什么值得学（枢纽性知识）**：
学会它会突然看懂一大片工具的底层逻辑，因为"文本 → AST → 转换 → 输出"是通用套路：
- Markdown → AST → HTML（react-markdown）
- 源代码 → AST → 转换 → 新代码（Babel、ESLint、Prettier）
- 模板字符串 → AST → 渲染（Vue 模板编译）
- 往下还能通向编译原理（词法分析、语法分析）

**建议的实践**：从零写一个最小的 Markdown 解析器（只支持标题、段落、加粗），亲手体验"字符串 → AST → HTML"。

**触发时机**：等当前项目打磨完（复制按钮、回车发送、中止生成、持久化做完），对"用 Markdown 库"很熟之后，再回头学"库内部怎么工作"。

---

## ✅ 待办项（功能/任务，要继续做）

### 交互打磨阶段（文档 8 之后的方向）

- [x] 代码块复制按钮（和 Markdown 渲染直接相关，用 `navigator.clipboard.writeText`，改动最小）
- [x] 回车发送（input 加 `onKeyDown`，Enter 触发、Shift+Enter 换行）
- [x] 清空消息按钮（`setMessages([])`）
- [x] system 输入框可折叠
- [x] 中止生成（`AbortController`，让用户能"停止"AI 回复）—— 阶段 10
- [x] 错误处理（给 `route.ts` 包 `try/catch`，前端处理中断/超时）—— 阶段 11

### 第二阶段：产品深度（让产品"有深度"，文档 10/11 之后）

- [x] 会话持久化（node:sqlite，让聊天记录刷新不丢，URL 带 id）—— 阶段 12
- [x] 多会话管理（侧边栏，列出所有会话、切换/重命名/删除）—— 阶段 13
- [x] 全局状态管理（Zustand，根治阶段 12 遗留的组件重建 bug）—— 阶段 14
- [x] RAG 知识库（LangChain 切块/嵌入/检索 + 会话级绑定 + 可插拔向量库）—— 阶段 15
- [x] 路线 B 重构（ChatOpenAI 替代裸 fetch SSE，统一 LangChain 抽象）—— 阶段 16
- [x] UI 美化（Tailwind v4 + shadcn/ui，共存策略渐进式改造）—— 阶段 17

> 🎉 **Nextjs-RAG-LC 项目（阶段 1-17）已收官**。这是一个集成 RAG + LangChain 全家桶的 AI 知识库助手：
> 流式对话、多轮上下文、Markdown 渲染、错误处理、会话持久化、多会话管理、
> 全局状态管理、LangChain RAG（文档切块+bge-m3嵌入+向量检索+上下文注入）、
> **ChatOpenAI 统一抽象（替代裸 fetch SSE）**。

### 第三阶段：简历项目化（让产品"能写简历"，更长远）

- [x] 把 Next.js 项目和 LangChain+RAG 合并升级成"AI 知识库助手"—— 阶段 15（Nextjs-RAG 项目）
- [x] 路线 B 重构：用 ChatOpenAI 替代裸 fetch SSE，统一 LangChain 抽象—— 阶段 16（Nextjs-RAG-LC 项目）
- [ ] 向量库换 Chroma 实现 + 部署上线（Vercel/自己服务器，阶段 18）
- [ ] 简历写法：场景 + 技术栈 + 可量化成果（阶段 19）

### 🔖 RAG / 持久化改进点（后续打磨）

**改进点 1：向量库换 Chroma 实现部署上线**
- **现状**：阶段 15 用 MemoryVectorStore（内存单例），重启重嵌入、无法 serverless 部署
- **改进**：在 `lib/vectorstore.ts` 抽象层基础上加 ChromaStore 实现，换掉单例导出，业务代码零改动
- **触发时机**：阶段 18 部署时做

**改进点 2：支持 PDF/Word 文档加载**
- **现状**：阶段 15 只支持纯文本（.txt/.md），用 file.text() 读
- **改进**：引入 LangChain 的 PDFLoader/DocxLoader，扩展上传接口
- **价值**：简历可写"支持多格式文档"

**改进点 3：检索结果溯源展示**
- **现状**：检索到的片段直接拼进 system，用户看不到 AI 引用了哪些片段
- **改进**：检索结果带元数据（来源文件名、片段位置），UI 上展示"本回答基于：xxx.txt 片段1/2"
- **价值**：提升可信度，面试可讲"可解释的 RAG"

**改进点 4：断连也能保存（后端 tee 流边写边存）**
- **现状**：前端流式结束后 POST 一次保存。若用户中途关浏览器/断网，这轮对话完全丢失
- **改进**：后端 route.ts 给流加 tee 分支，边转发边累积，流结束时后端直接写库
- **触发时机**：阶段 16 重构流处理时一起做

**改进点 5：新建会话 useEffect 时序竞态（阶段 14 遗留）**
- **现状**：Zustand 解决了组件重建丢 state，但"新建会话时 useEffect 加载空历史覆盖 messages"的竞态仍部分存在（用 justCreatedId 缓解，非根治）
- **彻底解法**：配合改进点 4（后端 tee 边写边存）
- **当前可接受**：流式会自动填回内容，用户可能看到"闪一下空"

---

## 📌 已完成的阶段（供回顾）

- [x] 阶段 1：Next.js 最简页面
- [x] 阶段 2：AI 对话页面（模拟）
- [x] 阶段 3：接入真实 API（Anthropic SDK）
- [x] 阶段 4：OpenAI 兼容改造（原生 fetch + 硅基流动）
- [x] 阶段 5：流式聊天改造
- [x] 阶段 6：多轮上下文
- [x] 阶段 7：系统提示词 system message
- [x] 阶段 8：Markdown 渲染
- [x] 阶段 9：交互打磨（复制按钮 + 回车发送 + 清空 + system 折叠）
- [x] 阶段 10/11：中止生成（AbortController）+ 错误处理（try/catch 分层 + 状态恢复）
- [x] 阶段 12：会话持久化（node:sqlite + 动态路由 /chat/[id] + 流后保存）
- [x] 阶段 13：多会话管理（侧边栏 + 切换/重命名/删除 + 事件驱动刷新）
- [x] 阶段 14：全局状态管理（Zustand，URL↔store 双向同步，根治组件重建丢 state 的 bug）
- [x] 阶段 15：RAG 知识库（LangChain 切块/嵌入/检索 + 会话级绑定 + Memory 向量库 + 抽象层）
- [x] 阶段 16：路线 B 重构（ChatOpenAI 替代裸 fetch SSE，统一 LangChain 抽象，保留所有中断/错误处理打磨）
- [x] 阶段 17：UI 美化（Tailwind v4 + shadcn/ui，共存策略，Button/Input/Textarea/ScrollArea 换入 ChatBox + layout 重构）← Nextjs-RAG-LC 项目收官

---

## ⚙️ 环境约束（换电脑时注意）

- **Node >= 22**：阶段 12 起用了 `node:sqlite`（Node 22 内置实验性模块）。低于 22 会报错。若换电脑后 Node 版本不够，可改用 `better-sqlite3`（需配 Python + 编译器，API 几乎一样，见文档 12 第一节）。
- **数据库文件 chat.db**：在项目根目录，被 .gitignore 忽略不进 git。换电脑后会自动新建一个空库（历史不跟随，因为是本地数据）。
