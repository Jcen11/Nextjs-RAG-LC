# 阶段 12 补充：修复"新建会话首条消息被吃掉"的竞态 bug

> 这是 `12.会话持久化.md` 的补充文档。原始文档讲了持久化的设计和实现，这份专门记录一个**真实运行中发现的 bug**——从现象、定位、根因到修复，完整复盘一遍 debug 过程。
>
> 为什么要单独成文：① debug 过程有学习价值（怎么从"现象"反推"代码哪错了"）；② 原始文档已经很长，混进去会喧宾夺主。

---

## 一、现象（用户反馈）

实际运行中发现：

> **新建会话的第一条消息会被"吃掉"**——用户从 `/chat`（无 id）进入，输入消息发送后，消息有时不显示；但**刷新页面后又能重新显示**。

"刷新能恢复"是关键线索——说明数据**已经存进数据库**了，问题出在**前端的显示**，不是后端的存储。

## 二、定位：刷新能恢复 → 问题在显示层

先从"刷新能恢复"这个现象反推：

```
刷新能恢复
  → 数据在数据库里（刷新时 useEffect 重新 GET，取到了完整历史）
  → 所以 POST /api/messages 保存是成功的，数据库有数据
  → 问题不在"存"，在"显示"
  → 那为什么首次发送时不显示？
  → 聚焦"首次发送"这条代码路径——handleSend 里发生了什么特殊的事？
```

## 三、根因：router.push 触发的 useEffect 和 handleSend 抢着改 messages

### 复盘首次发送的代码路径

`handleSend` 里，首次发送（无 conversationId）会做这几件事：

```tsx
// ChatBox.tsx 的 handleSend，简化版
let activeConvId = conversationId;
if (!activeConvId) {
  // ① 创建会话
  const res = await fetch("/api/conversations", { method: "POST", ... });
  activeConvId = (await res.json()).id;
  // ② 跳转 URL
  router.push(`/chat/${activeConvId}`);
}
// ③ 把 user 消息 + 空 assistant 占位加进 messages
setMessages((prev) => [...prev, userMessage, assistantMessage]);
// ④ POST /api/chat 流式接收，不断更新 messages
```

### 关键问题：第 ② 步 router.push 会触发 useEffect

`router.push` 改变了 URL，导致传给 ChatBox 的 `conversationId` prop 从 `undefined` 变成新 id。而 useEffect 的依赖数组是 `[conversationId]`：

```tsx
useEffect(() => {
  if (!conversationId) {
    setMessages([]);   // 无 id 清空
    return;
  }
  // 有 id：去取历史
  (async () => {
    const res = await fetch(`/api/conversations/${conversationId}`);
    const data = await res.json();
    setMessages(data.messages.map(...));  // ← 关键：用数据库历史覆盖 messages
  })();
}, [conversationId]);
```

**conversationId 变了 → useEffect 重跑 → 它会去取这个新会话的历史**。但这个会话是第 ① 步刚创建的，数据库里**还没有任何消息**（保存是第 ④ 步之后才发生的）。所以 `data.messages` 是空数组，`setMessages([])` 把当前显示清空了。

### 两股力量在抢 messages（竞态）

```
力量 A：handleSend 继续执行
  ③ setMessages([user, assistant空占位])
  ④ 流式更新，messages 累积成 [user:"你好", assistant:"你好啊..."]

力量 B：useEffect 被触发（conversationId 变了）
  fetch 历史 → 空（数据库还没存）
  setMessages([])   ← ⚠️ 把 A 显示的内容清空了！
```

A 和 B 都在异步操作 messages，谁后执行谁生效。如果 B（useEffect 的 setMessages([])）晚于 A 的流式更新，用户就会看到消息突然消失——这就是"首条消息被吃掉"的根因。

### 时序图

```
时间 →

T0  handleSend 开始
T1  ① POST 创建会话 → 拿到 id "f47ac10b-..."
T2  ② router.push("/chat/f47ac10b-...")
     ├─ URL 变了
     ├─ conversationId: undefined → "f47ac10b-..."
     └─ useEffect 被安排执行（异步，进队列）
T3  ③ setMessages([user:"你好", assistant:""])  ← handleSend 继续
T4  ④ POST /api/chat 流式接收，不断 setMessages，assistant 内容累积
T5  useEffect 执行：fetch 历史
T6  useEffect 的 fetch 回来：data.messages = []（数据库还没存）
T7  setMessages([])  ← ⚠️ 清空！把 T3/T4 显示的内容抹掉了
```

用户看到的就是 T7 那一刻：消息突然没了。刷新时走的是全新的 mount，useEffect 取到的是完整历史（T7 之后保存也完成了），所以"刷新能恢复"。

## 四、为什么不是每次都复现

"有时被吃、有时正常"——这是**竞态**的典型特征。两股异步力量的执行顺序不固定：

- 如果 useEffect 的 fetch（T5→T6）**慢于** handleSend 的流式更新完成，那 setMessages([]) 来得晚，会清空已显示的内容 → **消息消失**
- 如果 useEffect 的 fetch **快于** handleSend 的后续 setMessages，那 setMessages([]) 先执行，handleSend 的 setMessages 后执行并覆盖回来 → **看起来正常**

网络速度、数据库响应、流式 chunk 频率都会影响谁先谁后，所以表现为"有时消失有时正常"。

## 五、修复方案：用 ref 标记"这是本地刚建的会话"

核心思路：让 useEffect 知道"这个 conversationId 是 handleSend 刚创建的，数据库里还没东西，别去取历史"。

### 加一个 ref 标记

```tsx
// 组件顶部，和 abortRef 并列
const justCreatedRef = useRef(false);
```

useRef 在这里的作用和 abortRef 一样——跨函数共享一个值，且改它不触发重渲染。handleSend 写入，useEffect 读取。

### handleSend 里：创建会话后、router.push 前打标记

```tsx
if (!activeConvId) {
  const res = await fetch("/api/conversations", { ... });
  activeConvId = (await res.json()).id;
  justCreatedRef.current = true;   // ← 打标记（必须在 push 之前）
  router.push(`/chat/${activeConvId}`);
}
```

**为什么必须在 push 之前**：push 会同步触发 useEffect 重跑，标记要先于 useEffect 设置好，useEffect 才读得到。

### useEffect 里：开头检查标记

```tsx
useEffect(() => {
  if (!conversationId) {
    setMessages([]);
    return;
  }

  // ← 新增：本地刚建的会话，跳过加载历史
  if (justCreatedRef.current) {
    justCreatedRef.current = false;  // 用完立刻复位
    return;
  }

  // 正常加载历史...
  (async () => { ... })();
}, [conversationId]);
```

读到标记为 true 时：① 复位（用完归零，下次正常会话切换不受影响）；② 直接 return，跳过 fetch 历史。这样 messages 保持 handleSend 设置的内容，不被空历史覆盖。

### 修复后的时序

```
T0  handleSend 开始
T1  ① POST 创建会话 → id "f47ac10b-..."
T2  justCreatedRef.current = true   ← 打标记
T3  ② router.push → conversationId 变了 → useEffect 安排执行
T4  ③ setMessages([user, assistant空占位])  ← handleSend 继续
T5  ④ 流式接收，messages 累积
T6  useEffect 执行：
      读到 justCreatedRef = true
      → 复位 justCreatedRef = false
      → return（跳过 fetch 历史）
      ← 不再 setMessages([])，不会清空！
T7  handleSend 的流式更新正常完成，消息正常显示 ✓
```

## 六、为什么不直接换思路（几种替代方案对比）

debug 时考虑过几种解法，记录下来理解权衡：

| 方案 | 做法 | 为什么没选 |
|---|---|---|
| **A. ref 标记**（采用） | handleSend 打标记，useEffect 检查跳过 | 改动最小，逻辑清晰，不影响其它路径 |
| B. 不用 router.push | 创建会话后不跳 URL，只在内部 state 记 id | URL 不会更新，刷新会回到 /chat（无 id），丢失会话身份——违背持久化初衷 |
| C. useEffect 不覆盖非空 messages | if (messages.length > 0) return | 危险——正常切换会话时也跳过加载，导致切换会话不显示历史 |
| D. 后端创建会话时直接存第一条消息 | 创建会话和保存消息合并 | 改动大，且流式回复还没完成时没法存 assistant 内容 |

方案 A 最干净：它精确针对"本地刚建的会话"这一个场景，不影响其它任何路径（正常切换、刷新恢复都不受影响）。

## 七、这个 bug 教会我什么

### 1. 从现象反推根因的思路

```
现象：首条消息被吃掉，刷新能恢复
  → 推论1：刷新能恢复 → 数据库有数据 → 问题在显示
  → 推论2：只在"首次发送"出现 → 聚焦 handleSend 的特殊路径
  → 推论3：handleSend 首次发送特殊在哪？→ router.push 改变 conversationId
  → 推论4：conversationId 变了会怎样？→ useEffect 重跑
  → 推论5：useEffect 重跑会做什么？→ setMessages(历史)
  → 推论6：新会话的历史是空的 → setMessages([]) 清空了显示
  → 根因找到
```

**关键习惯**：不要一上来就猜代码哪错了，先从可观察的现象一步步反推，每一步都有依据。

### 2. "竞态"是异步编程的常见陷阱

两个异步操作（handleSend 的流式更新 + useEffect 的 fetch）操作同一个 state，执行顺序不固定，结果就不固定。这就是**竞态条件（race condition）**。

解决竞态的通用思路：
- **加锁/标记**（本例的 ref）：告诉一方"现在别动"
- **取消旧操作**（阶段 10 的 cancelled 标志）：让旧的失效
- **串行化**：强制顺序执行

### 3. ref 的第二个用途

之前 abortRef 用 ref 是为了"跨函数共享 controller"。这里 justCreatedRef 用 ref 是为了跨函数传递一次性信号——handleSend 设置，useEffect 读取并复位。两个场景本质相同：ref 是组件内跨函数共享数据、且不触发重渲染的工具。

## 八、改动汇总

只改了 components/ChatBox.tsx 一个文件，三处：

| 位置 | 改动 |
|---|---|
| 组件顶部 | 新增 const justCreatedRef = useRef(false); |
| useEffect 开头 | 新增 if (justCreatedRef.current) { justCreatedRef.current = false; return; } |
| handleSend 创建会话后 | 新增 justCreatedRef.current = true;（在 router.push 之前） |

12.会话持久化.md 里的 5.3 节和 A8 答案也更新了，指向本文档。

---

## 九、自测题

### Q1
"刷新能恢复"这个现象，排除了哪个可能？指向了哪个方向？

### Q2
为什么这个 bug 表现为"有时消失有时正常"，而不是"每次必现"？

### Q3
为什么 justCreatedRef.current = true 必须写在 router.push 之前？

### Q4
如果用户正常切换会话（从 /chat/aaa 点链接到 /chat/bbb），justCreatedRef 会干扰吗？为什么？

### Q5（进阶）
方案 C（useEffect 里 if (messages.length > 0) return）为什么不行？它会破坏什么正常场景？

<details>
<summary>答案</summary>

**A1**：排除了"数据库没存上"的可能（刷新能取到，说明存了）。指向"前端显示"——是 useEffect 在首次发送时错误地覆盖了 messages。

**A2**：因为是竞态——handleSend 的流式 setMessages 和 useEffect 的 setMessages([]) 谁后执行谁生效。两者都是异步的，执行顺序受网络/数据库响应速度影响，所以不固定，表现为偶发。

**A3**：因为 router.push 会同步触发 useEffect 重跑（conversationId 变了）。如果标记写在 push 之后，useEffect 执行时标记还没设置，读到的还是 false，修复就失效了。

**A4**：不会。正常切换会话时 handleSend 没被调用，justCreatedRef.current 保持初始值 false。useEffect 读到 false，走正常加载历史流程，不受影响。只有 handleSend 主动创建会话时才会设 true。

**A5**：它会破坏"切换会话"场景。用户从会话 A 切到会话 B 时，messages 里还有 A 的内容（length > 0），useEffect 会直接 return，导致 B 的历史加载不出来，用户看到的是 A 的消息。方案 C 没法区分"有内容是因为本地刚建"还是"有内容是因为显示着旧会话"，所以不行。ref 标记方案能精确区分。

</details>

---

# 第三轮：前两轮都没根治 → 引入全局状态管理（Zustand）

> 第一轮加 ref 标记，失败；第二轮分析出"组件重建"是根因。但前两轮都在"修修补丁"——试图让 useEffect 跳过加载、或者换查询参数绕开重建。这一轮换个视角：**问题的本质不是 useEffect 的问题，是 state 放错了地方**。
>
> 完整架构方案见 **`14.全局状态管理.md`**，这里只讲 debug 视角的演进。

## 为什么前两轮没根治

### 第一轮（ref 标记）失败的原因

`justCreatedRef` 存在**组件实例内部**。当 `router.push("/chat/abc")` 触发 `/chat` → `/chat/abc` 跳转时，Next.js 因为是两个不同的页面文件（`page.tsx` vs `[id]/page.tsx`），**卸载旧 ChatBox、挂载新 ChatBox**。新实例的 `justCreatedRef` 是初始值 `false`，读不到旧实例设的 `true`。

```
旧 ChatBox（/chat 里的）
  ├─ messages = [user:"你好", ...]
  ├─ justCreatedRef = true    ← 设了
  └─ 即将销毁 ↓

新 ChatBox（/chat/abc 里的，全新创建）
  ├─ messages = []            ← 全新的 state
  ├─ justCreatedRef = false   ← 全新的 ref，读不到旧实例的 true
  └─ useEffect 跑：取 abc 历史（空）→ setMessages([])
```

**根因**：ref 跟着组件实例生死，跨实例不传递。

### 第二轮（换查询参数）为什么不彻底

第二轮打算改用 `/chat?id=abc`（查询参数），让一个页面文件同时处理两种情况，组件不重建。

但这只是"绕开"重建，没解决根本问题：**messages 仍然存在组件 useState 里**。只要还在组件内部，任何意外的重建（比如未来 layout 变化、Suspense 边界、并发渲染）都可能再丢一次。而且 Sidebar 也有同类隐患（重命名时如果重建，编辑焦点会丢）。

**根本问题**：跨组件、跨重建需要存活的状态，不该放在组件里。

## 第三轮：把 state 提到组件外（Zustand store）

### 换个视角看 bug

前两轮一直在问"怎么让 useEffect 不覆盖 messages"。第三轮换了个问题：

> **messages 为什么放在 ChatBox 的 useState 里？**

答案：因为阶段 12 设计时没有全局状态管理，自然用了 useState。但 messages 需要**跨组件（Sidebar 要知道当前会话）+ 跨重建（URL 跳转会重建）**存活——这正是"全局状态"该解决的问题。

### Zustand 怎么根治

Zustand 的 store 是一个**存在于组件之外的数据源**。组件通过 `useChatStore((s) => s.messages)` 订阅它。组件卸载/重建，store 不受影响——它在组件树的"外面"。

```
改造前：messages 在 ChatBox useState 里
  ChatBox 重建 → state 清零 → messages 丢 → bug

改造后：messages 在 store 里
  ChatBox 重建 → 从 store 读 messages → 不丢 → bug 消失
```

ChatBox 的核心改动：
```tsx
// 改造前（阶段 12）
const [messages, setMessages] = useState<Message[]>([]);  // 在组件内

// 改造后（阶段 14）
const messages = useChatStore((s) => s.messages);          // 从组件外的 store 读
const setMessages = useChatStore((s) => s.setMessages);    // action 也在 store
```

因为 messages 现在在 store 里，ChatBox 重建后直接从 store 读到之前的值，`justCreatedRef` 那个 hack 彻底不需要了——删掉。

### URL ↔ store 双向同步

引入 store 后，"当前是哪个会话"（currentId）也在 store 里。但 URL 仍然要保留 `/chat/abc`（可分享、刷新恢复、前进后退）。所以需要双向同步：

- **URL → store**：`[id]/page.tsx` 从 URL 读 id，调 `store.setCurrentId(id)`。新建的客户端组件 `SyncConversationId` 干这事
- **store → URL**：Sidebar 切会话、ChatBox 新建会话时调 `router.push`，URL 变了再触发上面的 URL→store

store 是"真相源"（single source of truth），URL 是它的镜像。

## 三轮 debug 的完整时间线

```
阶段 12（会话持久化）
  ├─ 引入 router.push 跳转 + messages 放组件 useState
  └─ 埋下 bug：新建会话首条消息被吃掉

第一轮（阶段 12 内尝试修复）
  ├─ 加 justCreatedRef 标记，让 useEffect 跳过加载
  └─ 失败：ref 在组件实例内，组件重建后新实例读不到

第二轮（分析根因）
  ├─ 发现 router.push 导致组件卸载重建
  ├─ ref 跨实例失效的原理
  └─ 考虑换查询参数绕开重建，但只是"绕开"不是"根治"

第三轮（阶段 13/14，根治）
  ├─ 换视角：问题不在 useEffect，在 state 放错了地方
  ├─ 引入 Zustand：messages 提到组件外的 store
  ├─ 组件重建后从 store 读，不丢
  └─ justCreatedRef 删除，URL↔store 双向同步建立
```

**关键教训**：
1. **修 bug 要找根因，不要打补丁**。前两轮都在问"怎么让 useEffect 别覆盖"，第三轮才发现真正该问的是"messages 为什么放在组件里"
2. **有些 bug 的修复需要架构改动，不是一行代码能解决**。这个 bug 从 ref → 查询参数 → 全局状态管理，跨度很大。识别"这是个架构问题"比急着修更重要
3. **全局状态管理是分水岭**。Sidebar + ChatBox 需要共享状态的场景，本就该用 store。阶段 13 做多会话管理时引入是自然时机

## 详细架构方案

完整的 Zustand store 设计、URL↔store 同步机制、组件改造细节、自测题，全部在 **`14.全局状态管理.md`**。本文档只记录 debug 视角的演进，避免重复。

---

# 第四轮：实测验证 + 两个残留 bug 的修复

第三轮引入 Zustand 后实测，发现还有两个残留问题：

## 残留 bug 1：新建会话首条消息仍被吃

### 实测现象
- 新建会话发消息 → 消息不显示
- 刷新 → 消息能显示
- 点该会话 → 也能显示

### 根因：useEffect 在数据库保存前就加载了空历史

第三轮的 Zustand 解决了"组件重建丢 state"，但**没解决"新建会话时 useEffect 加载空历史覆盖"**这个时序竞态：

```
handleSend 创建会话（id=abc），但此时还没保存到数据库
  → router.push("/chat/abc")
  → 新 ChatBox mount，useEffect 跑
  → currentId 变成 abc，去 GET /api/conversations/abc
  → 数据库是空的（保存是流式结束后 finally 才发生）
  → setMessages([]) 覆盖了 store.messages（刚发的用户消息没了）
```

之前的 `justCreatedRef` 思路是对的（标记"别加载历史"），但放错了地方——ref 在组件实例内，重建后丢失。**放进 store 就跨实例存活了**。

### 修复：store 里加 justCreatedId

```ts
// store.ts 新增字段
justCreatedId: string | null;        // 刚创建但还没保存的会话 id
setJustCreatedId: (id) => ...        // setter

// ChatBox handleSend 创建会话后、router.push 前：
setJustCreatedId(activeConvId);      // 标记（必须在 push 前）
router.push(`/chat/${activeConvId}`);

// ChatBox useEffect 开头检查：
if (currentId && currentId === justCreatedId) {
  setJustCreatedId(null);            // 用完清标记（下次刷新正常加载）
  return;                            // 跳过加载历史
}
```

**为什么这次能成功**：`justCreatedId` 在 store 里，不在组件实例内。组件重建后，新实例从 store 读到 `justCreatedId`，依然能识别"这是刚建的会话"。

## 残留 bug 2：左侧会话栏不显示新建会话

### 实测现象
- 新建会话发消息 → 侧边栏没有新会话
- 刷新 → 新会话出现

### 根因：router.refresh 对客户端组件无效

第三轮改造时，Sidebar 删掉了 `[pathname]` 依赖（改为只 mount 时拉一次）。ChatBox 保存消息后调 `router.refresh()` 想刷新侧边栏——但 **router.refresh 刷新的是服务端组件树，对客户端组件 Sidebar 无效**。所以新建会话后，Sidebar 的 conversations 列表没更新。

### 修复：用 store 的 refreshConversations 替代 router.refresh

在 store 里加一个 `refreshConversations` action（封装 fetch + setConversations）：

```ts
// store.ts
refreshConversations: async () => {
  const res = await fetch("/api/conversations");
  const data = await res.json();
  set({ conversations: data.conversations });
},
```

ChatBox 保存消息成功后调它（替代 router.refresh）。因为 conversations 在 store 里，Sidebar 订阅了它，store 一更新 Sidebar 就自动重渲染。Sidebar 自己的 mount 也调它拉初始列表。

**这是全局状态管理的额外好处**：跨组件同步数据不用靠 router 触发，直接改 store，所有订阅者自动更新。

## 四轮 debug 完整复盘

| 轮次 | 尝试 | 结果 | 教训 |
|---|---|---|---|
| 1（阶段12） | justCreatedRef 标记 | 失败 | ref 在组件实例内，重建丢失 |
| 2 | 分析根因：组件重建 | 找到方向 | 但只诊断没修 |
| 3（阶段14） | Zustand 把 state 提到组件外 | 部分成功 | 解决了重建丢 state，但没解决 useEffect 时序竞态 |
| 4（本次） | justCreatedId 放进 store + refreshConversations | **根治** | 同样的标记思路，放对位置（store）才有效 |

**最大教训**：第二、三轮反复在"标记"和"组件重建"上打转。第四轮才发现两个独立 bug（消息被吃 + 侧边栏不刷新）被混在一起，分开解决才清晰。

---

# 第五轮：日志定位"消息闪烁后消失"——useEffect 依赖数组的自我触发

第四轮修复后实测，bug 变形：消息**能渲染出来**（说明进了 store），但**马上消失**（被某次 setMessages([]) 覆盖），刷新或切换回来又能显示。这次用日志精确定位。

## 调试方法：加临时 console.log

在 useEffect 开头和 handleSend 关键节点加日志，打印 `currentId`、`justCreatedId`、`store.messages.length`。复现后看控制台输出。

## 日志揭示的真相

关键日志（精简）：
```
[handleSend] setMessages 写入，store 即将有 2 条
[handleSend] 创建会话 id=abc 设 justCreatedId 并 push
[useEffect] currentId=null justCreatedId=abc      → 跳过（保护生效）
[useEffect] currentId=null justCreatedId=abc      → 跳过（保护生效）
[useEffect] currentId=abc justCreatedId=abc       → 清 justCreatedId
[useEffect] currentId=abc justCreatedId=null      → ⚠️ 加载历史！
```

**最后两行是 bug**：清标记 `setJustCreatedId(null)` 之后，useEffect **又跑了一次**，这次 justCreatedId 已经是 null，保护失效，走到加载历史分支，fetch 到空数组（数据库还没保存），覆盖 store.messages。

## 根因：useEffect 依赖数组的"自我触发"

```ts
// 出问题的写法
const justCreatedId = useChatStore((s) => s.justCreatedId);  // 订阅它

useEffect(() => {
  if (currentId === createdId) {
    setJustCreatedId(null);  // ← 清标记
  }
  // ...
}, [currentId, justCreatedId]);  // ← justCreatedId 在依赖里！
```

链条是这样的：
```
setJustCreatedId(null)         清标记
  → store.justCreatedId 变成 null
  → 因为 justCreatedId 在依赖数组里
  → React 检测到依赖变化，useEffect 重跑
  → 重跑时 createdId 已经是 null
  → 保护分支 if (createdId) 不成立
  → 走到加载历史分支
  → fetch 到空数组（数据库还没保存）
  → setMessages([]) 覆盖
  → 消息消失
```

**根因**：清标记这个动作，因为标记在依赖数组里，**触发了自己的重跑**——典型的 useEffect 依赖自我触发陷阱。

## 修复：justCreatedId 移出依赖，改用 getState 实时读

```ts
// 不再用 selector 订阅 justCreatedId（避免它进依赖数组）
// const justCreatedId = useChatStore((s) => s.justCreatedId);  // ❌ 删掉

useEffect(() => {
  // 用 getState() 实时读，不建立订阅
  const createdId = useChatStore.getState().justCreatedId;
  if (createdId) {
    if (currentId === createdId) {
      setJustCreatedId(null);  // 清标记不会触发重跑（因为没订阅）
    }
    return;
  }
  // ...
}, [currentId]);  // ← 只依赖 currentId，justCreatedId 不进依赖
```

**关键区别**：
- `useChatStore((s) => s.justCreatedId)`：**建立订阅**，值变会触发组件重渲染，如果在依赖数组里还会触发 useEffect 重跑
- `useChatStore.getState().justCreatedId`：**只读一次当前值**，不建立订阅，值的后续变化不会触发任何东西

用 getState 读，清标记这个动作就不会再触发 useEffect 重跑，自我触发的链条断开。

## 这次调试的教训

1. **useEffect 依赖数组要警惕自我触发**：如果 effect 内部修改了依赖列表里的值，会形成"改→重跑→改→重跑"的循环或连锁
2. **"订阅"和"读一次"是两回事**：selector（`useChatStore((s) => s.x)`）建立订阅，getState() 只读快照。不需要响应变化的值，用 getState 读更安全
3. **日志调试胜过推理**：这个 bug 涉及多次异步 useEffect 执行的时序，纯靠脑子推容易漏。加日志打印每次执行的 currentId/justCreatedId，一眼就看出"清标记后又跑了一次"
4. **bug 会变形**：第三轮是"完全不显示"，第四轮是"显示了但侧边栏不更新"，第五轮是"显示了又消失"。每次修复改变症状，要持续实测到完全正常

## 五轮 debug 完整复盘（更新版）

| 轮次 | 症状 | 尝试 | 结果 |
|---|---|---|---|
| 1（阶段12） | 消息不显示 | justCreatedRef 标记 | 失败（ref 在实例内，重建丢） |
| 2 | 同上 | 分析出组件重建 | 诊断方向 |
| 3（阶段14） | 消息不显示 | Zustand 把 state 提到组件外 | 部分（解决重建，没解决时序） |
| 4 | 消息不显示 + 侧边栏不更新 | justCreatedId 放 store + refreshConversations | 部分（保护被自我触发绕过） |
| 5（本次） | 消息显示后消失（闪烁） | justCreatedId 移出依赖，用 getState 读 | **根治** |

**最终修复的核心**：保护标记（justCreatedId）必须存在 store（跨实例存活），但**不能用订阅方式读它**（否则清标记触发 useEffect 重跑，保护失效）。用 getState() 只读不订阅，链条才断开。
