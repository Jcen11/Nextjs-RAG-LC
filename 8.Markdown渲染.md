# Next.js 聊天页加入 Markdown 渲染学习说明

这个文档基于你当前已经完成的 Next.js 多轮 + system + 流式聊天项目，继续给它加上 Markdown 渲染。

这次的目标是：

- 让 AI 回复里的标题、列表、加粗、表格、代码块正常显示
- 代码块带语法高亮
- 用户消息和 AI 消息有样式区分
- 引入第三方渲染库，而不是手写 Markdown 解析
- 保持代码最小化、教学化

这是从阶段 7 开始的"交互打磨"阶段里，**见效最快、视觉冲击最大**的一步。

---

## 这次改了哪些文件

```txt
components/
  Markdown.tsx          # 新增：Markdown 渲染组件
  ChatBox.tsx           # 修改：用 Markdown 组件渲染 assistant 消息

app/
  globals.css           # 修改：追加 Markdown 排版 + 消息样式

package.json           # 新增依赖

8.Markdown渲染.md
```

这次没有改动：

- `app/api/chat/route.ts`（后端完全不动）
- `app/chat/page.tsx`（页面壳不动）
- `.env.local`、`.env.local.example`

也就是说：

- 流式逻辑没变
- 多轮上下文没变
- system 提示词没变
- 模型供应商没变

变化只发生在"**AI 回复怎么显示**"这一层。

---

## 这次升级的核心目标

之前的版本是：

- AI 回复直接用 `{message.content}` 显示
- 代码块、列表、加粗这些 Markdown 标记**原样显示成字符**
- 用户和 AI 消息没有任何样式区分

现在的版本是：

- AI 回复经过 Markdown 渲染，标题/列表/表格/代码块正常排版
- 代码块带语法高亮（不同关键字不同颜色）
- 用户和 AI 消息有背景色区分

也就是说，你这次学的是：

**如何让 AI 回复从"一坨带 # 和 * 的纯文本"变成"有格式的内容"。**

---

## 推荐阅读顺序

建议按下面顺序阅读：

1. `components/Markdown.tsx`（新增）
2. `components/ChatBox.tsx`（看渲染那段改动）
3. `app/globals.css`（看追加的样式）
4. 这份文档

这样读的原因：

- 先看渲染组件本身
- 再看它怎么被接入聊天框
- 再看样式怎么配合
- 最后回头理解整体

---

## 关键概念：AI 回复为什么需要 Markdown 渲染

这是这次必须先理解的事。

### LLM 的输出天然是 Markdown 格式

你问 AI 一个问题，它的回复几乎一定是这样的结构：

```markdown
## 什么是闭包

闭包是指**函数与其词法环境的组合**。常见例子：

\`\`\`js
function outer() {
  let count = 0
  return function inner() {
    count++
  }
}
\`\`\`

主要特点：

1. 可以访问外部变量
2. 延伸了变量的生命周期
```

这里面有：二级标题、加粗、代码块（带语言标识）、有序列表。

### 不渲染会怎样

如果你直接 `{message.content}` 显示，用户看到的是：

```
## 什么是闭包

闭包是指**函数与其词法环境的组合**。常见例子：

​```js
function outer() {
...
```

那些 `##`、`**`、` ``` ` 全是**裸字符**，看起来像没排版的草稿。这就是为什么必须渲染。

### 渲染后

- `##` 变成大字号的二级标题
- `**...**` 变成**加粗**
- ` ```js ... ``` ` 变成带颜色的代码块
- `1.` 变成真正的有序列表

这才是用户期待的"AI 产品"的样子。

---

## 每个文件应该学什么

## `components/Markdown.tsx`（新增）

### 它是干什么的

这是一个独立的 Markdown 渲染组件：

- 接收一个 `content` 字符串
- 把它渲染成带格式的 JSX
- 代码块带语法高亮

### 为什么抽成独立组件

这是这次的一个设计决策。为什么不直接把渲染逻辑写进 `ChatBox.tsx`？

原因：

1. **职责单一**：`ChatBox` 已经够长，它的职责是"状态 + 交互 + 请求"，不该再背"渲染细节"。
2. **复用**：未来你做 RAG 回答展示、system 预览、文档预览，都能直接用这个组件。
3. **符合项目风格**：项目里 `Counter`、`ChatBox` 都是独立组件，新增能力也抽成组件，保持一致。

### 它的核心结构

```tsx
<ReactMarkdown remarkPlugins={[remarkGfm]} components={{ code: ... }}>
  {content}
</ReactMarkdown>
```

三个关键配置，下面逐个讲。

---

### 关键配置 1：`remark-gfm` 是干什么的

```tsx
remarkPlugins={[remarkGfm]}
```

`react-markdown` 默认只支持**基础 Markdown**（标题、列表、加粗、链接、代码块）。

但 GitHub 在基础 Markdown 上扩展了一版，叫 **GFM（GitHub Flavored Markdown）**，多了：

- **表格**（`| 列 | 列 |`）
- **删除线**（`~~文字~~`）
- **任务列表**（`- [x]`）

AI 回复里经常出现表格，所以必须加 `remark-gfm`，否则表格会渲染失败。

---

### 关键配置 2：自定义 `code` 组件

这是整个组件里**最复杂也最关键**的一段。

```tsx
components={{
  code(props) {
    const { children, className, ...rest } = props
    const match = /language-(\w+)/.exec(className || "")
    const lang = match ? match[1] : ""

    if (!lang) {
      return <code className={className} {...rest}>{children}</code>
    }

    return (
      <SyntaxHighlighter style={oneDark} language={lang} PreTag="div">
        {String(children).replace(/\n$/, "")}
      </SyntaxHighlighter>
    )
  }
}}
```

#### 它在解决什么问题

Markdown 里代码有两种：

1. **行内代码**：用单个反引号，比如 `` `count` ``，应该显示成 `count`（灰色背景小字）。
2. **代码块**：用三个反引号，比如
   ` ```js\nconsole.log(1)\n``` `，应该显示成带高亮的多行代码块。

这两种都对应 HTML 的 `<code>` 标签，但显示方式完全不同。所以必须拦截 `code`，自己判断该用哪种渲染。

#### 怎么区分行内代码和代码块

看 `className`：

- 行内代码的 `className` 是空（或不含 `language-`）
- 代码块的 `className` 形如 `language-js`、`language-python`

因为代码块写的时候会标语言（` ```js `），react-markdown 会把这个语言提取到 `className` 里。

所以判断逻辑是：

```tsx
const match = /language-(\w+)/.exec(className || "")
const lang = match ? match[1] : ""
if (!lang) { /* 行内代码 */ }
else { /* 代码块，lang 是 js/python/... */ }
```

#### 为什么用 `SyntaxHighlighter`

`SyntaxHighlighter` 是一个专门的代码高亮组件：

- 传给它语言名和代码文本
- 它按语言的语法规则给关键字上色
- `style={oneDark}` 是配色主题（深色）

---

### 关键配置 3：按需注册语言

```tsx
import { PrismLight as SyntaxHighlighter } from "react-syntax-highlighter"
import javascript from "react-syntax-highlighter/dist/esm/languages/prism/javascript"
// ...

SyntaxHighlighter.registerLanguage("javascript", javascript)
SyntaxHighlighter.registerLanguage("typescript", typescript)
// ...
```

#### 为什么要"按需"

`react-syntax-highlighter` 支持几百种语言。如果你全量引入，所有语言的语法文件都会打进 bundle，体积从几十 KB 暴增到几 MB，首屏加载会很慢。

`PrismLight` 是这个库的"按需加载版本"：

- 默认**不注册任何语言**
- 你只 `registerLanguage` 你需要的几种
- 只有注册过的语言才高亮
- **没注册的语言自动降级为纯文本，不会报错**

这个项目注册了前端常用的 8 种：javascript、typescript、jsx、tsx、json、bash、python、css。

#### 这个设计为什么重要

这是真实项目必须考虑的事。学习项目可以全量打包求简单，但简历项目要考虑性能。按需加载是生产项目的标准做法。

---

### 这个组件要不要写 `'use client'`

要。

```tsx
"use client"
```

因为 `SyntaxHighlighter` 用了浏览器 API，必须在客户端运行。`react-markdown` 本身可以在服务端跑，但配合 `SyntaxHighlighter` 后整体要声明为客户端组件。

---

## `components/ChatBox.tsx`

### 它这次改了什么

只动了**消息渲染那一小段**。

**旧（注释保留了）：**

```tsx
<ul>
  {messages.map((message, index) => (
    <li key={index}>
      <strong>{message.role === "user" ? "你" : "机器人"}：</strong>
      {message.content}
    </li>
  ))}
</ul>
```

**新：**

```tsx
<ul>
  {messages.map((message, index) => (
    <li key={index} className={message.role === "user" ? "msg-user" : "msg-assistant"}>
      <strong>{message.role === "user" ? "你" : "机器人"}：</strong>
      {message.role === "assistant" ? (
        <Markdown content={message.content} />
      ) : (
        message.content
      )}
    </li>
  ))}
</ul>
```

顶部加了一行 `import Markdown from "./Markdown"`。

system 状态、请求逻辑、流式读取逻辑**完全没动**。

### 为什么只渲染 assistant，user 仍纯文本

这是个设计决策。原因：

1. **user 输入习惯**：用户随手打的字一般不含 Markdown。如果渲染，用户输入的 `#` `*` 会被误解析。
2. **assistant 输出结构化**：AI 回复几乎必含代码块、列表、表格，必须渲染。
3. **符合真实产品**：ChatGPT、Claude 这些产品都只渲染 AI 回复，用户消息是纯文本。

所以代码里用三元判断：

```tsx
{message.role === "assistant" ? (
  <Markdown content={message.content} />
) : (
  message.content
)}
```

### 它属于前端还是服务端

前端。改的全是渲染相关的 JSX。

---

## `app/globals.css`

### 它这次改了什么

纯**追加**，没删任何旧样式。追加了两部分：

#### 1. 消息气泡区分

```css
.msg-user { background: #eef2ff; }
.msg-assistant { background: #f6f8fa; }
```

用户消息和 AI 消息有不同背景色，一眼能区分。

#### 2. Markdown 排版

```css
.markdown-body h1 { font-size: 1.4em; }
.markdown-body ul { padding-left: 22px; }
.markdown-body table { border-collapse: collapse; }
...
```

#### 为什么 Markdown 需要单独写样式

因为 `react-markdown` 只负责"把 Markdown 转成 HTML 标签"（比如把 `## 标题` 转成 `<h2>标题</h2>`）。

但**标签长什么样**（字号、间距、颜色）它不管，得靠 CSS。

如果不写，标题不会变大、列表不会缩进、表格没边框——看起来还是乱的。

所以 Markdown 渲染 = **库负责转换结构 + CSS 负责外观**，缺一不可。

---

## 为什么后端一行没动

这是这次最明显的一个特点。

因为 Markdown 是**纯前端展示问题**：

- AI 返回的就是一段 Markdown 文本字符串
- 字符串本身不变，变的是"怎么显示"
- 显示完全是浏览器的事，和后端、模型、流式传输都无关

这又一次印证了分层的好处：**展示层独立，改它不影响数据层和传输层。**

---

## 当前完整链路是什么

在阶段 7 基础上，唯一变化的是**最后一步的显示**：

```txt
浏览器：用户填写 system + 输入消息
  │
  ▼
ChatBox.tsx 的 handleSend()
  ▼
fetch('/api/chat', { body: { messages, system } })
  │
  ▼
route.ts：拼 system 到最前面，转发给上游
  ▼
硅基流动返回 SSE 流式片段
  ▼
route.ts 解析 delta.content，转纯文本流给前端   ← 和阶段 7 完全一样
  ▼
ChatBox.tsx 用 getReader() 逐块读取，追加到最后一条 assistant 消息   ← 和阶段 7 完全一样
  │
  ▼
★ 渲染：assistant 消息经过 <Markdown content={...} /> 渲染成带格式内容   ← 这次新增
  │
  ▼
页面显示带格式的回复
```

变化只在"读完后怎么显示"这一步。

---

## 这次升级后，你掌握了什么

### 1. Markdown 渲染的最小实现

你会知道：

- `react-markdown` 负责把 Markdown 文本转成 HTML 结构
- `remark-gfm` 扩展支持表格、删除线、任务列表
- `react-syntax-highlighter` 负责代码高亮

### 2. 行内代码 vs 代码块的区分

你会理解：

- 都对应 `<code>` 标签
- 靠 `className` 里的 `language-xxx` 区分
- 拦截 `code` 组件自己分流

### 3. 按需加载语言

你会接触到：

- `PrismLight` vs 全量版本的差异
- `registerLanguage` 只注册需要的语言
- bundle 体积意识

### 4. 组件抽取

你会建立工程意识：

- 渲染逻辑独立成组件
- 不堆进主组件
- 便于复用

### 5. XSS 安全意识

`react-markdown` 默认**不执行 HTML 标签**——如果 AI 回复里混了 `<script>`，它会被当文本显示，不会执行。这是为什么不用 `dangerouslySetInnerHTML` 的原因。这点在文档末尾的自测题里会再提。

### 6. 展示层独立

你会又一次体会到：

- 显示问题和数据/传输问题分层
- 改显示不影响后端和流式逻辑

---

## 你应该怎么运行和验证

### 1. 确认依赖装好

```bash
npm install
npm run dev
```

### 2. 打开聊天页

`http://localhost:3000/chat`

### 3. 让 AI 输出一段"含多种格式"的内容

发一条能触发多种 Markdown 格式的消息，比如：

```txt
请用 Markdown 介绍 JavaScript 的闭包，要求：
1. 有二级标题
2. 有加粗
3. 有一个代码块（用 js 标注语言）
4. 有一个有序列表
5. 有一个表格
```

### 4. 观察渲染效果

应该看到：

- `##` 变成正常大小的标题
- `**文字**` 变成加粗
- ` ```js ... ``` ` 变成**带颜色高亮**的代码块
- `1. 2. 3.` 变成真正的有序列表
- `| 列 | 列 |` 变成带边框的表格

### 5. 对比"未渲染"

如果你把 `ChatBox` 里那段新代码临时改回注释里的旧代码（`{message.content}`），同样的 AI 回复会变成一坨带 `#` `*` 的纯文本——对比之下渲染的价值非常明显。

---

## 学完这一版后，你应该能回答什么问题

1. 为什么 AI 回复需要 Markdown 渲染？不渲染会怎样？
2. `remark-gfm` 解决了什么问题？不加它会缺什么？
3. 怎么区分行内代码和代码块？依据是什么？
4. 为什么要用 `PrismLight` 而不是全量版本？按需加载是怎么实现的？
5. 为什么只渲染 assistant 消息，不渲染 user 消息？
6. 为什么这次后端、流式逻辑一行都没动？
7. 为什么用 `react-markdown` 而不是 `dangerouslySetInnerHTML`？这和安全有什么关系？

如果这些问题你都能讲清楚，说明你已经把"Markdown 渲染"这一层打通了。

---

## 下一步最适合继续学什么

在 Markdown 渲染之后，"交互打磨"还剩几项，按建议顺序：

1. **代码块复制按钮**：在代码块右上角加一个"复制"按钮，用 `navigator.clipboard.writeText`。和 Markdown 渲染直接相关。
2. **回车发送**：在 input 上加 `onKeyDown`，按 Enter（不带 Shift）就触发发送。
3. **清空消息**：加一个按钮，`setMessages([])`。
4. **中止生成**：用 `AbortController`，让用户在 AI 还没说完时能"停止"。
5. **错误处理**：给 `route.ts` 包 `try/catch`，前端处理中断/超时。

建议顺序：先做完交互细节，让产品顺滑；再补健壮性；最后进持久化（数据库）。

---

## 一个小提示

你会发现，从这一阶段开始，改动的重心从"AI 能力"转向了"前端体验"。

前 7 个阶段你解决的是"**让 AI 真的能用**"（接通模型、流式、多轮、人设）。
从这一阶段开始，你解决的是"**让产品真的好用**"（显示、交互、健壮性）。

这两个维度加起来，才是一个完整的全栈 AI 项目。这也是为什么我之前说，到这一步你的项目已经是"功能完整的 AI 聊天骨架"了——接下来是把骨架"装修"成产品。
