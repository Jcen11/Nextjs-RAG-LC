# Next.js 聊天页支持多轮上下文学习说明

这个文档基于你当前已经完成的 Next.js 流式聊天项目，继续把聊天从"单轮、无记忆"升级为"多轮、有记忆"。

这次的目标是：

- 前端把已有的消息历史整体发给后端
- 后端把消息历史整体转发给上游模型
- 让 AI 能记住前面几轮说过的话
- 前端流式读取逻辑完全不变
- 保持代码最小化、教学化

---

## 这次改了哪些文件

```txt
app/
  api/
    chat/
      route.ts

components/
  ChatBox.tsx

6.多轮上下文.md
```

这次没有改动这些文件：

- `app/chat/page.tsx`
- `.env.local`、`.env.local.example`

也就是说：

- 聊天页面壳没变
- 流式显示效果没变
- 模型供应商没变
- 环境变量没变

变化只发生在"前端发什么、后端转发什么"这一层。

---

## 这次升级的核心目标

之前的版本是：

- 前端只发当前这一条用户消息
- 后端只把这一条转发给模型
- 模型每次都"失忆"，不知道前面聊过什么

现在的版本是：

- 前端把完整消息历史发给后端
- 后端把完整消息历史转发给模型
- 模型能结合上下文回答

也就是说，你这次学的是：

**如何在不改流式逻辑、不改模型供应商的前提下，让聊天从"单轮"升级成"多轮"。**

---

## 推荐阅读顺序

建议按下面顺序阅读：

1. `components/ChatBox.tsx`
2. `app/api/chat/route.ts`
3. 这份文档

这样读的原因：

- 先看前端发请求时改了什么
- 再看后端收到后怎么转发
- 最后回头理解整条链路为什么几乎没变

---

## 每个相关文件应该学什么

## `components/ChatBox.tsx`

### 它这次改了什么

这是前端这次唯一改动的地方，而且只动了 `handleSend` 里**几行**。

它从：

- 请求体只发 `{ message: text }`（一个字符串）

改成了：

- 请求体发 `{ messages: messagesToSend }`（一个消息数组）

流式读取部分（`getReader()` 循环、逐段追加到最后一条 assistant 消息）**完全没动**。

### 它现在负责什么

它依然负责：

1. 输入框状态
2. 消息列表状态
3. 点击发送
4. 把**完整消息历史**发给 `/api/chat`
5. 流式读取返回，逐段更新最后一条 assistant 消息

### 它属于前端还是服务端

它属于前端。

### 它属于 React 还是 Next.js

#### React 部分

- `useState`
- 构造新数组 `[...messages, userMessage]`
- 输入框受控
- 点击事件
- 流式读取后更新最后一条消息

#### Next.js 部分

- `'use client'`
- 客户端组件运行方式

### 为什么要这样写

#### 1. 为什么要先构造 `messagesToSend`，再更新 UI

这是这次最关键的一个细节。

代码现在的顺序是：

```tsx
const userMessage: Message = { role: "user", content: text };

// 先构造要发给后端的历史
const messagesToSend = [...messages, userMessage];

// 再更新 UI：加 user 消息 + 空 assistant 占位
setMessages((prev) => [...prev, userMessage, assistantMessage]);
```

为什么必须**先构造 `messagesToSend`**？

因为 `messages` 是当前的旧状态，里面只包含"已经聊过的历史"，**还不包含这条新 user 消息，也不包含空 assistant 占位**。

如果你**先**调 `setMessages` 把空 assistant 占位加进去，再去构造要发送的内容，那发送的数组里就会**多出一条空 assistant 消息**：

```json
[
  { "role": "user", "content": "你好" },
  { "role": "assistant", "content": "你好呀" },
  { "role": "user", "content": "我叫小明" },
  { "role": "assistant", "content": "" }
]
```

最后那条空的 assistant 消息会被发给模型，模型会困惑："我还没说话，为什么历史里有一条我说的空话？"

所以正确顺序是：

1. **先**用旧的 `messages` 构造历史数组
2. **再**更新 UI

这是 React 状态异步更新的特性带来的细节，也是这次最容易踩坑的地方。

#### 2. 为什么请求体改成 `messages` 数组

因为后端这次约定接收 `messages`（数组），不再是 `message`（单字符串）。

前端把"历史 + 这条新消息"一次性发过去，后端就不用自己维护状态，模型也就能看到完整上下文。

### Vue 开发者最容易混淆的点

#### 1. 状态是"异步"的

很多人会想这样写：

```tsx
setMessages((prev) => [...prev, userMessage])
const response = await fetch("/api/chat", {
  body: JSON.stringify({ messages: messages }), // ❌ 错
})
```

这是错的，因为 `messages` 在这一行还是旧值，`setMessages` 不会立即更新它。

所以你看到这次代码里是**用一个新局部变量** `messagesToSend` 来传递：

```tsx
const messagesToSend = [...messages, userMessage]
```

这样就不依赖状态何时更新，干净可靠。

#### 2. Vue 的响应式和这里不同

Vue 里 `ref` 改了立刻能拿到新值。React 的 `useState` setter 不会立刻改原变量，必须等下一次渲染。这是两套心智模型最大的区别之一。

---

## `app/api/chat/route.ts`

### 它这次改了什么

这是后端这次唯一改动的地方，而且也只动了**开头几行 + 请求体一行**。

它从：

- 读取 `body.message`（单字符串）
- 上游请求体里硬编码单条消息数组

改成了：

- 读取 `body.messages`（数组）
- 上游请求体里直接转发收到的 `messages` 数组

流式 SSE 解析、`ReadableStream` 转发逻辑**完全没动**。

### 它现在负责什么

它依然负责：

1. 接收前端发来的请求
2. 读取 `messages` 数组
3. 校验是否是有效数组
4. 读取服务端环境变量
5. 把 `messages` 原样转发给上游模型
6. 流式读取上游返回，解析成纯文本流给前端

### 它属于前端还是服务端

它属于服务端。

### 它属于 React 还是 Next.js

这部分不属于 React，主要属于：

- Next.js API Route
- 服务端 HTTP 调用

### 为什么要这样写

#### 1. 为什么后端可以直接原样转发 `messages`

这是这次最值得理解的一点。

因为我们前端的 `Message` 类型是：

```ts
type Message = {
  role: "user" | "assistant"
  content: string
}
```

而 OpenAI 兼容接口要求的 `messages` 数组，每一项**正好就是这个结构**：

```json
[
  { "role": "user", "content": "..." },
  { "role": "assistant", "content": "..." }
]
```

所以**根本不需要任何格式转换**。后端拿到什么，就转发什么。

这就是为什么这一步改动这么小——因为数据结构本来就是天然对齐的。

#### 2. 为什么要加校验

```ts
if (!Array.isArray(messages) || messages.length === 0) {
  return Response.json(
    { reply: "没有收到有效的消息列表。" },
    { status: 400 }
  )
}
```

因为后端不再读"单字符串"，而是读"数组"。如果前端发了空数组、或者字段名写错（比如还发 `message`），后端不应该带着错误数据继续请求上游，而应该立刻返回明确错误。

这是一种非常小但很有用的保护：

- 不复杂
- 但能让你立刻定位问题在哪

#### 3. 为什么后端不自己维护会话状态

这是一个重要的架构选择。

后端**故意做成无状态**：

- 后端不保存任何会话历史
- 历史完全由前端持有并发送
- 后端只做"接收、校验、转发、流式回传"

这样的好处是：

- 后端简单
- 后端可以水平扩展（多实例不用同步状态）
- 未来加持久化时，也只是"前端从数据库读历史再发"，后端逻辑不用大改

### Vue 开发者最容易混淆的点

很多人会下意识以为"多轮上下文 = 后端要存数据"。

但在这个最小版本里**不是这样**。状态在前端，后端每次都是"一次性收到完整历史"。这是一种无状态设计，等以后接数据库时才会真正在后端持久化。

---

## 关键概念：OpenAI 兼容的 `messages` 数组是什么

这是这次必须建立的一个核心概念。

### 数组里每一项长什么样

```json
{
  "role": "user",
  "content": "用户说的话"
}
```

`role` 常见取值：

| role | 含义 |
| --- | --- |
| `user` | 用户说的话 |
| `assistant` | AI 之前回复过的话 |
| `system` | 系统提示词，定义 AI 的人设和规则（这一版**还不用**） |

### 为什么这个结构天然支持多轮

因为整个对话就是"一个数组"。

- 单轮 = 数组里只有一条 user 消息
- 多轮 = 数组里有按时间顺序排列的 user / assistant 交替

模型看到这个数组，就能"读懂"整段对话的来龙去脉。

### 单轮和多轮的请求体对比

**单轮（你之前的写法）：**

```json
{
  "model": "Qwen/Qwen3-8B",
  "messages": [
    { "role": "user", "content": "你好" }
  ],
  "stream": true
}
```

**多轮（现在的写法）：**

```json
{
  "model": "Qwen/Qwen3-8B",
  "messages": [
    { "role": "user", "content": "我叫小明" },
    { "role": "assistant", "content": "你好，小明！" },
    { "role": "user", "content": "我刚才说我叫什么？" }
  ],
  "stream": true
}
```

注意：

- 只是 `messages` 数组变长了
- 结构本身没变
- 这就是为什么前后端改动都那么小

---

## 当前完整链路是什么

在阶段 5 流式链路的基础上，只多了一步"带上历史"：

```txt
浏览器输入消息
  │
  ▼
ChatBox.tsx 的 handleSend()
  │  先构造 messagesToSend = 旧 messages + 新 user 消息
  │  再更新 UI：加 user 消息 + 空 assistant 占位
  ▼
fetch('/api/chat', { body: { messages: messagesToSend } })   ← 这次带上完整历史
  │
  ▼
Next.js 服务端 route.ts
  │  读取 messages 数组并校验
  │  读取 SILICONFLOW_API_KEY / OPENAI_BASE_URL / OPENAI_MODEL
  ▼
fetch('${baseURL}/chat/completions', { body: { model, messages, stream: true } })  ← 整个历史转发给上游
  │
  ▼
硅基流动根据完整上下文生成回复，持续返回 SSE 片段
  │
  ▼
route.ts 解析 delta.content，转纯文本流给前端   ← 和阶段 5 完全一样
  │
  ▼
ChatBox.tsx 用 getReader() 逐块读取，追加到最后一条 assistant 消息   ← 和阶段 5 完全一样
  │
  ▼
页面逐步显示完整回复
```

变化只发生在"发送请求"那一层。流式处理那一层一行没动。

---

## 为什么这次改动这么小

这是这次最值得你建立的一个工程直觉。

因为前面几个阶段的设计已经把"基础"打好了：

1. **数据结构对齐**：前端的 `Message` 类型和 OpenAI 兼容要求的格式天然一致，所以无需转换。
2. **接口层隔离**：前端只对接 `/api/chat`，后端只对接上游，每层职责清晰，改一层不影响另一层。
3. **流式逻辑独立**：流式的 SSE 解析、`ReadableStream` 转发是独立的一段，和"发什么内容"无关，所以多轮上下文不需要碰它。

这就是"小步演化 + 分层隔离"带来的回报：**每加一个能力，改动都集中在该出现的地方。**

---

## 这次升级后，你掌握了什么

### 1. 多轮上下文是怎么实现的

你会知道：

- 多轮本质就是把消息历史整体发给模型
- 不是什么神秘机制，就是一个数组

### 2. `messages` 数组的语义

你会理解：

- 数组里每一项的 `role` 表示"谁说的"
- 按时间顺序排列就构成完整对话
- 模型靠读这个数组来"记忆"

### 3. 为什么前后端改动这么小

你会建立工程直觉：

- 数据结构对齐 → 不用转换
- 职责分层 → 改动不扩散
- 这就是好的架构带来的好处

### 4. React 状态异步更新的一个实战坑

你会真正遇到并理解：

- 为什么不能在 `setMessages` 之后立刻用 `messages`
- 为什么要用一个局部变量 `messagesToSend`

### 5. 无状态后端的设计思路

你会接触到：

- 后端不存会话，状态全在前端
- 这种设计简单、易扩展
- 持久化是未来再做的事

---

## 你应该怎么运行和验证

### 1. 确认 `.env.local`

和阶段 5 完全一样，无需改动：

```env
SILICONFLOW_API_KEY=你的硅基流动 key
OPENAI_BASE_URL=https://api.siliconflow.cn/v1
OPENAI_MODEL=你的真实模型名
```

### 2. 启动开发服务

```bash
npm run dev
```

### 3. 打开聊天页

访问：

- `http://localhost:3000/chat`

### 4. 用"相互关联的两轮"来验证记忆

这是关键。要证明 AI 有记忆，两轮问题必须是**相互关联**的。

推荐的测试话术：

**第 1 轮：**

```txt
我叫小明，今年 20 岁。
```

**第 2 轮：**

```txt
我刚才说我叫什么？多大了？
```

### 5. 观察现象

如果多轮上下文生效：

- 第 2 轮 AI 应该能回答出"小明"和"20 岁"
- 因为它看到了完整历史，而不是只看到第 2 轮这一条

如果**没生效**（比如忘了改后端，还在发单条），AI 会回答"我不知道你叫什么"。

---

## 学完这一版后，你应该能回答什么问题

1. 多轮上下文的本质是什么？为什么"只是把数组传过去"就够了？
2. 为什么前端必须**先**构造 `messagesToSend`，再调 `setMessages`？顺序反了会怎样？
3. 为什么后端可以直接原样转发 `messages`，不需要格式转换？
4. `messages` 数组里 `role` 有哪些取值？分别表示什么？
5. 为什么这次前后端的改动都这么小？背后是什么设计在支撑？
6. 为什么这次后端仍然是无状态的？这样做有什么好处？
7. 如果以后要做"会话持久化"，会改哪一层？前端还是后端的流式逻辑？

如果这些问题你都能讲清楚，说明你已经把"多轮上下文"这一层打通了。

---

## 下一步最适合继续学什么

在这一版之后，最适合继续扩展的是（按建议顺序）：

1. **系统提示词 system message**：在 `messages` 数组开头加一条 `{ role: "system", content: "你是一个友好的助手" }`，定义 AI 人设。
2. **交互细节**：回车发送、清空消息、用户/机器人消息样式区分。
3. **错误处理**：给 `route.ts` 包 `try/catch`，前端处理中断/超时。
4. **Markdown 渲染**：AI 回复通常是 Markdown，引入 `react-markdown` 让代码块、列表正常显示。
5. **中止生成**：用 `AbortController`，让用户能"停止"。
6. **上下文长度限制**：当历史太长时截断或总结，避免超出模型 token 上限。
7. **再往后**：会话持久化（数据库）、鉴权、RAG、工具调用（function calling）。

建议顺序仍然是：

- 先加 system message，把多轮这一层彻底用熟
- 再打磨交互和健壮性
- 再考虑持久化和更复杂能力

---

## 一个小的伏笔

你会发现，这一版我们让前端把"完整历史"每次都重发一遍。

这在学习阶段完全没问题。但在真实产品里你会遇到一个问题：

> 当对话很长时，历史会越来越长，最终超出模型的 token 上限，请求会失败。

这就是为什么"上下文长度限制 / 截断 / 总结"是后面必须学的一项。它不是可选优化，而是多轮上下文的必然延伸。

不过现在还不用管它，先把多轮本身用熟。
