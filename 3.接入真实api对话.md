# Next.js 聊天页接入真实大模型 API 学习说明

这个文档基于你当前已经完成的 Next.js 最小聊天页项目，继续把聊天接口从“模拟回复”升级为“调用真实大模型 API”。

目标仍然是保持代码最小化、教学化，让你先看懂最核心的一条链路：

- 前端输入消息
- 前端 `fetch` 请求
- Next.js 服务端接口接收请求
- 服务端调用真实 Claude API
- 服务端返回 JSON
- 前端更新消息列表

---

## 这次改了哪些文件

```txt
app/
  api/
    chat/
      route.ts

package.json
```

这次没有改动聊天页面结构：

- `components/ChatBox.tsx` 保持原来的输入框、发送按钮、消息列表逻辑
- 前端依然通过 `fetch('/api/chat')` 请求接口

---

## 这次升级的核心目标

之前的版本里：

- `app/api/chat/route.ts` 只是返回模拟字符串

现在的版本里：

- `app/api/chat/route.ts` 会真的调用 Claude API
- 返回值不再是假数据，而是模型生成的文本

也就是说，这次你学的是：

**如何在 Next.js 项目里，把前端聊天页面接到真实大模型接口上。**

---

## 推荐阅读顺序

建议按下面顺序阅读：

1. `components/ChatBox.tsx`
2. `app/api/chat/route.ts`
3. `package.json`

这样读的原因：

- 先看前端是怎么发请求的
- 再看服务端是怎么接请求并调用模型的
- 最后看依赖是怎么补上的

---

## 每个相关文件应该学什么

## `components/ChatBox.tsx`

### 它这次扮演什么角色

它还是前端聊天组件，负责：

- 输入框内容
- 消息列表
- 发送按钮
- `fetch('/api/chat')`
- 拿到接口返回后更新页面

这次它的结构没有变化，说明一个很重要的点：

**前端不需要知道你后端是“模拟回复”还是“真实模型回复”。**

只要接口返回的 JSON 结构不变，前端就可以继续工作。

### 它属于前端还是服务端

它属于前端。

因为它运行在浏览器里，负责：

- 用户输入
- 点击事件
- 本地状态更新
- 发起 HTTP 请求

### 它属于 React 还是 Next.js

#### React 部分

- `useState`
- 输入框受控
- 点击事件
- 消息数组渲染
- `loading` 状态

#### Next.js 部分

- `'use client'`
- 这个组件在 App Router 中作为客户端组件运行

### 你要重点理解什么

重点不是它“怎么变了”，而是：

**它几乎没变。**

这反而很重要，因为这说明：

- 前端和模型 SDK 没有直接耦合
- 前端只是请求自己的 `/api/chat`
- 真实模型调用被封装在服务端

### Vue 开发者容易混淆的点

很多人第一次接大模型时，会误以为：

- 前端页面里直接写模型 SDK
- 浏览器直接带 API Key 调外部模型

这是不对的。

正确做法是：

- 前端只请求自己后端
- 真正的 API Key 和模型调用在服务端完成

---

## `app/api/chat/route.ts`

### 它现在是干什么的

它依然是 `/api/chat` 接口。

但和之前不同的是：

- 以前：返回模拟字符串
- 现在：调用真实 Claude API，再把结果返回给前端

它现在负责：

1. 接收前端发来的消息
2. 读取请求体里的 `message`
3. 创建 Anthropic SDK 客户端
4. 调用 `client.messages.create(...)`
5. 从模型响应里取出文本
6. 返回 `{ reply: '...' }`

### 它属于前端还是服务端

它属于服务端。

因为：

- `route.ts` 是 Next.js 的服务端接口文件
- 它运行在服务器环境
- 它可以读取 `process.env.ANTHROPIC_API_KEY`
- 它不应该暴露 API Key 给浏览器

### 它属于 React 还是 Next.js

这部分基本属于 Next.js 和服务端集成，不属于 React。

#### Next.js 部分

- `app/api/chat/route.ts` 这个文件约定
- `POST(request: Request)` 处理 POST 请求
- `Response.json(...)` 返回 JSON

#### Claude API / 服务端集成部分

- `import Anthropic from '@anthropic-ai/sdk'`
- `new Anthropic(...)`
- `client.messages.create(...)`

### 为什么要这样写

#### 1. 为什么密钥放在服务端

```ts
const client = new Anthropic({
  apiKey: process.env.ANTHROPIC_API_KEY
})
```

因为 API Key 是敏感信息，不能暴露到浏览器。

所以必须：

- 放在环境变量里
- 由服务端代码读取
- 由服务端调用真实模型

这是接大模型最基本的安全原则之一。

#### 2. 为什么继续保留 `/api/chat`

因为前端已经有一个清晰的请求入口：

```ts
fetch('/api/chat')
```

这样前端不用关心：

- 你用的是 Claude 还是别的模型
- 后端是否做了重试
- 后端是否做了上下文拼接

前端只认：

- 我发消息给 `/api/chat`
- 我拿到 `reply`

这就是良好的前后端边界。

#### 3. 为什么先做非流式

因为你当前阶段在学“最小闭环”。

非流式更适合理解：

- 请求发出去
- 服务端等完整结果
- 一次性返回
- 前端一次性显示

等你先把这条链路看懂，再升级成流式最合适。

#### 4. 为什么只传一条用户消息

现在这版是最小教学版：

```ts
messages: [
  {
    role: 'user',
    content: userMessage
  }
]
```

这样做的好处是简单、直观。

你现在先看懂：

- 用户输入怎么变成 Claude API 的 `messages`
- Claude API 返回的内容怎么再变成前端可用的 `reply`

多轮上下文是下一步再学的内容。

#### 5. 为什么从 `content` 里找 `text`

Claude API 返回的 `content` 是一个内容块数组，不保证只能直接 `message.content[0].text` 一把梭。

所以这里写成：

```ts
const text = message.content.find((block) => block.type === 'text')
```

这是更稳妥、也更贴近 SDK 返回结构的最小写法。

### Vue 开发者最容易混淆的点

#### 1. “接口文件”就在前端项目里

这和传统 Vue 项目常见的“前端一个仓库、后端一个仓库”不一样。

Next.js 允许你：

- 页面写在 `app/`
- 接口也写在 `app/api/`

#### 2. 服务端代码和前端代码能共存在一个项目，但运行环境不同

虽然文件都在同一个工程里，但：

- `components/ChatBox.tsx` 运行在浏览器
- `app/api/chat/route.ts` 运行在服务端

这是非常重要的边界意识。

#### 3. `process.env` 不等于“前端也能随便拿到”

在这里读取：

```ts
process.env.ANTHROPIC_API_KEY
```

是服务端行为，不是浏览器行为。

不要把这种写法直接搬进客户端组件里理解。

---

## `package.json`

### 它这次做了什么

新增依赖：

```json
"@anthropic-ai/sdk": "^0.57.0"
```

### 它属于前端还是服务端

它属于项目依赖配置，本身不是前端或服务端逻辑。

但这次新增的这个依赖，主要服务于：

- 服务端接口调用 Claude API

### 为什么要这样写

因为你这次要求：

- 使用真实大模型 API
- 保持最小化
- TypeScript 项目

最适合的做法就是直接使用 Anthropic 官方 SDK。

这样你能学到的是标准接入方式，而不是额外封装层。

---

## 这次升级后，完整链路是什么

现在整条请求链路是这样的：

```txt
浏览器输入消息
  -> ChatBox.tsx 里的 handleSend()
  -> fetch('/api/chat')
  -> Next.js 服务端 route.ts
  -> Claude API
  -> route.ts 返回 { reply }
  -> ChatBox.tsx 更新消息列表
```

这就是一个最小、真实、可扩展的聊天架构骨架。

---

## 哪些逻辑属于前端，哪些属于服务端

## 前端逻辑

文件：

- `components/ChatBox.tsx`

负责：

- 渲染输入框
- 渲染按钮
- 渲染消息列表
- 管理 `input`
- 管理 `messages`
- 管理 `loading`
- 调用 `fetch('/api/chat')`
- 接收后端返回的 JSON
- 更新界面

这些都是前端职责。

---

## 服务端逻辑

文件：

- `app/api/chat/route.ts`

负责：

- 接收前端请求
- 读取用户消息
- 读取环境变量里的 API Key
- 调用真实 Claude API
- 处理模型响应
- 把结果包装成 JSON 返回

这些都是服务端职责。

---

## 为什么不把 Claude API 直接放到前端

这是这次学习里非常关键的一点。

原因有两个：

### 1. 安全

如果你把 API Key 直接放到前端：

- 浏览器能看到
- 用户能看到
- 别人也能拿去调用

这显然不安全。

### 2. 架构清晰

前端只负责：

- UI
- 用户输入
- 显示结果

服务端负责：

- 调模型
- 管理密钥
- 未来做上下文拼接、限流、日志、鉴权

这就是正确的职责划分。

---

## 这个版本帮助你掌握了什么

在“最小聊天页”基础上，这一版会让你多掌握这些核心知识：

### 1. 真实模型接入位置

你会知道：

- 真正的大模型调用应该放在服务端
- 不应该直接放到客户端组件里

### 2. Next.js API Route 的真实用途

你会看到 `route.ts` 不只是教学接口，它可以作为：

- 前端和外部模型之间的中间层

### 3. 前后端边界

你会更清楚地区分：

- 前端：输入、状态、显示
- 服务端：密钥、模型调用、响应组装

### 4. 官方 SDK 的最小接入方式

你已经接触了：

- `@anthropic-ai/sdk`
- `new Anthropic(...)`
- `client.messages.create(...)`

### 5. 一个最小真实聊天系统的基础架构

虽然它还没有：

- 流式输出
- 多轮上下文
- 持久化
- RAG
- LangChain

但它已经具备了真实聊天系统最基础的骨架。

---

## 你应该怎么运行和验证

### 1. 安装依赖

```bash
npm install
```

### 2. 在根目录创建 `.env.local`

```env
ANTHROPIC_API_KEY=你的密钥
```

### 3. 启动项目

```bash
npm run dev
```

### 4. 打开聊天页

- `http://localhost:3000/chat`

### 5. 输入一句话测试

例如：

```txt
请简单介绍一下 Next.js
```

点击发送后：

- 前端请求 `/api/chat`
- 服务端调用 Claude API
- 前端显示真实模型返回结果

---

## 学完这一版后，你应该能回答什么问题

如果你已经理解了这个版本，你应该能回答这些问题：

1. 为什么前端 `fetch` 可以保持不变
2. 为什么真实模型调用要放在 `route.ts` 里
3. 为什么 API Key 不能放到前端
4. `client.messages.create(...)` 在这条链路中扮演什么角色
5. 哪些逻辑属于前端，哪些属于服务端
6. 为什么这一版先做非流式而不是流式
7. 为什么这个结构适合继续扩展成真实聊天产品

如果这些问题你都能说清楚，说明你已经真正理解了“Next.js 聊天页接入真实大模型 API”的第一层骨架。

---

## 下一步最适合继续学什么

基于这个版本，最自然的下一步是：

1. 给 `route.ts` 加 `try/catch`
2. 支持多轮消息上下文
3. 支持流式返回
4. 前端边收边显示
5. 再之后才考虑系统提示词、持久化、RAG

建议顺序仍然是：

- 先把最小真实链路彻底看懂
- 再升级成流式
- 再处理上下文和更复杂能力
