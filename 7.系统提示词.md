# Next.js 聊天页加入系统提示词学习说明

这个文档基于你当前已经完成的 Next.js 多轮上下文聊天项目，继续给它加上"系统提示词"（system message）。

这次的目标是：

- 引入第三种 role：`system`
- 让用户可以在页面上填写系统提示词
- 后端把 system 拼到 messages 最前面，再转发给上游
- 让 AI 按设定的人设/规则/风格回答
- 保持代码最小化、教学化

---

## 这次改了哪些文件

```txt
app/
  api/
    chat/
      route.ts

components/
  ChatBox.tsx

7.系统提示词.md
```

这次没有改动这些文件：

- `app/chat/page.tsx`
- `.env.local`、`.env.local.example`

也就是说：

- 聊天页面壳没变
- 流式显示效果没变
- 多轮上下文没变
- 模型供应商没变

变化只发生在"前端多收集一个 system、后端多拼一步"这一层。

---

## 这次升级的核心目标

之前的版本是：

- `messages` 数组里只有 `user` 和 `assistant`
- AI 没有人设，每次都是"默认状态"回答

现在的版本是：

- `messages` 数组最前面可以多一条 `system` 消息
- 用户能通过输入框设定 AI 的人设、规则、风格
- AI 会按这个设定来回答

也就是说，你这次学的是：

**如何让 AI 从"默认状态"变成"按你设定的角色回答"。**

---

## 推荐阅读顺序

建议按下面顺序阅读：

1. `components/ChatBox.tsx`
2. `app/api/chat/route.ts`
3. 这份文档

这样读的原因：

- 先看前端怎么收集 system
- 再看后端怎么拼接 system
- 最后回头理解整条链路

---

## 关键概念：什么是 system message

这是这次必须先建立的核心概念。

### 它是第三种 role

之前你已经有两种：

| role | 含义 |
| --- | --- |
| `user` | 用户说的话 |
| `assistant` | AI 之前回复过的话 |

这次新增第三种：

| role | 含义 |
| --- | --- |
| `system` | 系统提示词，定义 AI 的人设/规则/风格 |

### 它的位置

`system` 消息通常放在 `messages` 数组**最前面**，一般只放一条：

```json
[
  { "role": "system", "content": "你是一个海盗，说话总是带'啊哈'" },
  { "role": "user", "content": "你好" },
  { "role": "assistant", "content": "啊哈！你好啊，朋友！" }
]
```

### 它和 user/assistant 的本质区别

这是最容易混淆的一点。

- `user` / `assistant` 是**对话回合**，一问一答。
- `system` **不是对话回合**，它是**指令**。

你可以把它理解成：

- `system` 是"开拍前给演员的剧本说明"
- `user` / `assistant` 是"实际演出的对话"

AI 会把 `system` 当作"贯穿整场对话的规则"来遵守，而不是当成一句普通的话来回应。

### 它在多轮中持续生效

只要每次请求都带上同一条 system，它就会在每一轮都起作用。

这就是为什么这一步建立在"多轮上下文"之上——没有多轮，system 的价值体现不出来。

---

## 每个相关文件应该学什么

## `components/ChatBox.tsx`

### 它这次改了什么

前端这次有 4 处小改动：

1. `Message` 类型的 `role` 加了 `"system"`
2. 新增一个 `systemPrompt` 状态
3. 请求体多带一个 `system` 字段
4. JSX 多了一个 textarea 输入框

流式读取逻辑**完全没动**。

### 它现在负责什么

它依然负责：

1. 输入框状态
2. 消息列表状态
3. **系统提示词状态**（新增）
4. 点击发送
5. 把 `messages` + `system` 发给 `/api/chat`
6. 流式读取返回，逐段更新最后一条 assistant 消息

### 它属于前端还是服务端

它属于前端。

### 它属于 React 还是 Next.js

#### React 部分

- `useState`（包括新增的 `systemPrompt`）
- textarea 受控
- 构造请求体
- 流式读取

#### Next.js 部分

- `'use client'`
- 客户端组件运行方式

### 为什么要这样写

#### 1. 为什么 system 不放进 `messagesToSend`，而是单独发一个字段

这是这次前端最关键的设计决策。

很多人会想直接这样写：

```tsx
// ❌ 不推荐：把 system 混进 messages 数组
const messagesToSend = [
  { role: "system", content: systemPrompt },
  ...messages,
  userMessage,
]
```

但这次代码没这么做，而是：

```tsx
// ✅ 推荐：system 单独作为字段发送
body: JSON.stringify({
  messages: messagesToSend,  // 只装对话历史
  system: systemPrompt.trim() || undefined,  // 单独发
})
```

为什么？

因为 `messages` 数组在前端代表的是**实际对话历史**（user/assistant 的一问一答）。如果把 system 混进去，会有两个问题：

1. **职责不清**：system 是"指令"不是"对话"，混在一起会让数组语义变模糊。
2. **未来持久化麻烦**：将来要把对话存进数据库时，存的是"实际聊过的内容"。system 是每次请求时动态叠加的设定，不该和对话历史混在一起存。

所以前端保持 `messages` 纯净，system 单独发，由后端负责拼接。

#### 2. 为什么 `systemPrompt.trim() || undefined`

```tsx
system: systemPrompt.trim() || undefined
```

当用户没填 system 时，`systemPrompt` 是空字符串。空字符串 `trim()` 后还是空字符串，是 falsy，所以会变成 `undefined`。

`undefined` 在 `JSON.stringify` 时会被忽略，这个字段就不会出现在请求体里。后端收到的 `body.system` 就是 `undefined`，不会触发拼接逻辑。

这是一个很小的技巧，但能让"无 system"和"有 system"两种情况都自然走通，不需要额外判断。

### Vue 开发者最容易混淆的点

#### textarea 和 input 的受控方式一样

```tsx
<textarea
  value={systemPrompt}
  onChange={(event) => setSystemPrompt(event.target.value)}
/>
```

和之前的 `<input>` 写法**完全一样**——都是 `value` + `onChange`。

Vue 里 textarea 是用 `v-model` 直接绑在标签上：

```vue
<textarea v-model="systemPrompt" />
```

React 里 textarea 和 input 用的是同一套受控写法，这点和 Vue 不同。

---

## `app/api/chat/route.ts`

### 它这次改了什么

后端这次有 3 处小改动：

1. 多读一个 `system` 字段
2. 如果有 system，拼到 `messages` 最前面
3. 上游请求体用拼接后的 `messagesWithSystem`

下游流式 SSE 解析、`ReadableStream` 转发**完全没动**。

### 它现在负责什么

它依然负责：

1. 接收前端发来的请求
2. 读取 `messages` 数组
3. **读取 `system` 字段（新增）**
4. **把 system 拼到 messages 最前面（新增）**
5. 读取服务端环境变量
6. 转发给上游模型
7. 流式读取上游返回，解析成纯文本流给前端

### 它属于前端还是服务端

它属于服务端。

### 为什么要这样写

#### 1. 为什么由后端拼接 system，而不是前端拼好

这是这次最值得理解的一个架构决策。

回顾一下：前端把 `system` 作为独立字段发送，后端负责拼到 messages 最前面。为什么不让前端直接拼好？

原因有三个：

##### （1）职责清晰

- **前端**负责"收集用户输入"——它收集了对话历史和系统提示词。
- **后端**负责"组装符合上游协议的请求"——它知道 OpenAI 兼容接口要求 system 放在 messages 最前面、是 `{ role: "system", content }` 这种结构。

system 是**协议层的概念**，由后端组装更合理。前端不应该关心"上游要求 system 长什么样、放哪里"。

##### （2）前端 messages 数组保持纯净

前面说过，前端的 `messages` 只装对话历史。这样未来做持久化时，存进数据库的就是干净的对话记录，system 是每次请求时由后端动态叠加的。

如果让前端拼好 system 再发，那 messages 数组里就混入了非对话内容，持久化时还得再拆出来，多此一举。

##### （3）协议适配层的延伸

还记得阶段 4 学的"后端是协议适配层"吗？这里就是那个概念的延伸：

- 前端发的字段（`messages` + `system`）和上游要求的 `messages` 数组**可以不一致**。
- 后端做了一层"组装"：把两个独立字段拼成上游要的一个数组。

这就是协议适配层的价值——**前端不用了解上游协议的细节，后端负责把前端的数据"翻译"成上游要的格式**。

#### 2. 拼接逻辑为什么这样写

```ts
const messagesWithSystem =
  typeof system === "string" && system.trim()
    ? [{ role: "system", content: system }, ...messages]
    : messages
```

逐行理解：

- `typeof system === "string"`：确保 system 是字符串（防止前端没发这个字段时是 `undefined`）。
- `system.trim()`：去掉首尾空格后还有内容，才算有效。
- 两个条件都满足，才在前面拼一条 system 消息。
- 否则直接用原 `messages`，相当于"没有 system"。

这样写能同时处理"有 system"和"无 system"两种情况，不需要写 if/else 两套逻辑。

### Vue 开发者最容易混淆的点

很多人会以为"system 是前端的概念，应该前端处理"。

但实际上，**system 是 OpenAI 兼容协议里的概念**，前端只是"收集用户想设的提示词文本"，至于这个文本怎么变成符合协议的 `{ role: "system", content }` 消息、放哪里，是后端的职责。

这和之前学的"密钥只在服务端"是同一种思路——**协议相关的细节，收敛在后端**。

---

## 当前完整链路是什么

在阶段 6 多轮上下文的基础上，只多了一步"后端拼 system"：

```txt
浏览器：用户填写 system + 输入消息
  │
  ▼
ChatBox.tsx 的 handleSend()
  │  构造 messagesToSend = 旧 messages + 新 user 消息
  │  更新 UI：加 user 消息 + 空 assistant 占位
  ▼
fetch('/api/chat', {
  body: { messages: messagesToSend, system: systemPrompt }   ← system 单独发
})
  │
  ▼
Next.js 服务端 route.ts
  │  读取 messages + system
  │  ★ 把 system 拼到 messages 最前面：[{role:'system',...}, ...messages]
  │  读取环境变量
  ▼
fetch('${baseURL}/chat/completions', {
  body: { model, messages: messagesWithSystem, stream: true }   ← 拼好后的数组
})
  │
  ▼
硅基流动按 system 设定的人设生成回复，持续返回 SSE 片段
  │
  ▼
route.ts 解析 delta.content，转纯文本流给前端   ← 和阶段 6 完全一样
  │
  ▼
ChatBox.tsx 用 getReader() 逐块读取   ← 和阶段 6 完全一样
  │
  ▼
页面逐步显示按人设生成的回复
```

变化只发生在"后端拼 system"这一步。流式处理、前端读取逻辑一行没动。

---

## 为什么这次改动也这么小

和阶段 6 一样，这次改动小不是偶然，而是前面的设计在支撑：

1. **messages 数组结构天然支持 system**：就是再加一种 role，结构没变。
2. **后端是协议适配层**：system 的拼接发生在后端，不影响前端，也不影响流式逻辑。
3. **流式逻辑独立**：SSE 解析、ReadableStream 转发和"messages 里有没有 system"无关，所以根本不用碰它。

这又一次印证了那个结论：**好的分层设计，让每个新能力的改动都集中在该出现的地方。**

---

## 这次升级后，你掌握了什么

### 1. system message 是什么

你会知道：

- 它是第三种 role
- 它定义 AI 的人设/规则/风格
- 它放在 messages 最前面
- 它是"指令"不是"对话回合"

### 2. 三种 role 的分工

你会更清楚地理解：

- `system`：定规则（开拍前的剧本说明）
- `user`：提问（实际对话）
- `assistant`：回答（实际对话）

### 3. 后端组装请求的工程意识

你会接触到：

- 前端发的字段和上游要求的格式可以不一致
- 后端负责把前端的字段"翻译/组装"成上游要的格式
- 这是"协议适配层"概念的延伸

### 4. 前端数据保持纯净的设计思路

你会理解：

- 前端 messages 只装对话历史
- system 单独发，不混进数组
- 这样未来持久化时存的就是干净数据

---

## 你应该怎么运行和验证

### 1. 确认 `.env.local`

和之前完全一样，无需改动。

### 2. 启动开发服务

```bash
npm run dev
```

### 3. 打开聊天页

访问 `http://localhost:3000/chat`

你会看到页面上方多了一个"系统提示词"输入框。

### 4. 对比验证：不填 system vs 填 system

这是这次验证的关键——**对比**。

#### 情况 A：不填 system

系统提示词框留空，直接问：

```txt
你好
```

AI 会用默认方式回答，比如："你好！有什么可以帮助你的吗？"

#### 情况 B：填一个有人设的 system

在系统提示词框填：

```txt
你是一个海盗，说话总是带"啊哈"，语气粗犷
```

然后问同样的问题：

```txt
你好
```

AI 应该会按海盗人设回答，比如："啊哈！你好啊，朋友！"

#### 情况 C：验证 system 在多轮中持续生效

保持上面的 system 不变，继续问：

```txt
给我讲个笑话
```

AI 讲的笑话也应该是海盗风格。说明 system 在每一轮都起作用，不只是第一轮。

### 5. 清空 system 测试

把系统提示词框清空，再问一个问题，AI 应该回到默认风格。

---

## 学完这一版后，你应该能回答什么问题

1. system message 和 user/assistant 消息的本质区别是什么？
2. system 消息通常放在 messages 数组的什么位置？为什么？
3. 为什么前端把 system 作为独立字段发送，而不是直接拼进 messages 数组？
4. 为什么由后端拼接 system，而不是前端拼好？这个设计带来了什么好处？
5. `systemPrompt.trim() || undefined` 这行代码解决了什么问题？
6. 为什么这次流式逻辑一行都没动？
7. 如果未来要做"会话持久化"，system 应该怎么处理？存进数据库还是每次动态叠加？

如果这些问题你都能讲清楚，说明你已经把"系统提示词"这一层打通了。

---

## 下一步最适合继续学什么

在 system message 之后，项目在"AI 能力"这条线上已经比较完整了（多轮 + 流式 + 人设）。接下来建议**打磨交互和健壮性**，按这个顺序：

1. **交互细节**：回车发送、清空消息、用户/机器人消息样式区分、system 输入框可折叠。
2. **错误处理**：给 `route.ts` 包 `try/catch`，前端处理中断/超时。
3. **Markdown 渲染**：AI 回复通常是 Markdown，引入 `react-markdown` 让代码块、列表正常显示。
4. **中止生成**：用 `AbortController`，让用户能"停止"。
5. **上下文长度限制**：历史太长时截断或总结，避免超出 token 上限。
6. **再往后**：会话持久化（数据库）、鉴权、RAG、工具调用（function calling）。

建议顺序：

- 先把交互打磨顺，让产品"好用"
- 再补健壮性，让产品"可靠"
- 最后才进持久化和更复杂能力

到这一步，你的项目已经是一个**功能完整的 AI 聊天骨架**了，后面的工作主要是"打磨"和"加固"，而不是"补核心能力"。
