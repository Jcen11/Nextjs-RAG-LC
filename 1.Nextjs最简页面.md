# Next.js 最小项目学习说明

这个文档配合当前项目使用，目标是帮助你用最少代码理解 Next.js App Router 的核心概念。

## 适合谁

- 已经有 React 基础
- 还没有 Next.js 基础
- 想先理解最核心概念，不想一开始就接触 Tailwind、数据库、鉴权、状态管理

---

## 先看项目结构

```txt
app/
  about/
    page.tsx
  api/
    hello/
      route.ts
  globals.css
  layout.tsx
  page.tsx

components/
  Counter.tsx

package.json
tsconfig.json
next-env.d.ts
```

你重点先看这 5 个文件：

- `app/layout.tsx`
- `app/page.tsx`
- `app/about/page.tsx`
- `components/Counter.tsx`
- `app/api/hello/route.ts`

---

## 推荐阅读顺序

建议按下面顺序阅读：

1. `app/page.tsx`
2. `app/about/page.tsx`
3. `app/layout.tsx`
4. `components/Counter.tsx`
5. `app/api/hello/route.ts`

这样读的原因：

- 先看首页，理解页面是怎么定义的
- 再看 about 页，理解文件系统路由
- 再看 layout，理解页面为什么会被包起来
- 再看 Counter，理解客户端组件和 `useState`
- 最后看 API，理解 Next.js 不只是页面框架

---

## 每个文件应该学什么

## `app/page.tsx`

### 它是干什么的

- 它是首页
- 对应路由 `/`
- 展示标题、About 链接、Counter 组件

### 它属于 React 还是 Next.js

- 函数组件：React
- `app/page.tsx` 作为页面入口：Next.js
- `Link`：Next.js

### 你要重点理解什么

- App Router 中，`page.tsx` 代表一个页面
- 文件位置决定路由地址
- 页面里可以直接组合普通组件

### Vue 开发者最容易混淆的点

- 这里没有 `routes` 配置文件
- 路由不是手动注册的，而是由目录结构决定的
- `Link` 类似 Vue Router 里的 `router-link`

---

## `app/about/page.tsx`

### 它是干什么的

- 它是 About 页面
- 对应路由 `/about`

### 它属于 React 还是 Next.js

- 页面内容本身：React
- `about/page.tsx` 映射成 `/about`：Next.js

### 你要重点理解什么

- 路由层级和目录层级是一一对应的
- `app/about/page.tsx` 这种写法就是 App Router 的核心约定

### Vue 开发者最容易混淆的点

- Vue Router 常见思维是配置对象
- Next.js App Router 常见思维是目录即路由

---

## `app/layout.tsx`

### 它是干什么的

- 它是根布局
- 包住整个应用
- 放全局样式导入
- 放页面元信息
- 用 `children` 渲染具体页面

### 它属于 React 还是 Next.js

- 组件函数和 `children`：React
- `layout.tsx` 这个文件约定：Next.js
- `metadata`：Next.js

### 你要重点理解什么

- 所有页面都会经过这个布局
- `children` 就是当前页面内容
- App Router 通过 `layout.tsx` 自动建立布局关系

### Vue 开发者最容易混淆的点

- 可以先把它类比成“布局组件 + router-view”
- 但这里不是你手动配置出来的，而是 Next.js 自动识别的
- `metadata` 不是 React 标准能力，是 Next.js 提供的能力

---

## `components/Counter.tsx`

### 它是干什么的

- 这是一个普通 React 组件
- 使用 `useState` 管理计数
- 点击按钮进行加减

### 它属于 React 还是 Next.js

- `useState`、组件函数、点击事件：React
- `'use client'`：Next.js

### 你要重点理解什么

- 在 App Router 里，默认组件不是客户端组件
- 使用 `useState`、`onClick` 这种交互能力时，需要写 `'use client'`
- 这就是客户端组件最小示例

### Vue 开发者最容易混淆的点

- Vue 组件默认就是可交互的
- Next.js App Router 里不是默认所有组件都能直接用状态和事件
- `'use client'` 不是普通字符串，而是一个运行方式声明

---

## `app/api/hello/route.ts`

### 它是干什么的

- 它定义了一个 API 接口
- 访问 `/api/hello` 时返回 JSON

### 它属于 React 还是 Next.js

- 这是 Next.js 概念，不是 React 概念

### 你要重点理解什么

- `route.ts` 用来定义接口
- `GET` 表示处理 GET 请求
- Next.js 可以在同一个项目里同时写页面和接口

### Vue 开发者最容易混淆的点

- 传统 Vue 项目通常前后端分离得更明显
- Next.js 可以把页面和接口放在一个项目里
- 这不是 Express 的 `req/res` 风格，而更接近 Web 标准 `Request/Response`

---

## 这个项目里最重要的 6 个知识点

### 1. 文件系统路由

- `app/page.tsx` 对应 `/`
- `app/about/page.tsx` 对应 `/about`

这是 App Router 最核心的基础。

### 2. Layout 布局机制

- `layout.tsx` 会包住页面
- 页面内容通过 `children` 注入

### 3. 页面组件和普通组件的区别

- `page.tsx` 是路由页面
- `components/Counter.tsx` 是普通组件

### 4. 客户端组件

- 不是所有组件都能直接用 `useState`
- 需要交互时要写 `'use client'`

### 5. Next.js 页面跳转

- 用 `next/link`
- 不需要自己手写路由表

### 6. API Route

- `app/api/hello/route.ts` 说明 Next.js 不只是前端页面框架
- 它也有服务端接口能力

---

## React 和 Next.js 要分开理解

学习初期最重要的一件事，就是时刻区分：

### React 负责什么

- 组件
- JSX
- `useState`
- 事件处理
- 组件组合
- `children`

### Next.js 负责什么

- 文件系统路由
- `layout.tsx` / `page.tsx` / `route.ts` 这些约定
- `next/link`
- `metadata`
- 客户端组件与服务端组件的组织方式

很多初学 Next.js 的困惑，本质上都是因为把 React 和 Next.js 混在一起理解了。

---

## 作为 Vue 开发者，你最该先完成的思维切换

### 从“配置路由”切换到“目录就是路由”

Vue 常见：

- 写 `routes`
- 手动把组件映射到路径

Next.js 常见：

- 建目录
- 放 `page.tsx`
- 路径自动生成

### 从“组件默认可交互”切换到“交互组件要显式标记”

Vue 里组件默认就可以写响应式和事件。

Next.js App Router 里：

- 默认更偏服务端组件
- 用 `useState`、点击事件时，通常要写 `'use client'`

### 从“前后端分离项目”切换到“一个项目里也可以同时有页面和接口”

这会影响你对项目结构的理解。

---

## 建议你边看边验证

启动项目：

```bash
npm install
npm run dev
```

然后访问：

- 首页：`http://localhost:3000`
- About：`http://localhost:3000/about`
- API：`http://localhost:3000/api/hello`

你可以边访问边对应源码：

- 首页对应 `app/page.tsx`
- About 对应 `app/about/page.tsx`
- API 对应 `app/api/hello/route.ts`

---

## 学完这个项目后，你应该掌握什么

看完并跑通这个项目后，你应该能明确回答这些问题：

1. App Router 里页面是怎么定义的
2. `layout.tsx` 是做什么的
3. `page.tsx` 和普通组件有什么区别
4. 为什么 `useState` 组件要写 `'use client'`
5. `next/link` 是干什么的
6. `route.ts` 为什么能返回接口数据
7. React 概念和 Next.js 概念分别是什么

如果这些问题你都能说清楚，就说明你已经把 Next.js 最基础的一层打通了。

---

## 下一步最适合学什么

在这个最小项目之后，建议按这个顺序继续：

1. 嵌套路由
2. 嵌套布局
3. 动态路由
4. Server Component 基础
5. 数据获取
6. 表单与 Server Action

先不要急着学：

- Tailwind
- 数据库
- 鉴权
- 大型状态管理
- 部署优化

先把路由、布局、客户端组件、接口这几个基础打稳，后面会轻松很多。
