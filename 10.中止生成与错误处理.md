# 阶段 10/11：中止生成 + 错误处理

> 这次把"健壮性"两条线合并成一次更新——前 9 个阶段解决"能用 + 好用"，这两个解决"不出事"。
>
> **本阶段做的两件事**：
> 1. **中止生成**：用户点"停止"，立刻中断 AI 回复，已生成的内容保留
> 2. **错误处理**：后端 `route.ts` 全面加 `try/catch`，前端区分"中止/网络错误/其它异常"，给友好提示
>
> 涉及文件：`app/api/chat/route.ts`、`components/ChatBox.tsx`、`app/globals.css`
> 约定：被替换的旧代码全部**注释保留**，方便对比前后变化。

---

## 一、为什么合并

按原路线，"中止生成"是阶段 10，"错误处理"是阶段 11。但它们其实是**同一件事的两面**——都是"请求在过程中出岔子"：

| 情况 | 谁触发 | 该怎么处理 |
|---|---|---|
| 用户点"停止" | 用户主动 | 不报错，保留已生成内容 |
| 连不上上游/超时 | 网络或服务 | 报错，提示重试 |
| 流读到一半上游断了 | 服务端崩 | 报错，给已生成部分 + 错误提示 |
| 上游返回非 JSON 错误体 | 上游异常 | 容错，不二次崩溃 |

它们的底层都靠**同一个机制**——`AbortController`。所以一次讲清更连贯，分开讲会割裂。

---

## 二、核心概念：AbortController

这是 Web 标准 API（浏览器和 Node 18+ / Next 服务端都支持），用来**中断一个异步操作**。

### 基本用法

```ts
const controller = new AbortController();

// 把 signal 传给任何支持中断的异步操作
fetch(url, { signal: controller.signal });

// 想中断时——任何地方都能调
controller.abort();  // → fetch 立刻抛出 AbortError
```

**关键三件套**：
- `controller.signal` —— 一个 `AbortSignal` 对象，传给 `fetch`，相当于"挂号"
- `controller.abort()` —— 拨这个号，挂断所有挂了号的请求
- `signal.aborted` —— 布尔值，判断"是不是已经中断过"

### 为什么用 ref 不用 state

前端代码里：
```ts
const abortRef = useRef<AbortController | null>(null);
```

`useRef` 和 `useState` 的区别：

| | useState | useRef |
|---|---|---|
| 改值会触发重渲染吗 | ✅ 会 | ❌ 不会 |
| 用途 | 存"要显示给用户看的数据" | 存"组件用的工具/句柄" |
| 例子 | messages、input、loading | 定时器 id、AbortController、DOM 引用 |

`AbortController` 是个"遥控器"，用户看不到它，改它不需要重渲染页面 → 用 `ref`。

---

## 三、后端改造（`route.ts`）

后端是这次改动的大头。原来的 `route.ts` 有几个脆弱点，逐一加固。

### 3.1 请求体解析容错

**旧**：
```ts
const body = await request.json();  // 非法 JSON 会抛错且无人接
```

**新**：
```ts
let body;
try {
  body = await request.json();
} catch {
  return Response.json({ reply: "请求体不是合法的 JSON。" }, { status: 400 });
}
```

**为什么**：前端永远会发合法 JSON，但生产环境要防的不是"正常情况"，是"意外情况"——比如被爬虫乱请求、被恶意构造 payload。一行 `try/catch` 让接口在乱请求下也不会 500 崩栈。

### 3.2 超时 + 客户端中断（重点）

这是这次的核心。一个 `AbortController` 同时管两件事：

```ts
const UPSTREAM_TIMEOUT_MS = 30000;

const upstreamController = new AbortController();
const timeoutId = setTimeout(() => {
  upstreamController.abort();  // 30 秒上游没动静 → 主动中断
}, UPSTREAM_TIMEOUT_MS);

// 客户端中断（request.signal）→ 同步中断上游
const onClientAbort = () => upstreamController.abort();
request.signal.addEventListener("abort", onClientAbort);

let response: Response;
try {
  response = await fetch(`${baseURL}/chat/completions`, {
    // ... headers, body ...
    signal: upstreamController.signal,  // ← 关键：把 signal 挂上
  });
} catch {
  clearTimeout(timeoutId);
  request.signal.removeEventListener("abort", onClientAbort);
  const aborted = upstreamController.signal.aborted;
  return Response.json(
    { reply: aborted ? "请求超时或已中断。" : "无法连接到上游 AI 服务。" },
    { status: aborted ? 504 : 502 },
  );
}

clearTimeout(timeoutId);  // 连上了，别让定时器误杀正常流
```

**这里有几个关键理解点**：

#### a) `request.signal` 是什么
Next.js 的 `POST(request: Request)` 里，`request` 是标准 `Request` 对象，自带 `signal`。**客户端断开连接时（比如用户点了停止、关了页面），这个 signal 会自动 abort。** 我们监听它，就能知道"客户端不要了"。

#### b) 为什么用一个 controller 同时管两件事
两个独立的中断源（超时 / 客户端断开）都要去中断上游 fetch。与其写两套逻辑，不如让它们都去触发同一个 `upstreamController.abort()`，下游只看一个信号。这是"扇入"的设计——多个输入汇到一个动作。

#### c) HTTP 状态码的选择
- `504 Gateway Timeout`：网关超时（我们这边作为"网关"，上游没及时回）
- `502 Bad Gateway`：网关收到了无效响应（连不上上游）
- 用对状态码，方便以后接监控/日志系统时一眼看出是哪类问题。

#### d) `clearTimeout` 的时机
连上上游后立刻清掉超时定时器。否则 30 秒后定时器还会触发 abort，把正在正常流的请求误杀掉。**定时器用完一定要清，否则会泄漏。**

### 3.3 错误体容错

**旧**：
```ts
if (!response.ok) {
  const data = await response.json();  // 上游返回 HTML 错误页 → 这里又抛错
  // ...
}
```

**新**：
```ts
if (!response.ok) {
  let errorText = "未知错误";
  try {
    const data = await response.json();
    errorText = data?.error?.message || errorText;
  } catch {
    // 上游返回的不是 JSON（比如 HTML 错误页、纯文本），维持"未知错误"
  }
  // ... 用 errorText
}
```

**为什么**：上游报错时不一定返回 JSON。比如某些 CDN/网关在 502 时返回 HTML 错误页，这时 `response.json()` 又会抛错——原本一个错误会变成"错误 + 错误"，体验更差。包一层 try/catch 就稳了。

### 3.4 SSE 单行容错

**旧**：
```ts
const data = JSON.parse(jsonText);  // 一行坏数据 → 整条流崩
```

**新**：
```ts
let data;
try {
  data = JSON.parse(jsonText);
} catch {
  continue;  // 这行解析失败就跳过，不影响后面的行
}
```

**为什么**：SSE 流是按行来的。万一某一行被网络分包切坏、或者上游偶尔吐一行非法 JSON，旧代码会让整个聊天直接崩掉。新代码只丢这一行，后面的 token 照常显示。**"局部失败不拖垮全局"是流式处理的重要原则。**

### 3.5 流的 cancel：让客户端能真正中断上游

```ts
const stream = new ReadableStream<Uint8Array>({
  async start(controller) { /* ...原来的读取循环... */ },
  cancel() {
    reader.cancel();  // 新增
  },
});
```

**这是中止生成能"立刻停"的关键**。流程是这样的：

```
用户点"停止"
  ↓
前端 controller.abort()
  ↓
fetch 被 abort → 前端到后端的连接断开
  ↓
后端 request.signal 触发 abort（3.2 监听的那个）
  ↓
upstreamController.abort() → 后端到上游的 fetch 也断开
  ↓
但还有最后一环：后端读上游的 reader 还在 read() 里卡着
  ↓
ReadableStream 的 cancel() 被调用 → reader.cancel() → 立刻解除阻塞
```

少了 `cancel()` 这步，虽然 fetch signal 会触发，但 `reader.read()` 那个 Promise 可能要等下一次数据来才 resolve，"停止"就有延迟。加上 `reader.cancel()`，reader 立刻返回 done，整个链条干净利落断开。

**记忆点**：完整的中断链路是「前端 abort → fetch 中断 → request.signal → 上游 fetch 中断 → reader.cancel」。少一环，中断就不彻底。

### 3.6 深入：后端的 cancel 能不能省（常见疑问）

这一节回答一个很容易冒出来的疑问，也是面试时可能被追问的点。

**疑问**：前端 `abort()` 已经断了 HTTP 连接，后端 `request.signal` 也触发了 `upstreamController.abort()` 把上游 fetch 断了。那后端 reader 读的是上游的流，上游都断了，reader 自然读不到东西——**后端那个 `cancel() { reader.cancel() }` 是不是多余的？**

#### 先分清：链路里有两个 reader

这是理解本节的前提，很容易混：

```
上游 AI ──流A──> 后端 reader（读上游，route.ts:152）──> 后端 ReadableStream ──流B──> 前端 reader（读后端，ChatBox.tsx）
```

| | 后端 reader | 前端 reader |
|---|---|---|
| 在哪 | `route.ts` 的 `response.body.getReader()` | `ChatBox.tsx` 的 `response.body?.getReader()` |
| 读谁 | 上游 AI 返回的流（流A） | 后端返回的流（流B） |
| 归谁管 | 后端代码 | 前端代码 |

前端 `reader.cancel()` cancel 的是**前端那个 reader（读流B）**，对后端 reader（读流A）**没有任何直接作用**——它们是两个独立的 reader 对象，分属两条独立的流。前端的手够不到后端的 reader。

所以"把后端 cancel 去掉、改到前端 cancel"这个想法，物理上做不到——前端的代码碰不到后端的 reader 对象。**各管各的 reader，各断各的流。**

#### 那"后端 cancel 能不能去掉"（靠上游 abort 间接断 reader）

这个问法是对的。如果只看"后端 reader 最终会不会停"——会停，因为 `upstreamController.abort()` 断了上游 fetch，后端 reader 读的是上游的流，上游没了，reader 最终也会结束。

**但"最终会停"和"立刻停"不一样**。问题出在时序：

```ts
while (true) {
  const { done, value } = await reader.read();  // ← 卡在这
  // ...
}
```

`upstreamController.abort()` 触发后，上游 fetch 断开，但**这个 `reader.read()` 不会立刻返回**。它要等：
- 上游 socket 真正关闭
- TCP 缓冲区排空
- 运行时把"流已取消"的状态传到 reader

这段时间可能是**几十毫秒到几秒**。这期间后端 reader 还卡在 `await reader.read()` 里，对应的后端 worker / 连接资源没释放。

`stream.cancel() { reader.cancel() }` 的作用就是**不等上游信号传过来，直接对 reader 下令"立刻停"**，让那个卡住的 `await` 马上返回。

#### 两种方案的真实对比

| | 去掉后端 cancel（只靠上游 abort） | 保留后端 cancel（现在的写法） |
|---|---|---|
| 用户体验 | ✓ 一样，前端已停 | ✓ 一样 |
| 上游 API 花费 | ✓ 一样，都靠 abort 断了 | ✓ 一样 |
| 后端 reader 停下速度 | 慢，等上游信号传过来 | 快，立刻 |
| 后端资源释放 | 延迟几十ms~几秒 | 立刻 |
| 高并发下 | 连接堆积风险 | 干净 |

#### 为什么这在生产环境很重要

单用户、低并发场景下，两种方案**用户感知不到差别**——后端 reader 多卡一会儿，最终也会因上游断开而结束，没人察觉。

但生产环境里，假设 **100 个用户同时点停止**，后端就会有 **100 个 reader 各卡几十ms~几秒**才释放。这种堆积会占用后端连接池、worker 线程，严重时影响其它请求的响应速度。

> **面试话术**：被问"你的项目怎么处理中断"时，不只是说"前端 abort 了"，而是能讲清楚"后端为什么还要 cancel——为了立刻释放 reader 占用的资源，避免高并发下连接堆积"。这一层考虑就是"业余项目"和"工程项目"的分水岭。

#### 还有一个相关疑问：前端能不能用 reader.cancel 代替 abort

不能。语义和后果都不同：

| | `controller.abort()` | `reader.cancel()` |
|---|---|---|
| 断什么 | HTTP 连接（强信号） | 前端消费侧（弱信号） |
| 后端感知 | ✓ `request.signal` 触发 | ✗ 不一定触发 |
| 后端停上游 | ✓ 立刻 | ✗ 可能继续烧 |
| 进 catch | ✓ 进，能区分中止 | ✗ 不进，被当正常结束 |
| 适合场景 | **用户主动停止** | 流读完后清理资源 |

`abort()` 让 fetch 抛异常进 catch，前端的 `controller.signal.aborted` 判断才成立（才能加"（已停止）"标记）。`reader.cancel()` 让 `reader.read()` 正常返回 done，走的是"正常结束"分支——已生成的消息会被当成"AI 正常说完"，逻辑就错了。

**所以前端用 `abort()` 是对的**：它不只是停前端，更是给后端一个明确的"我不要了"信号，让整条链路干净断开。`reader.cancel()` 适合的是"流正常读完了，清理一下"这种收尾场景，不是用来中断请求的。

---

## 四、前端改造（`ChatBox.tsx`）

### 4.1 加 ref 和 handleStop

```ts
import { useRef, useState } from "react";

const abortRef = useRef<AbortController | null>(null);

function handleStop() {
  abortRef.current?.abort();
}
```

### 4.2 发送时新建 controller，传给 fetch

```ts
const controller = new AbortController();
abortRef.current = controller;

try {
  const response = await fetch("/api/chat", {
    // ...
    signal: controller.signal,  // ← 关键
  });
  // ...
}
```

**为什么每次新建**：每个请求要一个独立的 controller。复用已 abort 过的 controller 没用——它的 signal 已经是 aborted 状态，新 fetch 会立刻被中断。

### 4.3 catch 区分中止 vs 异常（重点）

这是前端最值得理解的一段：

```ts
} catch (error: unknown) {
  const aborted = controller.signal.aborted;

  setMessages((prev) => {
    const next = [...prev];
    const lastMessage = next[next.length - 1];

    if (aborted) {
      // 用户主动中止 → 不算错误，保留已生成内容
      next[next.length - 1] = {
        ...lastMessage,
        content:
          lastMessage.content.trim().length > 0
            ? `${lastMessage.content}\n\n_（已停止）_`
            : "_（已取消）_",
      };
    } else {
      // 真异常 → 区分网络错误，给具体提示
      const isNetworkError =
        error instanceof TypeError && error.message === "Failed to fetch";
      next[next.length - 1] = {
        role: "assistant",
        content: isNetworkError
          ? "网络连接失败，请检查网络后重试。"
          : "请求过程中发生异常，请重试。",
      };
    }
    return next;
  });
} finally {
  abortRef.current = null;  // 清理，避免下次发送误用到旧的 controller
  setLoading(false);
}
```

**几个关键理解点**：

#### a) 为什么用 `controller.signal.aborted` 而不是判断 error
`fetch` 被 abort 时抛的是 `DOMException`，名字叫 `AbortError`。但**直接靠 error 类型判断不可靠**——不同浏览器/运行时可能行为不同。更稳的方式是查我们自己的 controller：`controller.signal.aborted` 是我们自己创建的、确定可靠的状态。

#### b) 中止为什么不算错误
用户点"停止"是**预期行为**，不是错误。已经生成的部分可能对用户有用（比如 AI 答到一半用户已经懂了），所以保留内容 + 加个"（已停止）"标记，而不是用错误样式覆盖掉。

#### c) "Failed to fetch" 是什么
浏览器在 fetch **完全连不上**时（DNS 失败、网络断开、CORS 被拦），会抛 `TypeError: Failed to fetch`。这是个比较固定的特征，可以用来给用户更具体的提示（"检查网络"而不是笼统的"发生异常"）。注意：服务端返回 500 **不是**这种错误——那种情况 `response.ok` 是 false，走的是上面的 `if (!response.ok)` 分支，不会进 catch。

#### d) `error: unknown` 类型注解
TypeScript 5.x 默认 catch 的 error 是 `unknown` 类型（不能直接 `error.message`）。要先判断类型再用：
```ts
error instanceof TypeError && error.message === "Failed to fetch"
```
这是 TS 严格模式下的安全写法——强迫你先确认 error 是什么再访问它的属性。

### 4.4 停止按钮

```tsx
{loading && (
  <button className="stop-btn" onClick={handleStop} type="button">
    停止
  </button>
)}
```

只在 `loading` 时显示。用条件渲染（不是 disabled）——loading 结束按钮直接消失，逻辑更清晰。

### 4.5 旧 catch 注释保留

按约定，旧的 catch 没删，注释保留在原位，方便对比"旧版只给笼统提示" vs "新版分情况处理"的演进。

---

## 五、CSS 改动（`globals.css`）

纯追加 `.stop-btn` 样式，用警示色（红底）和其它按钮区分：

```css
.stop-btn {
  background: #fee2e2;
  color: #b91c1c;
  border: 1px solid #fca5a5;
  border-radius: 4px;
  padding: 2px 12px;
  margin-left: 8px;
  cursor: pointer;
}
.stop-btn:hover {
  background: #fecaca;
}
```

---

## 六、完整中断流程图（一定要看懂）

```
┌─────────┐     ┌─────────────┐     ┌─────────────┐
│  用户   │     │  前端       │     │  后端       │     ┌─────────┐
│ 点"停止"│     │ ChatBox     │     │ route.ts    │     │ 上游 AI │
└────┬────┘     └──────┬──────┘     └──────┬──────┘     └────┬────┘
     │                 │                   │                  │
     │  handleStop()   │                   │                  │
     ├────────────────►│                   │                  │
     │                 │ abortRef.abort()  │                  │
     │                 ├──────────────────►│                  │
     │                 │                   │ request.signal   │
     │                 │                   │   abort 触发     │
     │                 │                   ├──┐               │
     │                 │ fetch 连接断开    │  │ upstream.abort │
     │                 │◄──────────────────┘  └──────────────►│
     │                 │                   │                  │ 上游 fetch 断
     │                 │                   │ reader.cancel()  │
     │                 │                   ├──┐               │
     │                 │                   │  └─► reader 解除 │
     │                 │                   │      阻塞        │
     │                 │ catch(aborted)    │                  │
     │                 │ 保留已生成内容    │                  │
     │                 │ + "（已停止）"    │                  │
     │  看到停止效果   │                   │                  │
     │◄────────────────┘                   │                  │
```

**口诀**：用户点 → 前端 abort → 后端 request.signal 触发 → 上游 fetch abort → reader.cancel。**五环联动**，少一环中断就不干净。

---

## 七、状态码对照表

后端现在会返回的状态码：

| 码 | 含义 | 触发条件 |
|---|---|---|
| 400 | Bad Request | 请求体非 JSON / messages 不是数组或为空 |
| 500 | Internal Error | 没配 API key / 上游返回非 ok / 没返回 body |
| 502 | Bad Gateway | 连不上上游（网络层） |
| 504 | Gateway Timeout | 上游 30 秒没响应 / 客户端中断 |

前端处理：
- `!response.ok`（400/500/502/504）→ 读 `data.reply` 显示后端给的提示
- 抛进 catch（fetch 连不上后端）→ 显示"网络连接失败"

---

## 八、改动汇总（对照看前后变化）

### `app/api/chat/route.ts`
- ✅ `request.json()` 包 try/catch（非法 JSON 返回 400）
- ✅ 加 `UPSTREAM_TIMEOUT_MS = 30000` 常量
- ✅ 新增 `upstreamController`：管超时 + 客户端中断
- ✅ `request.signal.addEventListener("abort", ...)` 监听客户端断开
- ✅ `fetch` 加 `signal`、包 try/catch
- ✅ fetch 失败区分 `aborted`（504）vs 网络错误（502）
- ✅ 上游非 ok 时，错误体 `response.json()` 也包 try/catch
- ✅ SSE `JSON.parse` 单行包 try/catch + `continue`
- ✅ `ReadableStream` 加 `cancel() { reader.cancel() }`
- ✅ finally 里 `removeEventListener` + `reader.releaseLock` 清理

### `components/ChatBox.tsx`
- ✅ import 加 `useRef`
- ✅ 新增 `abortRef = useRef<AbortController | null>(null)`
- ✅ 新增 `handleStop()` 函数
- ✅ `handleSend` 里每次新建 controller，存进 ref，传 `signal` 给 fetch
- ✅ catch 改为 `error: unknown`，区分 `aborted` / 网络错误 / 其它
- ✅ 旧 catch 注释保留
- ✅ JSX 加"停止"按钮（loading 时显示）
- ✅ finally 里 `abortRef.current = null` 清理

### `app/globals.css`
- ✅ 追加 `.stop-btn` 样式

---

## 九、自测题（答案在文末）

### Q1
为什么 `abortRef` 用 `useRef` 而不是 `useState`？如果用 `useState` 会发生什么？

### Q2
后端的 `request.signal` 是什么？它什么时候会触发 abort？

### Q3
完整描述从"用户点停止"到"上游 fetch 真正断开"经过的 5 个环节。

### Q4
后端 fetch 失败时，为什么用 `upstreamController.signal.aborted` 判断是不是超时，而不是看 `error.name === 'AbortError'`？

### Q5
前端 catch 里，"用户主动中止"为什么不当成错误处理？这反映了一个什么设计原则？

### Q6
服务端返回 HTTP 500 时，前端会进 catch 吗？为什么？那 catch 主要接的是什么情况？

### Q7
后端 `ReadableStream` 的 `cancel()` 方法如果不写，会怎样？中断还能工作吗？

### Q8
"Failed to fetch" 这个错误在什么情况下出现？它和"服务端返回 500"在前端是怎么分别处理的？

### Q9（进阶）
为什么连上上游后要 `clearTimeout(timeoutId)`？不清理会有什么后果？

### Q10（进阶）
如果用户在 AI 还没开始回复（loading 中、reader 还没创建）就点停止，流程还能正常走吗？为什么？

---

## 十、自测题答案

<details>
<summary>点击展开答案</summary>

**A1**：因为 `AbortController` 是组件内部用的"句柄"，不需要触发重渲染——用户看不到它，改它不需要重画页面。如果用 `useState`，每次 `setAbortRef` 会触发一次无意义的重渲染，且 `state` 的更新是异步的，停止按钮点下去那一刻可能拿不到最新的 controller。

**A2**：`request.signal` 是 Next.js 给 `POST(request: Request)` 的 `request` 对象自带的 `AbortSignal`。当**客户端断开连接**时（用户点了停止让前端 abort 了 fetch、或者关了页面），这个 signal 会自动触发 abort。它是"客户端不要了"的信号。

**A3**：① 用户点停止 → ② 前端 `handleStop` 调 `abortRef.current.abort()` → ③ fetch 被 abort，前端到后端的连接断开，后端的 `request.signal` 触发 abort → ④ `onClientAbort` 回调调 `upstreamController.abort()`，后端到上游的 fetch 也断 → ⑤ `ReadableStream.cancel()` 被调用，`reader.cancel()` 解除 `reader.read()` 的阻塞。五环联动。

**A4**：因为 `controller.signal.aborted` 是我们自己创建的对象，状态完全可靠、确定。而靠 `error.name` 或 error 类型判断不可靠——不同运行时（浏览器/Node）、不同 fetch 实现，abort 抛的 error 可能有差异。查自己的 controller 最稳。

**A5**：因为用户主动中止是**预期行为**，不是错误。已经生成的内容对用户可能有用，保留下来 + 加"（已停止）"标记，比用错误样式覆盖掉更合理。这反映的原则是：**区分"预期内的中断"和"意外的失败"**，前者静默处理，后者才报错。

**A6**：不会进 catch。500 是正常的 HTTP 响应，fetch 会正常 resolve，只是 `response.ok` 是 false。前端走 `if (!response.ok)` 分支，读 `data.reply` 显示后端给的错误提示。catch 主要接的是 **fetch 根本没连上后端**的情况（网络断开、DNS 失败、CORS 被拦、被 abort）。

**A7**：能工作，但不彻底。没有 `cancel()`，虽然 fetch 的 signal 会触发，但 `reader.read()` 那个 Promise 可能要等下一次数据到达才 resolve，导致"停止"有延迟——AI 可能还会再吐几个字才真的停。加了 `cancel()`，reader 立刻返回 done，中断立刻生效。

**A8**："Failed to fetch" 在**网络层完全连不上**时出现（网络断、DNS 失败、CORS 被拦）。它会让 fetch 抛 `TypeError`，进前端的 catch。而"服务端返回 500"是 fetch 成功连接但服务端报错，进的是 `if (!response.ok)` 分支，不进 catch。两者在代码里是两条不同的处理路径。

**A9**：`clearTimeout` 是为了防止定时器在请求正常进行时误触发。如果不清理，30 秒后定时器会调 `upstreamController.abort()`，把一个正在正常流式输出的请求误杀掉——AI 明明在好好回答，30 秒一到突然断了。连上上游后请求已经在正常流，就该把"超时保护"撤掉。

**A10**：能正常走。因为前端是在 `handleSend` 开头就新建了 controller 并传给 fetch，中断靠的是 `controller.abort()`，和 reader 是否创建无关。用户在 reader 创建前点停止，fetch 会被 abort，直接进 catch，`controller.signal.aborted` 为 true，走"已取消"分支。后端那边，`request.signal` 一旦 abort，连 fetch 都没发起就会进 catch，返回 504。整条链路在任何时机都能干净中断——这是 AbortController 设计上的优势。

</details>

---

## 十一、阶段总结

这一阶段把项目从"能用"推向"可靠"。学到的核心可迁移知识：

1. **AbortController** 是 Web 标准的中断机制，fetch / 任何支持 signal 的 API 都能用，前后端通用
2. **完整中断链路**：用户动作 → 前端 abort → 后端 request.signal → 上游 fetch abort → reader.cancel，五环联动
3. **错误处理分层**：请求体解析、上游连接、上游响应、流式解析，每层独立 try/catch，局部失败不拖垮全局
4. **状态码语义化**：400/500/502/504 各有含义，方便监控和排查
5. **区分预期中断和意外失败**：前者静默保留，后者报错提示
6. **TypeScript 严格模式**：catch 的 error 是 unknown，要先类型判断再访问属性

**下一阶段预告**：阶段 12——会话持久化（SQLite 起步），让聊天记录刷新页面也不丢。从"健壮性"进入"产品深度"。
