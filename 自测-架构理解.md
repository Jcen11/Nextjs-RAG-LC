# 自测：架构理解

> 看完所有文档和代码后用这份自测。能答上来 = 架构真懂了；答不上来 = 回去重看对应文档。
>
> **使用方式**：先不看答案，自己在草稿上写。全部写完再对答案（每题末尾有 `<details>` 折叠答案）。
>
> 答完给自己的评级：
> - 90%+ 对：架构通了，可以推进新阶段
> - 60-90%：基本懂，针对性补漏
> - < 60%：建议重读 README + 阶段12之后架构梳理

---

## 题型一：填空——每个文件的作用（核心）

> 这是主题型。能说清每个文件干嘛，说明架构通了。按目录分组，越往后越难。

### app/ 目录（路由层）

| 文件 | 作用（自己填） |
|---|---|
| `app/layout.tsx` | |
| `app/page.tsx` | |
| `app/about/page.tsx` | |
| `app/globals.css` | |
| `app/chat/layout.tsx` | |
| `app/chat/page.tsx` | |
| `app/chat/[id]/page.tsx` | |

### app/api/ 目录（API 层）

| 文件 | 作用（自己填） |
|---|---|
| `app/api/chat/route.ts` | |
| `app/api/conversations/route.ts` | |
| `app/api/conversations/[id]/route.ts` | |
| `app/api/messages/route.ts` | |
| `app/api/knowledge/route.ts` | |
| `app/api/knowledge/[id]/route.ts` | |
| `app/api/knowledge/[id]/documents/route.ts` | |
| `app/api/hello/route.ts` | |

### components/ 目录（UI 组件）

| 文件 | 作用（自己填） | 服务端/客户端？ |
|---|---|---|
| `components/ChatBox.tsx` | | |
| `components/Sidebar.tsx` | | |
| `components/KbSelector.tsx` | | |
| `components/KnowledgePanel.tsx` | | |
| `components/Markdown.tsx` | | |
| `components/SyncConversationId.tsx` | | |
| `components/Counter.tsx` | | |
| `components/ui/button.tsx` | | |
| `components/ui/input.tsx` | | |
| `components/ui/textarea.tsx` | | |
| `components/ui/dialog.tsx` | | |
| `components/ui/scroll-area.tsx` | | |

### lib/ 目录（业务逻辑）

| 文件 | 作用（自己填） |
|---|---|
| `lib/db.ts` | |
| `lib/queries.ts` | |
| `lib/store.ts` | |
| `lib/langchain.ts` | |
| `lib/rag.ts` | |
| `lib/vectorstore.ts` | |
| `lib/utils.ts` | |

### 配置文件

| 文件 | 作用（自己填） |
|---|---|
| `postcss.config.mjs` | |
| `components.json` | |
| `tsconfig.json` 里的 `@/*` paths | |
| `.env.local` | |

---

## 题型二：数据流追踪（中等）

### 题 1：新建会话发首条消息的完整数据流
用户在 `/chat` 输入"你好"点发送，到消息显示出来，数据经过了哪些环节？至少写出 8 个步骤。

<details><summary>答案要点（自己写完再看）</summary>

1. ChatBox.handleSend 触发
2. setMessages 把 [user, 空assistant] 写进 store
3. 发现无 currentId → POST /api/conversations 创建会话 → 拿到 id
4. setJustCreatedId(id) 标记（防 useEffect 覆盖）
5. router.push("/chat/新id") 改 URL
6. SyncConversationId 把 id 同步进 store.currentId
7. POST /api/chat（带 messages + kbId）→ ChatOpenAI 流式回复
8. 流式 chunk 累积 → updateLastMessage 更新 store → UI 显示
9. 流结束 → POST /api/messages 保存到数据库 → refreshConversations 刷新侧边栏
</details>

### 题 2：刷新页面恢复历史的完整数据流
用户在 `/chat/abc` 按刷新，历史消息怎么显示出来的？

<details><summary>答案</summary>

1. 浏览器请求 /chat/abc → Next.js 渲染 [id]/page.tsx（服务端 await params 拿到 abc）
2. page 渲染 SyncConversationId + ChatBox
3. SyncConversationId 的 useEffect 跑 → setCurrentId("abc")
4. ChatBox 的 useEffect（依赖 currentId）触发 → GET /api/conversations/abc
5. 后端 queries.getMessages(abc) 查数据库 → 返回消息数组
6. setMessages(历史) → store 更新 → ChatBox 重渲染显示历史
</details>

### 题 3：RAG 问答的数据流
用户在绑了知识库的会话提问"RAG 怎么切块"，AI 怎么基于文档回答的？

<details><summary>答案</summary>

1. ChatBox.handleSend 带 kbId 发 POST /api/chat
2. route.ts 检测到 kbId → 调 retrieveContext(kbId, 用户提问)
3. retrieveContext 用 bge-m3 把提问嵌入向量 → 向量库 similaritySearch(k=3)
4. 把 top3 相关片段拼成"以下是相关知识:..."字符串
5. 作为 system message 拼到 messages 数组前面
6. ChatOpenAI.stream(带知识的 messages) → 流式回复
7. AI 回复基于检索到的知识（不是凭空编）
</details>

---

## 题型三：设计决策（中难）

### 题 4：为什么 messages 放 store，input 留组件 useState？
<details><summary>答案</summary>

- messages 跨重建需存活（组件卸载重建后还要显示）→ 进 store
- input 是纯 UI 临时状态，重建后从空开始无所谓 → 留组件
- 口诀：重建后必须保留的进 store；重建后从空开始也无所谓的留组件
</details>

### 题 5：为什么 justCreatedId 用 getState() 读，不用 selector 订阅？
<details><summary>答案</summary>

如果用 selector 订阅并放进 useEffect 依赖数组，清标记 setJustCreatedId(null) 会触发 useEffect 重跑；重跑时标记已清空、保护失效，走到加载历史分支，fetch 到空数组覆盖 store.messages（消息消失 bug）。用 getState() 读不建立订阅，清标记不触发重跑。（详见 12b 第五轮）
</details>

### 题 6：为什么 RAG 不接管 chat 路由？
<details><summary>答案</summary>

RAG 只是 chat 的"前置步骤"——检索相关片段拼进 system message。LLM 调用的流式/中断/错误处理逻辑（阶段 10/11 打磨的）一行不改，保留健壮性。RAG 和 chat 解耦，RAG 有问题关掉就行，不影响纯对话。
</details>

### 题 7：为什么 vectorstore.ts 要做抽象层？
<details><summary>答案</summary>

业务代码（rag.ts、API）只依赖 VectorStore 接口，不绑死具体实现。当前用 MemoryVectorStore（内存），以后换 Chroma（持久化）只改 vectorstore.ts 一个文件，业务逻辑零改动。也是简历卖点（可插拔设计）。
</details>

### 题 8：服务端组件能渲染客户端组件吗？反过来呢？
<details><summary>答案</summary>

- 服务端 → 渲染客户端子组件：✅ 可以（[id]/page.tsx 渲染 ChatBox）
- 客户端 → 渲染服务端组件：❌ 不行（会把服务端组件当客户端处理，失去服务端能力）。变通：当 children 传进来
</details>

---

## 题型四：排查场景（难）

### 题 9：用户反馈"新建会话发消息，消息显示后消失"，你怎么排查？（STAR 法讲）
<details><summary>答案要点</summary>

- S 现象：新建会话发消息，消息显示后立刻消失（闪烁），刷新或切换回来又能显示
- T 任务：定位根因并修复
- A 行动：
  1. 从"刷新能恢复"反推 → 数据库有数据 → 问题在显示层
  2. 加 console.log 打印每次 useEffect 执行的 currentId/justCreatedId/messages.length
  3. 发现清标记 setJustCreatedId(null) 改变了依赖数组里的值，触发 useEffect 自我重跑
  4. 重跑时标记已清空、保护失效，走到加载历史分支，fetch 空数组覆盖 messages
- R 结果：justCreatedId 移出依赖数组，改用 getState() 读，根治
- 教训：useEffect 依赖数组自我触发是常见陷阱；日志调试胜过推理
</details>

### 题 10：用户点"停止"，完整的中断链路是什么？
<details><summary>答案要点（五环）</summary>

1. handleStop → abortRef.current.abort()
2. fetch 被 abort，前端到后端的 HTTP 连接断
3. 后端 request.signal 触发 → upstreamController.abort() → 后端到上游的 fetch 断
4. 后端 ReadableStream 失去消费者 → 运行时调 stream.cancel()
5. cancel() 里 reader.cancel() → 解除后端读上游的 reader 阻塞
（少一环中断不彻底）
</details>

---

## 题型五：进阶追问（加分题）

### 题 11：如果文档量到 10 万篇，RAG 怎么优化？
<details><summary>答案要点</summary>

- MemoryVectorStore 不行了（内存爆 + 全量扫描慢）
- 换持久化向量库（Chroma/FAISS/Milvus）支持 ANN 索引
- 嵌入只做一次（入库时持久化，不重复）
- rerank（粗检索 top50 → cross-encoder 精排 top3）
- 混合检索（向量 + BM25 关键词）
- 抽象层让换向量库只改一个文件
</details>

### 题 12：项目部署到 Vercel，哪些地方要改？
<details><summary>答案要点</summary>

- SQLite（node:sqlite）不能用：Vercel 是 serverless，无持久本地文件 + 不支持实验性模块 → 换 Turso（SQLite 云）或 Postgres
- 改 lib/db.ts 的连接方式（SQL 大部分通用）
- 向量库 MemoryVectorStore 也要换（重启就丢）→ Chroma 或托管向量库
- .env.local 的 key 配到 Vercel 环境变量
</details>

### 题 13：高并发下 SQLite 有什么问题？
<details><summary>答案要点</summary>

SQLite 单写入者——同时只能一个写，其它写要等。高并发写（多用户同时发消息保存）会排队甚至锁库。解决：WAL 模式 / 换 MySQL/Postgres / 写操作走队列。
</details>

### 题 14：如果想加"暗色模式"，改哪里？
<details><summary>答案要点</summary>

shadcn 的主题变量在 globals.css 的 :root。加一套暗色变量：
```css
@media (prefers-color-scheme: dark) {
  :root {
    --background: 222 47% 11%;
    --foreground: 210 40% 98%;
    /* ...其它颜色反转 */
  }
}
```
所有用 bg-primary/text-foreground 等类的组件自动跟随。
</details>

---

## 评分标准

| 题型 | 题数 | 权重 |
|---|---|---|
| 一、填空文件作用 | 33 个文件 | 40%（核心） |
| 二、数据流追踪 | 3 题 | 25% |
| 三、设计决策 | 5 题 | 20% |
| 四、排查场景 | 2 题 | 10% |
| 五、进阶追问 | 4 题 | 5%（加分） |

**关键看题型一**——33 个文件作用能写对 28+ 个，架构就通了。数据流和决策题答对 70% 即可。排查和进阶是加分。

---

## 答完后的行动建议

- **题型一错的多** → 重读 README 架构图 + 阶段12之后架构梳理
- **题型二错的多** → 重读 12（持久化流程）+ 15（RAG 流程）
- **题型三错的多** → 重读 14（Zustand 决策）+ 15（RAG 决策）
- **题型四错的多** → 重读 12b（五轮 debug）+ 10（中断链路）
- **题型五答得出** → 架构理解很深，可以推进部署/简历阶段了
