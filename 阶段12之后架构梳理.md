# 阶段 12 之后架构梳理

> 这份文档专门梳理**阶段 12（会话持久化）之后**的架构变动，方便复盘"现在的系统长什么样、阶段 12 以来改了什么、该看哪些文件"。
>
> 涵盖阶段 13（多会话管理）、阶段 14（全局状态管理）、以及为修复竞态 bug 做的若干轮调整。
>
> **阅读建议**：先看第一节的架构总览建立全局印象，再按第五节的推荐顺序逐个文件读。

---

## 一、最新系统架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        浏览器（客户端）                          │
│                                                                  │
│   Zustand store（lib/store.ts）← 组件外的全局状态中心            │
│   持有：currentId / messages / systemPrompt / loading /          │
│         conversations / justCreatedId                            │
│         ↑                                                        │
│         │ 订阅 + 调 action                                        │
│                                                                  │
│   ┌────────────┐  ┌──────────────────────────┐                  │
│   │  Sidebar   │  │  ChatBox                  │                  │
│   │（客户端）   │  │（客户端）                  │                  │
│   │ 会话列表    │  │ 消息显示+发送+流式         │                  │
│   │ 切换/重命名 │  │ Markdown 渲染             │                  │
│   │ /删除       │  │ 中止生成（AbortController）│                  │
│   └─────┬──────┘  └────────┬─────────────────┘                  │
│         │                  │                                      │
│         │  fetch           │  fetch（流式 + 保存）                │
└─────────┼──────────────────┼──────────────────────────────────────┘
          │                  │
──────────┼──────────────────┼─── HTTP ────────────────────────────
          ↓                  ↓
┌─────────────────────────────────────────────────────────────────┐
│                    Next.js 服务端                                │
│                                                                  │
│   路由层（app/）                                                  │
│   ┌─────────────────────────────────────────────┐               │
│   │ app/chat/layout.tsx（服务端，壳子）           │               │
│   │   渲染 <Sidebar/> + {children}               │               │
│   │   ↳ 切换路由时 layout 不重建，Sidebar 保留    │               │
│   ├─────────────────────────────────────────────┤               │
│   │ app/chat/page.tsx（服务端，/chat）            │               │
│   │   <ClearCurrentId/> + <ChatBox/>             │               │
│   ├─────────────────────────────────────────────┤               │
│   │ app/chat/[id]/page.tsx（服务端，/chat/xxx）   │               │
│   │   await params 取 id                          │               │
│   │   <SyncConversationId id/> + <ChatBox/>      │               │
│   └─────────────────────────────────────────────┘               │
│                                                                  │
│   API 层（app/api/）                                              │
│   ┌────────────────┐ ┌──────────────────┐ ┌────────────────┐    │
│   │ /api/chat      │ │ /api/conversations│ │/api/messages   │    │
│   │ POST           │ │ GET 列表          │ │ POST 保存一轮   │    │
│   │ 转发给上游 AI  │ │ POST 新建         │ │ user+assistant  │    │
│   │ 流式 SSE 解析  │ │ /api/conv/[id]    │ │ 事务原子性      │    │
│   │ 超时+中断处理  │ │ GET 历史/DELETE   │ │ 首条消息生成标题 │    │
│   │                │ │ PATCH 重命名      │ │                 │    │
│   └───────┬────────┘ └─────────┬────────┘ └────────┬───────┘    │
│           │                    │                   │             │
│           ↓                    ↓                   ↓             │
│   ┌──────────────────────────────────────────────────────────┐  │
│   │ lib/queries.ts（CRUD 封装，prepared statements 防注入）   │  │
│   │ lib/db.ts（node:sqlite 单例连接 + 建表 + 事务辅助）        │  │
│   └──────────────────────────────────────────────────────────┘  │
│                          ↓                                       │
│                   chat.db（SQLite 文件）                         │
└─────────────────────────────────────────────────────────────────┘
```

### 各层职责一句话总结

| 层 | 文件 | 职责 |
|---|---|---|
| **全局状态** | `lib/store.ts` | Zustand store，组件外的单一数据源，持有会话/消息/loading 等 |
| **路由（壳）** | `app/chat/layout.tsx` | 服务端组件，包住 Sidebar + children，切换路由不重建 |
| **路由（页）** | `app/chat/page.tsx`、`[id]/page.tsx` | 服务端组件，取 URL 参数 + 渲染同步子组件 + ChatBox |
| **URL↔store 桥梁** | `components/SyncConversationId.tsx` | 客户端组件，用 useEffect 把 URL 的 id 同步进 store |
| **聊天交互** | `components/ChatBox.tsx` | 客户端，消息显示+发送+流式+Markdown+中止 |
| **会话管理** | `components/Sidebar.tsx` | 客户端，会话列表+切换+重命名+删除 |
| **API** | `app/api/*/route.ts` | 后端接口，转发 AI / 查改会话 / 保存消息 |
| **数据访问** | `lib/queries.ts`、`lib/db.ts` | SQL 封装 + 数据库连接 |

---

## 二、数据流：一次完整的"新建会话并发消息"

这是理解架构最好的方式——跟着数据走一遍。假设用户在 `/chat`（无 id）发首条消息：

```
①  ChatBox.handleSend 触发
      ↓
②  setMessages([user, 空assistant]) → 写进 store
      ↓
③  发现 currentId 是 null → POST /api/conversations 创建会话
      ↓
    返回 id（如 "f18b6707-..."）
      ↓
④  setJustCreatedId(id) → 标记"刚建的会话"（防 useEffect 误覆盖）
      ↓
⑤  router.push("/chat/f18b6707-...")
      ↓
⑥  Next.js 客户端导航：URL 变 /chat/f18b6707-...
      ├─ app/chat/layout.tsx 不动（Sidebar 保留）
      ├─ children 从 /chat/page.tsx 换成 /chat/[id]/page.tsx
      │
      ├─ [id]/page.tsx（服务端）await params 拿到 id
      │     ↓ 渲染 <SyncConversationId id/> + <ChatBox/>
      │
      └─ SyncConversationId（客户端）useEffect 跑
            ↓ store.setCurrentId("f18b6707-...")
            ↓ store.currentId 变化
            ↓
⑦  ChatBox 重建（新实例），useEffect 跑（依赖 currentId）
      ├─ getState().justCreatedId 还在 → 跳过加载历史（保护生效）
      └─ store.messages 已有 [user, 空assistant]（步骤②写的），不丢
      ↓
⑧  旧 handleSend 继续：POST /api/chat 流式接收
      ├─ 每个 chunk：updateLastMessage(累积内容) → store 更新 → UI 显示
      └─ currentId 变成 id 后，justCreatedId 匹配 → 清标记
      ↓
⑨  流式结束（finally）：
      ├─ POST /api/messages 保存 user+assistant 到数据库
      └─ refreshConversations() → 侧边栏出现新会话
```

**核心要点**：
- **store 是真相源**，组件重建后从 store 读，不丢
- **justCreatedId 保护**让新建会话时 useEffect 不去数据库加载空历史
- **URL↔store 双向同步**：router.push 改 URL → SyncConversationId 同步进 store

---

## 三、阶段 12 之后新增/变动的文件清单

### 新增的文件（阶段 13-14）

| 文件 | 哪个阶段 | 为什么加 |
|---|---|---|
| **`lib/store.ts`** | 阶段 14 | Zustand 全局状态中心。**核心，必读** |
| **`components/SyncConversationId.tsx`** | 阶段 14 修复期 | URL→store 同步的客户端子组件 |
| `app/chat/layout.tsx` | 阶段 13 | chat 路由的壳子（Sidebar + children） |
| `13.多会话管理.md` | 阶段 13 | 文档 |
| `14.全局状态管理.md` | 阶段 14 | 文档 |
| `SQLite速通.md` | 阶段 12 | 数据库基础参考（不算变动） |

### 有变动的文件（阶段 13-14）

| 文件 | 变动 |
|---|---|
| **`components/ChatBox.tsx`** | 大改：从本地 useState 迁移到 store；加 justCreatedId 保护；handleSend 顺序调整 |
| **`components/Sidebar.tsx`** | 改：从 store 读 conversations/currentId；删 usePathname 解析；用 refreshConversations |
| **`app/chat/[id]/page.tsx`** | 改：不再传 conversationId prop，改为渲染 SyncConversationId 子组件 |
| **`app/chat/page.tsx`** | 改：加 ClearCurrentId 子组件 |
| `app/globals.css` | 阶段 13 加了侧边栏样式 |
| `app/api/conversations/route.ts` | 阶段 13 加了 GET（列表） |
| `app/api/conversations/[id]/route.ts` | 阶段 13 加了 PATCH（重命名） |
| `lib/queries.ts` | 阶段 13 加了 listConversations |

### 阶段 12 之后没动的文件（保持不变）

| 文件 | 说明 |
|---|---|
| `app/api/chat/route.ts` | AI 转发逻辑（阶段 10/11 后稳定） |
| `app/api/messages/route.ts` | 保存消息逻辑（阶段 12 后稳定） |
| `lib/db.ts` | 数据库连接（阶段 12 后稳定） |
| `components/Markdown.tsx` | Markdown 渲染（阶段 8/9 后稳定） |
| `app/layout.tsx`、`app/page.tsx`、`app/about/page.tsx` | 基础页面 |

---

## 四、三个一定要搞懂的关键设计

### 1. selector 订阅 vs getState 读（第五轮 debug 的核心）

```ts
// 订阅（值变会触发重渲染 + 如果在依赖数组里会触发 useEffect 重跑）
const justCreatedId = useChatStore((s) => s.justCreatedId);

// 只读一次（不订阅，不触发任何东西）
const createdId = useChatStore.getState().justCreatedId;
```

`justCreatedId` 故意用 getState 读，是为了避免"清标记触发 useEffect 重跑"。详见 `12b.会话持久化补充.md` 第五轮。

### 2. URL ↔ store 双向同步

```
URL → store：SyncConversationId（访问 /chat/abc 时把 abc 写进 store.currentId）
store → URL：Sidebar/ChatBox 的 router.push（切换会话时改 URL）
```

store 是真相源，URL 是可分享的镜像。详见 `14.全局状态管理.md` 第五节。

### 3. 服务端组件渲染客户端子组件

```
[id]/page.tsx（服务端）—— await params 取 id
    ↓ 把 id 作为 prop
SyncConversationId（客户端）—— useEffect 把 id 写进 store
```

各司其职。详见 `14.全局状态管理.md`。

---

## 五、推荐阅读顺序（重点）

重点理解阶段 14 的架构变动。按这个顺序看，由浅入深：

### 第一优先：理解"状态从哪来"

**1. `lib/store.ts`**（必读，这是阶段 14 的核心，84 行）
- store 持有哪些状态、哪些 action
- 重点看：`justCreatedId` 字段、`updateLastMessage`、`refreshConversations`
- 理解：为什么这些 state 要放 store 而不是组件 useState

**2. `14.全局状态管理.md`**（配合 store.ts 读）
- Zustand 是什么、为什么选它（vs Context / Redux）
- store 设计原则（什么进 store、什么留组件）
- URL↔store 双向同步机制

### 第二优先：理解"组件怎么用 store"

**3. `components/ChatBox.tsx`**（重点看前 100 行）
- 怎么用 selector 订阅 store：`useChatStore((s) => s.messages)`
- **重点**：useEffect 里 `useChatStore.getState().justCreatedId` —— 为什么用 getState 而不是 selector（第五轮 debug 的核心）
- handleSend 里 setMessages → setJustCreatedId → router.push 的顺序

**4. `components/Sidebar.tsx`**（对比 ChatBox 看）
- 同样从 store 读，但更简单（没有 justCreatedId 那些）

### 第三优先：理解"URL 和 store 怎么同步"

**5. `components/SyncConversationId.tsx`**（短，36 行）
- URL→store 同步的桥梁
- 配合 `app/chat/[id]/page.tsx` 看：服务端组件取 id → 传给客户端子组件

**6. `app/chat/[id]/page.tsx`** + **`app/chat/page.tsx`**
- 服务端组件 + 客户端子组件的协作模式

### 第四优先：理解 debug 演进（可选，但很有价值）

**7. `12b.会话持久化补充.md`** 的第四、五轮
- 第四轮：两个残留 bug（消息被吃 + 侧边栏不刷新）的根因
- **第五轮最有价值**：useEffect 依赖自我触发的根因 + 日志调试过程

---

## 六、阶段 12 之后的核心变化（一句话概括）

**从"组件内 state + URL 当通信管道"升级到"全局 store + URL→store 单向同步"**。
（注：当前只有 URL→store 一个方向；setCurrentId 的幂等写法为未来加反向同步预留，详见 14 文档第九节）

- **阶段 12**：messages 放在 ChatBox 的 useState 里，靠 URL 传 conversationId → 组件重建丢 state → bug
- **阶段 13**：加 Sidebar，但 Sidebar 和 ChatBox 还是各自管 state，靠 URL 间接通信 → 同类隐患
- **阶段 14**：引入 Zustand，把跨组件、跨重建需要存活的 state 提到组件外的 store → 组件重建后从 store 读，根治

这个变化的本质是**架构升级**，不是单纯的 bug 修复。这也是为什么前几轮 debug 一直在打补丁失败——根因需要架构层面的改动才能解决。

---

## 七、遗留的改进点（已记录在 待学与待办.md）

1. **断连也能保存**：后端 tee 流边写边存，避免前端断开时丢失已生成内容（留到合入 LangChain 时做）
2. **新建会话 useEffect 时序**：当前用 justCreatedId 保护已基本解决，但理论上仍有"刚保存前的极短窗口"，配合改进点 1 可彻底解决

这两点不影响正常使用，是面试可以讲的"已知限制 + 改进方向"。
