# 阶段 14：全局状态管理（Zustand）

> 这是阶段 12/13 遗留 bug 的根治方案，也是多会话管理的架构升级。原本计划"合入 LangChain 时引入全局状态管理"，但因为修复"新建会话首条消息被吃"的 bug 需要它，提前到阶段 13 做多会话管理时一起做了。
>
> **本阶段做的事**：
> 1. 引入 Zustand 作为全局状态管理库
> 2. 把跨组件、跨重建需要存活的状态从组件 useState 迁移到 store
> 3. 建立 URL ↔ store 双向同步
> 4. 根治阶段 12 遗留的竞态 bug

---

## 一、为什么需要全局状态管理

### 痛点：组件重建丢失 state

阶段 12 的设计里，`messages`、`systemPrompt`、`loading` 都放在 ChatBox 的 `useState` 里。这在单页面时没问题，但引入路由跳转后出了 bug：

```
用户从 /chat（无 id）发首条消息
  → handleSend 创建会话 → router.push("/chat/abc")
  → Next.js 卸载 /chat 的 ChatBox，挂载 /chat/abc 的新 ChatBox
  → 新 ChatBox 的 messages 是空的（useState 初始值 []）
  → 新 ChatBox 的 useEffect 取刚创建会话的历史（空）→ setMessages([])
  → 用户看到空白，首条消息"被吃"
```

根因：**state 存在组件里，组件重建就没了**。详见 `12b.会话持久化补充.md`。

### 痛点二：跨组件共享状态

阶段 13 加了 Sidebar（侧边栏），它和 ChatBox 需要共享"当前是哪个会话"（currentId）和"会话列表"。但两个组件各自有自己的 state，没法直接共享——之前靠 URL 当"通信管道"（Sidebar 解析 `usePathname` 拿 currentId），这种间接方式脆弱、易出 bug。

### 解法：把 state 提到组件外

全局状态管理的核心思想：**把需要跨组件、跨重建共享的 state，放在组件树之外的"单一数据源"里**。组件通过订阅它来读、通过调 action 来改。组件销毁重建都不影响 store——它在"外面"。

```
改造前：
  ChatBox 的 useState(messages)
  Sidebar 的 useState(conversations)
  两个组件各自管各自的，互不相通，重建即丢

改造后：
  store（在组件外）
    ├─ messages
    ├─ conversations
    ├─ currentId
    └─ ...
  ChatBox 订阅 store.messages
  Sidebar 订阅 store.conversations + store.currentId
  组件重建无所谓，store 一直在
```

---

## 二、为什么选 Zustand

全局状态管理库有几个选择，对比一下：

| | Zustand | Redux | React Context |
|---|---|---|---|
| 学习曲线 | 极低，一个 `create` 搞定 | 高，有 action/reducer/dispatch 样板 | 低，是 React 自带 |
| 样板代码 | 极少 | 多（要写 reducer、action types、connect） | 中等（要建 Provider） |
| 精准订阅 | ✅ selector 只订阅需要的字段 | ✅ 但要配 reselect | ❌ context 值变，所有消费者都重渲染 |
| 是否要 Provider | ❌ 不需要 | ✅ 需要 `<Provider>` | ✅ 需要 `<Provider>` |
| 包大小 | ~1KB | 较大 | 0（内置） |
| 适合 | 中小型项目、快速上手 | 大型项目、严格架构 | 简单共享、低频更新 |

**选 Zustand 的理由**：
1. **最轻量**：没有 Provider 包裹，组件直接 `useStore` 就能用
2. **精准订阅**：用 selector `useChatStore((s) => s.messages)` 只在 messages 变化时重渲染，不是整个 store 变都重渲染
3. **学习成本最低**：一个 `create` 函数定义所有状态和 action，没有 Redux 的概念负担
4. **本项目规模合适**：单个 store 够用，不需要 Redux 的复杂度

---

## 三、store 设计（`lib/store.ts`）

### 什么进 store、什么留组件 useState

这是个重要判断标准——**不是所有 state 都该进 store**：

| 进 store | 留组件 useState |
|---|---|
| 跨组件共享的（conversations、currentId） | 纯 UI 临时状态（input 输入框值） |
| 跨重建需存活的（messages、systemPrompt、loading） | 只在单个组件内用、重建丢了也无所谓的（systemOpen 折叠态、editingId 重命名） |
| 多处读取的 | 局部的 |

**判断口诀**：如果这个 state 重建后必须保留 → 进 store；如果重建后从空开始也无所谓 → 留组件。

### store 的状态和 action

```ts
// 状态
conversations: Conversation[]   // 所有会话列表（Sidebar 用）
currentId: string | null        // 当前选中会话（Sidebar + ChatBox 共用）
messages: Message[]             // 当前会话的消息（ChatBox 用）
systemPrompt: string            // 当前会话的 system 提示词
loading: boolean                // 是否正在生成

// actions（操作函数）
setCurrentId(id)                // 设当前会话（幂等：id 没变就不动，为未来反向同步预留）
setMessages(msgs)               // 整体替换消息列表
appendMessage(msg)              // 追加一条
updateLastMessage(content)      // 流式追加用：把最后一条的 content 整体替换
setLoading(val)
setSystemPrompt(prompt)
setConversations(convs)         // 会话列表整体替换
```

### create 函数的写法

```ts
import { create } from "zustand";

export const useChatStore = create<ChatState>((set) => ({
  // 初始状态
  conversations: [],
  currentId: null,
  messages: [],
  // ...

  // actions：用 set 修改状态
  setMessages: (messages) => set({ messages }),

  appendMessage: (message) =>
    set((state) => ({ messages: [...state.messages, message] })),

  updateLastMessage: (content) =>
    set((state) => {
      if (state.messages.length === 0) return state;
      const next = [...state.messages];
      const last = next[next.length - 1];
      next[next.length - 1] = { ...last, content };
      return { messages: next };
    }),

  // 幂等写法（值没变返回原引用，Zustand 不更新）——为未来反向同步预留
  setCurrentId: (id) =>
    set((state) => (state.currentId === id ? state : { currentId: id })),
}));
```

**关键点**：
- `set` 可以接对象（直接替换字段）或函数（拿到 `state` 基于它改）
- `set((state) => ...)` 返回的会**合并**到现有 state，不是整体替换（除非第二个参数传 true）
- `setCurrentId` 里的 `state.currentId === id ? state : ...` 是幂等写法（值没变就返回原引用，Zustand 不触发更新）——当前是单向同步用不上，为未来反向同步预留，详见第九节

---

## 四、组件如何用 store（selector 订阅）

### 读状态：用 selector 精准订阅

```tsx
// ✅ 好的写法：用 selector 只订阅需要的字段
const messages = useChatStore((s) => s.messages);      // 只在 messages 变时重渲染
const loading = useChatStore((s) => s.loading);         // 只在 loading 变时重渲染

// ❌ 不好的写法：订阅整个 store，任何字段变都重渲染
const store = useChatStore();  // 别这么写
const messages = store.messages;
```

selector 是个函数 `(state) => 你要的字段`。Zustand 会比较这个函数返回值，变了才触发重渲染。**这是性能优化的关键**——避免无关 state 变化导致不必要重渲染。

### 读 action：也用 selector

```tsx
const setMessages = useChatStore((s) => s.setMessages);
```

action 是函数，引用稳定（不会变），所以订阅它不会触发重渲染。但用 selector 写法保持一致、清晰。

### 在非渲染上下文读：getState

```tsx
// 在事件处理函数、catch 块里，需要读 store 最新值（不是渲染闭包的旧值）
const lastContent = useChatStore.getState().messages.slice(-1)[0]?.content ?? "";
```

`getState()` 返回 store 当前的快照，不订阅。适合在 handler 里"实时读一次"。本项目的 catch 块用了它（因为 `messages` 闭包值是发送前的，而 store 已经被流式更新过）。

---

## 五、URL 与 store 的同步（当前是单向）

引入 store 后，"当前是哪个会话"在 store.currentId 里。但 URL 仍要保留 `/chat/abc`（可分享、刷新恢复、前进后退）。

### ⚠️ 诚实说明：当前只有 URL → store 单向同步

本项目**目前只有 URL → store 一个方向**：

```
方向 A（存在）：URL → store
  URL 变了（用户访问/刷新/前进后退/点击会话触发 router.push）
  → SyncConversationId 跑 → setCurrentId(id) → store 更新

方向 B（不存在）：store → URL
  ❌ 当前代码没有任何"监听 store.currentId 变化然后 router.push"的逻辑
```

`router.push` 只在**用户的主动操作**里调用（Sidebar 点击会话、ChatBox 新建会话），不是"订阅 store 变化触发的"。所以数据流是：**用户操作 → 改 URL → URL 同步给 store**，单向。

之前文档说"双向同步"是**言过其实**——它假设了"store 变了会反向改 URL"，但代码里没有。改成诚实的表述。

### 那 setCurrentId 的防御是干嘛的（见第九节）

既然是单向、不会死循环，`setCurrentId` 里"值没变就返回原引用"的判断**在当前代码几乎用不上**。它是**防御性编程**——为未来可能加的"store → URL 反向同步"预留安全网。详见第九节。

### URL → store（用户访问/刷新/前进后退 URL 时）

问题：Next.js 的 `page.tsx` 是服务端组件，不能直接用 Zustand（客户端 hook）。解法是写一个**客户端子组件**做同步。

`app/chat/[id]/page.tsx`：
```tsx
// 客户端组件：收到 id prop，同步进 store
function SyncConversationId({ id }: { id: string }) {
  const setCurrentId = useChatStore((s) => s.setCurrentId);
  useEffect(() => {
    setCurrentId(id);
  }, [id, setCurrentId]);
  return null;  // 不渲染东西，只做副作用
}

// 服务端组件 page：渲染 SyncConversationId + ChatBox
export default async function ChatByIdPage({ params }) {
  const { id } = await params;
  return (
    <main>
      <SyncConversationId id={id} />
      <ChatBox />
    </main>
  );
}
```

`SyncConversationId` 是个"哑组件"——不渲染 UI，只在 mount 时把 URL 的 id 同步进 store。它存在的唯一价值是"在服务端组件里桥接客户端 store"。

`app/chat/page.tsx`（无 id 入口）对称地写一个 `ClearCurrentId`，访问 /chat 时清空 currentId。

#### 补充概念：逻辑组件（effect-only component）

`SyncConversationId` 这种"只做副作用、不渲染 UI（`return null`）"的组件，在 React 里有个名字叫**逻辑组件**（也叫 effect-only component）。它的形态固定：

```tsx
function SomeLogic({ deps }: { deps: SomeType }) {
  useEffect(() => {
    // 做点事（同步、订阅、埋点等）
  }, [deps]);
  return null;  // ← 不往 DOM 渲染任何东西
}
```

**为什么需要逻辑组件**：React 的副作用（useEffect）必须挂在组件生命周期上——脱离组件，useEffect 无处可挂。所以"我想让副作用 X 在某时机跑"必须有个组件来承载它。

**为什么放 JSX 里**：因为 React 里让 useEffect 跑起来只有两种方式——
- 方式 A：渲染一个组件（`<SyncConversationId/>`），它 mount 时 useEffect 自动跑
- 方式 B：自定义 hook（`useSyncConversationId(id)`），但必须在另一个**客户端**组件内部调

本项目必须用方式 A：page 是服务端组件（要用 `await params`），不能用 hook；所以把副作用拆到一个客户端子组件里，page 渲染它来触发 mount → useEffect 跑。

**判断标志**：看到 `return null` 的组件，它就是个"为了跑 useEffect 而存在的隐形监听器"。放 JSX 里不是因为要显示它，是因为**不渲染它就不 mount、不 mount 它的 useEffect 就不跑**。

**常见场景**：
- 数据同步（本项目的 URL→store）
- 埋点上报（页面加载时发统计数据，不显示东西）
- 订阅外部事件（监听窗口 resize、websocket 消息）
- 预加载（加载下个页面需要的资源）

这些都是"需要副作用在某时机跑、但不需要显示 UI"的场景。

### store → URL（用户在 UI 里切换会话时）

Sidebar 切会话、ChatBox 新建会话时调 `router.push`：

```tsx
// Sidebar.handleSelect
function handleSelect(id: string) {
  router.push(`/chat/${id}`);  // 改 URL → 触发 URL→store 同步
}

// ChatBox 新建会话后
router.push(`/chat/${activeConvId}`);
```

URL 变了 → Next.js 渲染对应的 page → page 里的 SyncConversationId 把新 id 同步进 store → store.currentId 变 → ChatBox 的 useEffect 触发加载历史。

### 防御性编程：setCurrentId 的幂等写法

```ts
setCurrentId: (id) =>
  set((state) => (state.currentId === id ? state : { currentId: id })),
//                                ↑ 如果 id 没变，返回原 state（同一个引用）
```

#### 先说清楚：当前代码用不上这个防御

本项目目前只有 URL → store 单向同步（见第五节），不会产生循环。`setCurrentId` 被调用的场景：

| 场景 | currentId 之前 | 调 setCurrentId(新值) | 防御生效吗 |
|---|---|---|---|
| 用户点会话 B（之前在 A） | A | B | ❌ 不生效（值真变了，正常更新） |
| 刷新 /chat/B | null（刷新重置） | B | ❌ 不生效（值真变了） |
| 用户点当前会话 B | B | B | ✅ 生效——但 handleSelect 有 `if(id===currentId) return` 提前拦截，根本走不到这 |

所以在当前代码里，这个判断**几乎不会被触发**。

#### 那为什么还留着它：为"反向同步"预留安全网

这是**防御性编程**——为未来可能加的"store → URL 反向同步"逻辑预留保护。假设以后你在 Sidebar 里加：

```ts
// 假设性代码：订阅 currentId 变化，反向同步到 URL
useEffect(() => {
  router.push(`/chat/${currentId}`);   // currentId 变了就改 URL
}, [currentId]);
```

这就是真正的双向同步，会产生死循环：

```
setCurrentId("B") → currentId 变 B → useEffect 跑 → router.push("/chat/B")
  → URL 变 → SyncConversationId 跑 → setCurrentId("B")
    → currentId 变 B？ ← 防御在这里：值没变，返回原引用，Zustand 不更新
      → useEffect 不跑（currentId 没变）→ 链条终止 ✓
```

没有防御，最后一步会创建新 state、Zustand 认为变了、useEffect 又跑、又改 URL……无限循环。

#### 为什么"返回原引用"能终止

Zustand 用 `Object.is(newState, currentState)` 比较 set 前后的 state（比较引用，不是值）：

- 返回 `{ currentId: id }`（新对象）→ 引用不同 → Zustand 认为变了 → 通知订阅者
- 返回 `state`（原对象，同引用）→ `Object.is(state, state) === true` → Zustand 认为没变 → 不通知

**"返回原引用" = 告诉 Zustand 啥也没变**。这是利用 Zustand 引用比较机制的约定。

#### 这个模式什么时候该用

不是所有 action 都要这么写，**只有"可能被重复调用、且值相同时不该触发更新"的才需要**：

| 场景 | 要不要 |
|---|---|
| 双向同步（未来加反向同步时） | ✅ 要 |
| 事件监听器里的 setState（resize/scroll 频繁触发） | ✅ 要 |
| 用户输入触发的更新（输入框） | ❌ 不要（每次都是有意义的变更） |

判断口诀：这个 action 会不会被"重复、但实际没变化"地调用？会 → 加防御；不会 → 不用。

### 数据流全貌

```
用户点 Sidebar 的会话 B
  → handleSelect(B): router.push("/chat/B")      [store→URL]
  → URL 变成 /chat/B
  → Next.js 渲染 /chat/[id]/page.tsx（id=B）
  → SyncConversationId 调 store.setCurrentId(B)  [URL→store]
  → store.currentId 从 A 变成 B
  → ChatBox 的 useEffect 依赖 currentId，触发
  → ChatBox 加载 B 的历史（GET /api/conversations/B）
  → messages 更新，显示 B 的对话
```

---

## 六、ChatBox 改造要点

### 删掉的东西

```tsx
// 删：conversationId prop（改从 store 读 currentId）
export default function ChatBox({ conversationId }: ...)  // ❌

// 删：justCreatedRef（store 接管，不需要这个 hack）
const justCreatedRef = useRef(false);  // ❌ 删掉

// 删：本地 messages/systemPrompt/loading useState（改从 store 读）
const [messages, setMessages] = useState([]);  // ❌
const [systemPrompt, setSystemPrompt] = useState("");  // ❌
const [loading, setLoading] = useState(false);  // ❌
```

### 新增的东西

```tsx
// 从 store 读状态
const currentId = useChatStore((s) => s.currentId);
const messages = useChatStore((s) => s.messages);
const systemPrompt = useChatStore((s) => s.systemPrompt);
const loading = useChatStore((s) => s.loading);

// 从 store 读 actions
const setMessages = useChatStore((s) => s.setMessages);
const updateLastMessage = useChatStore((s) => s.updateLastMessage);
// ...

// 保留：纯 UI 状态 + abortRef
const [input, setInput] = useState("");
const [systemOpen, setSystemOpen] = useState(false);
const abortRef = useRef<AbortController | null>(null);
```

### 流式更新改用 updateLastMessage

```tsx
// 旧：本地 setMessages 追加 chunk
setMessages((prev) => {
  const next = [...prev];
  next[next.length - 1] = { ...last, content: last.content + chunk };
  return next;
});

// 新：用 store 的 updateLastMessage，传累积的完整 content
updateLastMessage(assistantContent);  // assistantContent 是同步累积的局部变量
```

### catch 里用 getState 读实时值

```tsx
// catch 块里，渲染闭包的 messages 是旧值（发送前的）
// 要读 store 实时值，用 getState
const lastContent = useChatStore.getState().messages.slice(-1)[0]?.content ?? "";
```

---

## 七、Sidebar 改造要点

```tsx
// 删：conversations useState（改从 store 读）
const [conversations, setConversations] = useState([]);  // ❌

// 删：usePathname 解析 currentId（改从 store 读）
const pathname = usePathname();  // ❌
const currentId = pathname.startsWith("/chat/") ? ... : null;  // ❌

// 删：[pathname] 依赖的 useEffect（改为 mount 时拉一次，create/rename/delete 后主动刷新）
useEffect(() => { loadConversations(); }, [pathname]);  // ❌

// 新：从 store 读
const conversations = useChatStore((s) => s.conversations);
const currentId = useChatStore((s) => s.currentId);
const setConversations = useChatStore((s) => s.setConversations);
```

handleSelect 不变（还是 `router.push(/chat/${id})`），但现在它通过 URL→store 同步间接更新 currentId，而不是直接改本地 state。

---

## 八、bug 的根治验证

改造后，原 bug 场景的流程：

```
用户从 /chat 发首条消息
  → handleSend 创建会话，messages 已进 store
  → router.push("/chat/abc")
  → Next.js 重建 ChatBox（新实例）
  → 新 ChatBox 从 store 读 messages ← 关键！store 里已经有刚发的消息了
  → SyncConversationId 把 abc 同步进 store.currentId
  → useEffect 触发，加载 abc 历史
  → 此时数据库可能还没存（保存是流式后），但 store 里 messages 已有内容
```

等等——这里 useEffect 加载历史如果数据库还是空的，会不会又覆盖？**会**，但这次不一样：因为组件是从 store 读，store 是单一数据源。只要 useEffect 的 `setMessages` 拿到空数组覆盖，就还是 bug。

实际上需要确认：新建会话场景下，useEffect 加载历史是不是还是问题。答案是：**流式完成后才保存**，所以 useEffect 在"刚创建、流式进行中"加载历史时，数据库是空的。这仍然是潜在竞态。

**更彻底的解法**：useEffect 加载历史时，如果 store 里 messages 已经有内容且 currentId 没变，跳过加载。但这会引入"切换会话时不加载"的新问题。

**当前实现的实际行为**：新建会话时，router.push 后 useEffect 会跑、会加载历史（空）、会覆盖 messages（变成空）。但因为流式 fetch 还在进行，下一个 chunk 会继续 updateLastMessage，把内容填回来。所以用户可能看到"闪一下空"然后内容回来——比之前"完全消失"好，但不完美。

**真正的根治需要后端 tee 边写边存**（待学与待办里的改进点），这样 useEffect 加载历史时数据库已有数据。这是个值得后续继续打磨的点。

> 注：上面这段是诚实记录——Zustand 解决了"组件重建丢 state"，但"新建会话时 useEffect 覆盖"的竞态仍部分存在。完整修复需要配合后端改动。

---

## 九、自测题

### Q1
为什么 `messages` 该进 store，而 `input`（输入框值）该留组件 useState？

### Q2
Zustand 的 selector 写法 `useChatStore((s) => s.messages)` 比直接 `useChatStore()` 好在哪？

### Q3
URL → store 同步为什么需要单独的客户端子组件（SyncConversationId）？不能直接在 page.tsx 里写吗？

### Q4
`setCurrentId` 里的 `state.currentId === id ? state : { currentId: id }` 解决什么问题？

### Q5
catch 块里为什么用 `useChatStore.getState()` 而不是直接读渲染时的 `messages`？

### Q6（进阶）
引入 store 后，"新建会话时 useEffect 加载空历史覆盖 messages"的问题完全消失了吗？为什么？

<details>
<summary>答案</summary>

**A1**：messages 需要跨重建存活（组件重建后还要显示），且未来要跨组件共享（Sidebar 可能要显示最近消息）→ 进 store。input 是纯 UI 临时状态，重建后从空开始无所谓 → 留组件。

**A2**：selector 只在 messages 变化时触发重渲染。直接 `useChatStore()` 订阅整个 store，任何字段（哪怕无关的 loading）变都重渲染，性能差。

**A3**：page.tsx 是服务端组件，不能用 Zustand（它是客户端 hook，依赖 React 的客户端运行时）。抽一个客户端子组件，它能在浏览器里运行、用 useEffect 调 store。

**A4**：幂等写法（防御性编程）。`setCurrentId` 判断 id 没变就返回原 state 引用，Zustand 用 `Object.is` 比较发现引用相同、不触发更新。**诚实说**：本项目当前只有 URL→store 单向同步，这个防御几乎用不上；它是为未来可能加的"store→URL 反向同步"预留的安全网——反向同步会死循环（store 变→改 URL→URL 又同步回 store），幂等写法能在"值没变"时断开链条。

**A5**：渲染时的 messages 是闭包捕获的旧值（发送前的，那时流式还没开始）。catch 执行时，流式已经多次 updateLastMessage 改过 store，但渲染闭包看不到。getState() 读 store 实时快照，拿到的是最新值。

**A6**：没有完全消失。Zustand 解决了"组件重建丢 state"，但 useEffect 本身还是会跑、会加载历史。新建会话时数据库还没存（保存是流式后），useEffect 拿到空历史会覆盖。但因为组件从 store 读 + 流式持续 updateLastMessage，内容会被填回。用户可能看到"闪一下空"。真正根治需要后端 tee 边写边存（待学与待办的改进点）。

</details>

---

## 十、阶段总结

1. **全局状态管理是分水岭**：跨组件、跨重建共享的状态，应该放 store，不该堆在组件 useState
2. **Zustand 是轻量优选**：无 Provider、selector 精准订阅、一个 create 搞定，学习成本低
3. **URL → store 单向同步**：URL 是可分享的会话标识，store 是当前状态；SyncConversationId 把 URL 的 id 写进 store。setCurrentId 的幂等写法是为未来反向同步预留的防御
4. **服务端组件用客户端 store**：抽哑子组件（SyncConversationId）做副作用桥接
5. **getState 读实时值**：事件处理函数里读 store 用 getState，不用渲染闭包的旧值

**下一阶段**：阶段 15 合入 LangChain + RAG。store 会进一步扩展（存检索结果、文档列表等）。现在的 store 结构为后续扩展打好了基础。
